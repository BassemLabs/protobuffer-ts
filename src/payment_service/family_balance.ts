// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: payment_service/family_balance.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";
import { AuditPrincipal } from "../utils/audit_actor";
import { ObjectId } from "../utils/object_id";
import { RequestContext } from "../utils/request_context";
import { InvoiceResponse } from "./invoice";

export const protobufPackage = "payment_service";

export enum FamilyBalanceEntryType {
  FAMILY_BALANCE_ENTRY_TYPE_UNSPECIFIED = "FAMILY_BALANCE_ENTRY_TYPE_UNSPECIFIED",
  FAMILY_BALANCE_PAYMENT = "FAMILY_BALANCE_PAYMENT",
  FAMILY_BALANCE_APPLIED = "FAMILY_BALANCE_APPLIED",
  FAMILY_BALANCE_APPLICATION_REVERSED = "FAMILY_BALANCE_APPLICATION_REVERSED",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function familyBalanceEntryTypeFromJSON(object: any): FamilyBalanceEntryType {
  switch (object) {
    case 0:
    case "FAMILY_BALANCE_ENTRY_TYPE_UNSPECIFIED":
      return FamilyBalanceEntryType.FAMILY_BALANCE_ENTRY_TYPE_UNSPECIFIED;
    case 1:
    case "FAMILY_BALANCE_PAYMENT":
      return FamilyBalanceEntryType.FAMILY_BALANCE_PAYMENT;
    case 2:
    case "FAMILY_BALANCE_APPLIED":
      return FamilyBalanceEntryType.FAMILY_BALANCE_APPLIED;
    case 3:
    case "FAMILY_BALANCE_APPLICATION_REVERSED":
      return FamilyBalanceEntryType.FAMILY_BALANCE_APPLICATION_REVERSED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return FamilyBalanceEntryType.UNRECOGNIZED;
  }
}

export function familyBalanceEntryTypeToJSON(object: FamilyBalanceEntryType): string {
  switch (object) {
    case FamilyBalanceEntryType.FAMILY_BALANCE_ENTRY_TYPE_UNSPECIFIED:
      return "FAMILY_BALANCE_ENTRY_TYPE_UNSPECIFIED";
    case FamilyBalanceEntryType.FAMILY_BALANCE_PAYMENT:
      return "FAMILY_BALANCE_PAYMENT";
    case FamilyBalanceEntryType.FAMILY_BALANCE_APPLIED:
      return "FAMILY_BALANCE_APPLIED";
    case FamilyBalanceEntryType.FAMILY_BALANCE_APPLICATION_REVERSED:
      return "FAMILY_BALANCE_APPLICATION_REVERSED";
    case FamilyBalanceEntryType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function familyBalanceEntryTypeToNumber(object: FamilyBalanceEntryType): number {
  switch (object) {
    case FamilyBalanceEntryType.FAMILY_BALANCE_ENTRY_TYPE_UNSPECIFIED:
      return 0;
    case FamilyBalanceEntryType.FAMILY_BALANCE_PAYMENT:
      return 1;
    case FamilyBalanceEntryType.FAMILY_BALANCE_APPLIED:
      return 2;
    case FamilyBalanceEntryType.FAMILY_BALANCE_APPLICATION_REVERSED:
      return 3;
    case FamilyBalanceEntryType.UNRECOGNIZED:
    default:
      return -1;
  }
}

export interface FamilyBalanceEntry {
  id: ObjectId | undefined;
  organization: ObjectId | undefined;
  family: ObjectId | undefined;
  amount?: number | undefined;
  entry_type?: FamilyBalanceEntryType | undefined;
  invoice: ObjectId | undefined;
  transaction: ObjectId | undefined;
  school_year: ObjectId | undefined;
  created_at:
    | Date
    | undefined;
  /** Staff-only audit metadata. Parent-facing responses omit this field. */
  created_by?: AuditPrincipal | undefined;
}

export interface FamilyBalanceSummary {
  family: ObjectId | undefined;
  available_balance?: number | undefined;
  entries: FamilyBalanceEntry[];
  total_entries?: number | undefined;
}

export interface CreateFamilyBalanceInvoiceRequest {
  context: RequestContext | undefined;
  family: ObjectId | undefined;
  school_year: ObjectId | undefined;
  amount?: number | undefined;
}

export interface GetFamilyBalanceRequest {
  context: RequestContext | undefined;
  family: ObjectId | undefined;
  page?: number | undefined;
  page_size?: number | undefined;
}

export interface ApplyFamilyBalanceRequest {
  context: RequestContext | undefined;
  family: ObjectId | undefined;
  school_year: ObjectId | undefined;
  amount?: number | undefined;
}

export interface ApplyFamilyBalanceResponse {
  balance: FamilyBalanceSummary | undefined;
  invoices: InvoiceResponse[];
  applied_amount?: number | undefined;
}

function createBaseFamilyBalanceEntry(): FamilyBalanceEntry {
  return {
    id: undefined,
    organization: undefined,
    family: undefined,
    amount: undefined,
    entry_type: undefined,
    invoice: undefined,
    transaction: undefined,
    school_year: undefined,
    created_at: undefined,
    created_by: undefined,
  };
}

export const FamilyBalanceEntry: MessageFns<FamilyBalanceEntry> = {
  encode(message: FamilyBalanceEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      ObjectId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.family !== undefined) {
      ObjectId.encode(message.family, writer.uint32(26).fork()).join();
    }
    if (message.amount !== undefined) {
      writer.uint32(33).double(message.amount);
    }
    if (message.entry_type !== undefined) {
      writer.uint32(40).int32(familyBalanceEntryTypeToNumber(message.entry_type));
    }
    if (message.invoice !== undefined) {
      ObjectId.encode(message.invoice, writer.uint32(50).fork()).join();
    }
    if (message.transaction !== undefined) {
      ObjectId.encode(message.transaction, writer.uint32(58).fork()).join();
    }
    if (message.school_year !== undefined) {
      ObjectId.encode(message.school_year, writer.uint32(66).fork()).join();
    }
    if (message.created_at !== undefined) {
      Timestamp.encode(toTimestamp(message.created_at), writer.uint32(74).fork()).join();
    }
    if (message.created_by !== undefined) {
      AuditPrincipal.encode(message.created_by, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FamilyBalanceEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFamilyBalanceEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.family = ObjectId.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 33) {
            break;
          }

          message.amount = reader.double();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.entry_type = familyBalanceEntryTypeFromJSON(reader.int32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.invoice = ObjectId.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.transaction = ObjectId.decode(reader, reader.uint32());
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.school_year = ObjectId.decode(reader, reader.uint32());
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.created_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.created_by = AuditPrincipal.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FamilyBalanceEntry {
    return {
      id: isSet(object.id) ? ObjectId.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      family: isSet(object.family) ? ObjectId.fromJSON(object.family) : undefined,
      amount: isSet(object.amount) ? globalThis.Number(object.amount) : undefined,
      entry_type: isSet(object.entryType) ? familyBalanceEntryTypeFromJSON(object.entryType) : undefined,
      invoice: isSet(object.invoice) ? ObjectId.fromJSON(object.invoice) : undefined,
      transaction: isSet(object.transaction) ? ObjectId.fromJSON(object.transaction) : undefined,
      school_year: isSet(object.schoolYear) ? ObjectId.fromJSON(object.schoolYear) : undefined,
      created_at: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      created_by: isSet(object.createdBy) ? AuditPrincipal.fromJSON(object.createdBy) : undefined,
    };
  },

  toJSON(message: FamilyBalanceEntry): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = ObjectId.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.family !== undefined) {
      obj.family = ObjectId.toJSON(message.family);
    }
    if (message.amount !== undefined) {
      obj.amount = message.amount;
    }
    if (message.entry_type !== undefined) {
      obj.entryType = familyBalanceEntryTypeToJSON(message.entry_type);
    }
    if (message.invoice !== undefined) {
      obj.invoice = ObjectId.toJSON(message.invoice);
    }
    if (message.transaction !== undefined) {
      obj.transaction = ObjectId.toJSON(message.transaction);
    }
    if (message.school_year !== undefined) {
      obj.schoolYear = ObjectId.toJSON(message.school_year);
    }
    if (message.created_at !== undefined) {
      obj.createdAt = message.created_at.toISOString();
    }
    if (message.created_by !== undefined) {
      obj.createdBy = AuditPrincipal.toJSON(message.created_by);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FamilyBalanceEntry>, I>>(base?: I): FamilyBalanceEntry {
    return FamilyBalanceEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FamilyBalanceEntry>, I>>(object: I): FamilyBalanceEntry {
    const message = createBaseFamilyBalanceEntry();
    message.id = (object.id !== undefined && object.id !== null) ? ObjectId.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.family = (object.family !== undefined && object.family !== null)
      ? ObjectId.fromPartial(object.family)
      : undefined;
    message.amount = object.amount ?? undefined;
    message.entry_type = object.entry_type ?? undefined;
    message.invoice = (object.invoice !== undefined && object.invoice !== null)
      ? ObjectId.fromPartial(object.invoice)
      : undefined;
    message.transaction = (object.transaction !== undefined && object.transaction !== null)
      ? ObjectId.fromPartial(object.transaction)
      : undefined;
    message.school_year = (object.school_year !== undefined && object.school_year !== null)
      ? ObjectId.fromPartial(object.school_year)
      : undefined;
    message.created_at = object.created_at ?? undefined;
    message.created_by = (object.created_by !== undefined && object.created_by !== null)
      ? AuditPrincipal.fromPartial(object.created_by)
      : undefined;
    return message;
  },
};

function createBaseFamilyBalanceSummary(): FamilyBalanceSummary {
  return { family: undefined, available_balance: undefined, entries: [], total_entries: undefined };
}

export const FamilyBalanceSummary: MessageFns<FamilyBalanceSummary> = {
  encode(message: FamilyBalanceSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.family !== undefined) {
      ObjectId.encode(message.family, writer.uint32(10).fork()).join();
    }
    if (message.available_balance !== undefined) {
      writer.uint32(17).double(message.available_balance);
    }
    for (const v of message.entries) {
      FamilyBalanceEntry.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.total_entries !== undefined) {
      writer.uint32(32).uint64(message.total_entries);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FamilyBalanceSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFamilyBalanceSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.family = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 17) {
            break;
          }

          message.available_balance = reader.double();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.entries.push(FamilyBalanceEntry.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.total_entries = longToNumber(reader.uint64());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FamilyBalanceSummary {
    return {
      family: isSet(object.family) ? ObjectId.fromJSON(object.family) : undefined,
      available_balance: isSet(object.availableBalance) ? globalThis.Number(object.availableBalance) : undefined,
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => FamilyBalanceEntry.fromJSON(e))
        : [],
      total_entries: isSet(object.totalEntries) ? globalThis.Number(object.totalEntries) : undefined,
    };
  },

  toJSON(message: FamilyBalanceSummary): unknown {
    const obj: any = {};
    if (message.family !== undefined) {
      obj.family = ObjectId.toJSON(message.family);
    }
    if (message.available_balance !== undefined) {
      obj.availableBalance = message.available_balance;
    }
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => FamilyBalanceEntry.toJSON(e));
    }
    if (message.total_entries !== undefined) {
      obj.totalEntries = Math.round(message.total_entries);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FamilyBalanceSummary>, I>>(base?: I): FamilyBalanceSummary {
    return FamilyBalanceSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FamilyBalanceSummary>, I>>(object: I): FamilyBalanceSummary {
    const message = createBaseFamilyBalanceSummary();
    message.family = (object.family !== undefined && object.family !== null)
      ? ObjectId.fromPartial(object.family)
      : undefined;
    message.available_balance = object.available_balance ?? undefined;
    message.entries = object.entries?.map((e) => FamilyBalanceEntry.fromPartial(e)) || [];
    message.total_entries = object.total_entries ?? undefined;
    return message;
  },
};

function createBaseCreateFamilyBalanceInvoiceRequest(): CreateFamilyBalanceInvoiceRequest {
  return { context: undefined, family: undefined, school_year: undefined, amount: undefined };
}

export const CreateFamilyBalanceInvoiceRequest: MessageFns<CreateFamilyBalanceInvoiceRequest> = {
  encode(message: CreateFamilyBalanceInvoiceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.family !== undefined) {
      ObjectId.encode(message.family, writer.uint32(18).fork()).join();
    }
    if (message.school_year !== undefined) {
      ObjectId.encode(message.school_year, writer.uint32(26).fork()).join();
    }
    if (message.amount !== undefined) {
      writer.uint32(33).double(message.amount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateFamilyBalanceInvoiceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateFamilyBalanceInvoiceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.family = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.school_year = ObjectId.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 33) {
            break;
          }

          message.amount = reader.double();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateFamilyBalanceInvoiceRequest {
    return {
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      family: isSet(object.family) ? ObjectId.fromJSON(object.family) : undefined,
      school_year: isSet(object.schoolYear) ? ObjectId.fromJSON(object.schoolYear) : undefined,
      amount: isSet(object.amount) ? globalThis.Number(object.amount) : undefined,
    };
  },

  toJSON(message: CreateFamilyBalanceInvoiceRequest): unknown {
    const obj: any = {};
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.family !== undefined) {
      obj.family = ObjectId.toJSON(message.family);
    }
    if (message.school_year !== undefined) {
      obj.schoolYear = ObjectId.toJSON(message.school_year);
    }
    if (message.amount !== undefined) {
      obj.amount = message.amount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateFamilyBalanceInvoiceRequest>, I>>(
    base?: I,
  ): CreateFamilyBalanceInvoiceRequest {
    return CreateFamilyBalanceInvoiceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateFamilyBalanceInvoiceRequest>, I>>(
    object: I,
  ): CreateFamilyBalanceInvoiceRequest {
    const message = createBaseCreateFamilyBalanceInvoiceRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.family = (object.family !== undefined && object.family !== null)
      ? ObjectId.fromPartial(object.family)
      : undefined;
    message.school_year = (object.school_year !== undefined && object.school_year !== null)
      ? ObjectId.fromPartial(object.school_year)
      : undefined;
    message.amount = object.amount ?? undefined;
    return message;
  },
};

function createBaseGetFamilyBalanceRequest(): GetFamilyBalanceRequest {
  return { context: undefined, family: undefined, page: undefined, page_size: undefined };
}

export const GetFamilyBalanceRequest: MessageFns<GetFamilyBalanceRequest> = {
  encode(message: GetFamilyBalanceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.family !== undefined) {
      ObjectId.encode(message.family, writer.uint32(18).fork()).join();
    }
    if (message.page !== undefined) {
      writer.uint32(24).uint32(message.page);
    }
    if (message.page_size !== undefined) {
      writer.uint32(32).uint32(message.page_size);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetFamilyBalanceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFamilyBalanceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.family = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.page = reader.uint32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.page_size = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFamilyBalanceRequest {
    return {
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      family: isSet(object.family) ? ObjectId.fromJSON(object.family) : undefined,
      page: isSet(object.page) ? globalThis.Number(object.page) : undefined,
      page_size: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : undefined,
    };
  },

  toJSON(message: GetFamilyBalanceRequest): unknown {
    const obj: any = {};
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.family !== undefined) {
      obj.family = ObjectId.toJSON(message.family);
    }
    if (message.page !== undefined) {
      obj.page = Math.round(message.page);
    }
    if (message.page_size !== undefined) {
      obj.pageSize = Math.round(message.page_size);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFamilyBalanceRequest>, I>>(base?: I): GetFamilyBalanceRequest {
    return GetFamilyBalanceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFamilyBalanceRequest>, I>>(object: I): GetFamilyBalanceRequest {
    const message = createBaseGetFamilyBalanceRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.family = (object.family !== undefined && object.family !== null)
      ? ObjectId.fromPartial(object.family)
      : undefined;
    message.page = object.page ?? undefined;
    message.page_size = object.page_size ?? undefined;
    return message;
  },
};

function createBaseApplyFamilyBalanceRequest(): ApplyFamilyBalanceRequest {
  return { context: undefined, family: undefined, school_year: undefined, amount: undefined };
}

export const ApplyFamilyBalanceRequest: MessageFns<ApplyFamilyBalanceRequest> = {
  encode(message: ApplyFamilyBalanceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.family !== undefined) {
      ObjectId.encode(message.family, writer.uint32(18).fork()).join();
    }
    if (message.school_year !== undefined) {
      ObjectId.encode(message.school_year, writer.uint32(26).fork()).join();
    }
    if (message.amount !== undefined) {
      writer.uint32(33).double(message.amount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApplyFamilyBalanceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApplyFamilyBalanceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.family = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.school_year = ObjectId.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 33) {
            break;
          }

          message.amount = reader.double();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApplyFamilyBalanceRequest {
    return {
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      family: isSet(object.family) ? ObjectId.fromJSON(object.family) : undefined,
      school_year: isSet(object.schoolYear) ? ObjectId.fromJSON(object.schoolYear) : undefined,
      amount: isSet(object.amount) ? globalThis.Number(object.amount) : undefined,
    };
  },

  toJSON(message: ApplyFamilyBalanceRequest): unknown {
    const obj: any = {};
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.family !== undefined) {
      obj.family = ObjectId.toJSON(message.family);
    }
    if (message.school_year !== undefined) {
      obj.schoolYear = ObjectId.toJSON(message.school_year);
    }
    if (message.amount !== undefined) {
      obj.amount = message.amount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ApplyFamilyBalanceRequest>, I>>(base?: I): ApplyFamilyBalanceRequest {
    return ApplyFamilyBalanceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ApplyFamilyBalanceRequest>, I>>(object: I): ApplyFamilyBalanceRequest {
    const message = createBaseApplyFamilyBalanceRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.family = (object.family !== undefined && object.family !== null)
      ? ObjectId.fromPartial(object.family)
      : undefined;
    message.school_year = (object.school_year !== undefined && object.school_year !== null)
      ? ObjectId.fromPartial(object.school_year)
      : undefined;
    message.amount = object.amount ?? undefined;
    return message;
  },
};

function createBaseApplyFamilyBalanceResponse(): ApplyFamilyBalanceResponse {
  return { balance: undefined, invoices: [], applied_amount: undefined };
}

export const ApplyFamilyBalanceResponse: MessageFns<ApplyFamilyBalanceResponse> = {
  encode(message: ApplyFamilyBalanceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.balance !== undefined) {
      FamilyBalanceSummary.encode(message.balance, writer.uint32(10).fork()).join();
    }
    for (const v of message.invoices) {
      InvoiceResponse.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.applied_amount !== undefined) {
      writer.uint32(25).double(message.applied_amount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApplyFamilyBalanceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApplyFamilyBalanceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.balance = FamilyBalanceSummary.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.invoices.push(InvoiceResponse.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 25) {
            break;
          }

          message.applied_amount = reader.double();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApplyFamilyBalanceResponse {
    return {
      balance: isSet(object.balance) ? FamilyBalanceSummary.fromJSON(object.balance) : undefined,
      invoices: globalThis.Array.isArray(object?.invoices)
        ? object.invoices.map((e: any) => InvoiceResponse.fromJSON(e))
        : [],
      applied_amount: isSet(object.appliedAmount) ? globalThis.Number(object.appliedAmount) : undefined,
    };
  },

  toJSON(message: ApplyFamilyBalanceResponse): unknown {
    const obj: any = {};
    if (message.balance !== undefined) {
      obj.balance = FamilyBalanceSummary.toJSON(message.balance);
    }
    if (message.invoices?.length) {
      obj.invoices = message.invoices.map((e) => InvoiceResponse.toJSON(e));
    }
    if (message.applied_amount !== undefined) {
      obj.appliedAmount = message.applied_amount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ApplyFamilyBalanceResponse>, I>>(base?: I): ApplyFamilyBalanceResponse {
    return ApplyFamilyBalanceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ApplyFamilyBalanceResponse>, I>>(object: I): ApplyFamilyBalanceResponse {
    const message = createBaseApplyFamilyBalanceResponse();
    message.balance = (object.balance !== undefined && object.balance !== null)
      ? FamilyBalanceSummary.fromPartial(object.balance)
      : undefined;
    message.invoices = object.invoices?.map((e) => InvoiceResponse.fromPartial(e)) || [];
    message.applied_amount = object.applied_amount ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
