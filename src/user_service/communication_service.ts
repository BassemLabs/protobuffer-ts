// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: user_service/communication_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  NotificationType,
  notificationTypeFromJSON,
  notificationTypeToJSON,
  notificationTypeToNumber,
} from "../utils/notification_type";
import { ObjectId } from "../utils/object_id";
import { RequestContext } from "../utils/request_context";
import { CommunicationFilters, CommunicationTarget } from "./communication";

export const protobufPackage = "user_service";

export interface PreviewCommunicationRequest {
  context: RequestContext | undefined;
  filters: CommunicationFilters | undefined;
  channels: NotificationType[];
}

export interface SendCommunicationRequest {
  context: RequestContext | undefined;
  filters: CommunicationFilters | undefined;
  channels: NotificationType[];
  subject: string;
  body: string;
}

export interface GetBroadcastsListRequest {
  context: RequestContext | undefined;
  per_page?: number | undefined;
  page?: number | undefined;
}

export interface GetBroadcastRequest {
  context: RequestContext | undefined;
  broadcast_id: ObjectId | undefined;
}

export interface GetRecipientsListRequest {
  context: RequestContext | undefined;
  broadcast_id: ObjectId | undefined;
}

export interface ResolveTargetsRequest {
  context: RequestContext | undefined;
  filters: CommunicationFilters | undefined;
}

export interface ResolveTargetsResponse {
  targets: CommunicationTarget[];
}

function createBasePreviewCommunicationRequest(): PreviewCommunicationRequest {
  return { context: undefined, filters: undefined, channels: [] };
}

export const PreviewCommunicationRequest: MessageFns<PreviewCommunicationRequest> = {
  encode(message: PreviewCommunicationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.filters !== undefined) {
      CommunicationFilters.encode(message.filters, writer.uint32(18).fork()).join();
    }
    writer.uint32(26).fork();
    for (const v of message.channels) {
      writer.int32(notificationTypeToNumber(v));
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreviewCommunicationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePreviewCommunicationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.filters = CommunicationFilters.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag === 24) {
            message.channels.push(notificationTypeFromJSON(reader.int32()));

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.channels.push(notificationTypeFromJSON(reader.int32()));
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PreviewCommunicationRequest {
    return {
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      filters: isSet(object.filters) ? CommunicationFilters.fromJSON(object.filters) : undefined,
      channels: globalThis.Array.isArray(object?.channels)
        ? object.channels.map((e: any) => notificationTypeFromJSON(e))
        : [],
    };
  },

  toJSON(message: PreviewCommunicationRequest): unknown {
    const obj: any = {};
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.filters !== undefined) {
      obj.filters = CommunicationFilters.toJSON(message.filters);
    }
    if (message.channels?.length) {
      obj.channels = message.channels.map((e) => notificationTypeToJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PreviewCommunicationRequest>, I>>(base?: I): PreviewCommunicationRequest {
    return PreviewCommunicationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PreviewCommunicationRequest>, I>>(object: I): PreviewCommunicationRequest {
    const message = createBasePreviewCommunicationRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.filters = (object.filters !== undefined && object.filters !== null)
      ? CommunicationFilters.fromPartial(object.filters)
      : undefined;
    message.channels = object.channels?.map((e) => e) || [];
    return message;
  },
};

function createBaseSendCommunicationRequest(): SendCommunicationRequest {
  return { context: undefined, filters: undefined, channels: [], subject: "", body: "" };
}

export const SendCommunicationRequest: MessageFns<SendCommunicationRequest> = {
  encode(message: SendCommunicationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.filters !== undefined) {
      CommunicationFilters.encode(message.filters, writer.uint32(18).fork()).join();
    }
    writer.uint32(26).fork();
    for (const v of message.channels) {
      writer.int32(notificationTypeToNumber(v));
    }
    writer.join();
    if (message.subject !== "") {
      writer.uint32(34).string(message.subject);
    }
    if (message.body !== "") {
      writer.uint32(42).string(message.body);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendCommunicationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendCommunicationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.filters = CommunicationFilters.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag === 24) {
            message.channels.push(notificationTypeFromJSON(reader.int32()));

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.channels.push(notificationTypeFromJSON(reader.int32()));
            }

            continue;
          }

          break;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.subject = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.body = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendCommunicationRequest {
    return {
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      filters: isSet(object.filters) ? CommunicationFilters.fromJSON(object.filters) : undefined,
      channels: globalThis.Array.isArray(object?.channels)
        ? object.channels.map((e: any) => notificationTypeFromJSON(e))
        : [],
      subject: isSet(object.subject) ? globalThis.String(object.subject) : "",
      body: isSet(object.body) ? globalThis.String(object.body) : "",
    };
  },

  toJSON(message: SendCommunicationRequest): unknown {
    const obj: any = {};
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.filters !== undefined) {
      obj.filters = CommunicationFilters.toJSON(message.filters);
    }
    if (message.channels?.length) {
      obj.channels = message.channels.map((e) => notificationTypeToJSON(e));
    }
    if (message.subject !== "") {
      obj.subject = message.subject;
    }
    if (message.body !== "") {
      obj.body = message.body;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SendCommunicationRequest>, I>>(base?: I): SendCommunicationRequest {
    return SendCommunicationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SendCommunicationRequest>, I>>(object: I): SendCommunicationRequest {
    const message = createBaseSendCommunicationRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.filters = (object.filters !== undefined && object.filters !== null)
      ? CommunicationFilters.fromPartial(object.filters)
      : undefined;
    message.channels = object.channels?.map((e) => e) || [];
    message.subject = object.subject ?? "";
    message.body = object.body ?? "";
    return message;
  },
};

function createBaseGetBroadcastsListRequest(): GetBroadcastsListRequest {
  return { context: undefined, per_page: 0, page: 0 };
}

export const GetBroadcastsListRequest: MessageFns<GetBroadcastsListRequest> = {
  encode(message: GetBroadcastsListRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.per_page !== undefined && message.per_page !== 0) {
      writer.uint32(16).uint64(message.per_page);
    }
    if (message.page !== undefined && message.page !== 0) {
      writer.uint32(24).uint64(message.page);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBroadcastsListRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBroadcastsListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.per_page = longToNumber(reader.uint64());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.page = longToNumber(reader.uint64());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBroadcastsListRequest {
    return {
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      per_page: isSet(object.perPage) ? globalThis.Number(object.perPage) : 0,
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
    };
  },

  toJSON(message: GetBroadcastsListRequest): unknown {
    const obj: any = {};
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.per_page !== undefined && message.per_page !== 0) {
      obj.perPage = Math.round(message.per_page);
    }
    if (message.page !== undefined && message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetBroadcastsListRequest>, I>>(base?: I): GetBroadcastsListRequest {
    return GetBroadcastsListRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetBroadcastsListRequest>, I>>(object: I): GetBroadcastsListRequest {
    const message = createBaseGetBroadcastsListRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.per_page = object.per_page ?? 0;
    message.page = object.page ?? 0;
    return message;
  },
};

function createBaseGetBroadcastRequest(): GetBroadcastRequest {
  return { context: undefined, broadcast_id: undefined };
}

export const GetBroadcastRequest: MessageFns<GetBroadcastRequest> = {
  encode(message: GetBroadcastRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.broadcast_id !== undefined) {
      ObjectId.encode(message.broadcast_id, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBroadcastRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBroadcastRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.broadcast_id = ObjectId.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBroadcastRequest {
    return {
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      broadcast_id: isSet(object.broadcastId) ? ObjectId.fromJSON(object.broadcastId) : undefined,
    };
  },

  toJSON(message: GetBroadcastRequest): unknown {
    const obj: any = {};
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.broadcast_id !== undefined) {
      obj.broadcastId = ObjectId.toJSON(message.broadcast_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetBroadcastRequest>, I>>(base?: I): GetBroadcastRequest {
    return GetBroadcastRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetBroadcastRequest>, I>>(object: I): GetBroadcastRequest {
    const message = createBaseGetBroadcastRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.broadcast_id = (object.broadcast_id !== undefined && object.broadcast_id !== null)
      ? ObjectId.fromPartial(object.broadcast_id)
      : undefined;
    return message;
  },
};

function createBaseGetRecipientsListRequest(): GetRecipientsListRequest {
  return { context: undefined, broadcast_id: undefined };
}

export const GetRecipientsListRequest: MessageFns<GetRecipientsListRequest> = {
  encode(message: GetRecipientsListRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.broadcast_id !== undefined) {
      ObjectId.encode(message.broadcast_id, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRecipientsListRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRecipientsListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.broadcast_id = ObjectId.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRecipientsListRequest {
    return {
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      broadcast_id: isSet(object.broadcastId) ? ObjectId.fromJSON(object.broadcastId) : undefined,
    };
  },

  toJSON(message: GetRecipientsListRequest): unknown {
    const obj: any = {};
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.broadcast_id !== undefined) {
      obj.broadcastId = ObjectId.toJSON(message.broadcast_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetRecipientsListRequest>, I>>(base?: I): GetRecipientsListRequest {
    return GetRecipientsListRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetRecipientsListRequest>, I>>(object: I): GetRecipientsListRequest {
    const message = createBaseGetRecipientsListRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.broadcast_id = (object.broadcast_id !== undefined && object.broadcast_id !== null)
      ? ObjectId.fromPartial(object.broadcast_id)
      : undefined;
    return message;
  },
};

function createBaseResolveTargetsRequest(): ResolveTargetsRequest {
  return { context: undefined, filters: undefined };
}

export const ResolveTargetsRequest: MessageFns<ResolveTargetsRequest> = {
  encode(message: ResolveTargetsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.filters !== undefined) {
      CommunicationFilters.encode(message.filters, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveTargetsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveTargetsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.filters = CommunicationFilters.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResolveTargetsRequest {
    return {
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      filters: isSet(object.filters) ? CommunicationFilters.fromJSON(object.filters) : undefined,
    };
  },

  toJSON(message: ResolveTargetsRequest): unknown {
    const obj: any = {};
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.filters !== undefined) {
      obj.filters = CommunicationFilters.toJSON(message.filters);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResolveTargetsRequest>, I>>(base?: I): ResolveTargetsRequest {
    return ResolveTargetsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveTargetsRequest>, I>>(object: I): ResolveTargetsRequest {
    const message = createBaseResolveTargetsRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.filters = (object.filters !== undefined && object.filters !== null)
      ? CommunicationFilters.fromPartial(object.filters)
      : undefined;
    return message;
  },
};

function createBaseResolveTargetsResponse(): ResolveTargetsResponse {
  return { targets: [] };
}

export const ResolveTargetsResponse: MessageFns<ResolveTargetsResponse> = {
  encode(message: ResolveTargetsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.targets) {
      CommunicationTarget.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveTargetsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveTargetsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.targets.push(CommunicationTarget.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResolveTargetsResponse {
    return {
      targets: globalThis.Array.isArray(object?.targets)
        ? object.targets.map((e: any) => CommunicationTarget.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ResolveTargetsResponse): unknown {
    const obj: any = {};
    if (message.targets?.length) {
      obj.targets = message.targets.map((e) => CommunicationTarget.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResolveTargetsResponse>, I>>(base?: I): ResolveTargetsResponse {
    return ResolveTargetsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveTargetsResponse>, I>>(object: I): ResolveTargetsResponse {
    const message = createBaseResolveTargetsResponse();
    message.targets = object.targets?.map((e) => CommunicationTarget.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
