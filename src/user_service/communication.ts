// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: user_service/communication.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Notification } from "../utils/notification";
import {
  NotificationType,
  notificationTypeFromJSON,
  notificationTypeToJSON,
  notificationTypeToNumber,
} from "../utils/notification_type";
import { ObjectId } from "../utils/object_id";
import { PhoneNumber } from "../utils/phone_number";
import { UserType, userTypeFromJSON, userTypeToJSON, userTypeToNumber } from "../utils/user_type";

export const protobufPackage = "user_service";

export interface CommunicationFilters {
  homeroom_ids: ObjectId[];
  course_ids: ObjectId[];
  family_ids: ObjectId[];
  semester_ids: ObjectId[];
  school_year_id?: ObjectId | undefined;
  audience_user_types: UserType[];
}

export interface CommunicationBroadcast {
  id: ObjectId | undefined;
  organization: ObjectId | undefined;
  filters: CommunicationFilters | undefined;
  channels: NotificationType[];
  subject: string;
  body: string;
}

export interface CommunicationTarget {
  user_type: UserType;
  user_id: ObjectId | undefined;
  email?: string | undefined;
  phone_number?: PhoneNumber | undefined;
}

export interface CommunicationChannelCounts {
  email_recipients: number;
  sms_recipients: number;
  phone_call_recipients: number;
}

export interface BroadcastList {
  broadcasts: CommunicationBroadcast[];
  broadcasts_count: number;
}

export interface RecipientList {
  recipients: Notification[];
  recipients_count: number;
}

export interface PreviewCommunicationResponse {
  total_recipients: number;
  email_recipients: number;
  sms_recipients: number;
  phone_call_recipients: number;
  estimated_cost: number;
}

function createBaseCommunicationFilters(): CommunicationFilters {
  return {
    homeroom_ids: [],
    course_ids: [],
    family_ids: [],
    semester_ids: [],
    school_year_id: undefined,
    audience_user_types: [],
  };
}

export const CommunicationFilters: MessageFns<CommunicationFilters> = {
  encode(message: CommunicationFilters, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.homeroom_ids) {
      ObjectId.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.course_ids) {
      ObjectId.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.family_ids) {
      ObjectId.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.semester_ids) {
      ObjectId.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.school_year_id !== undefined) {
      ObjectId.encode(message.school_year_id, writer.uint32(42).fork()).join();
    }
    writer.uint32(50).fork();
    for (const v of message.audience_user_types) {
      writer.int32(userTypeToNumber(v));
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommunicationFilters {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommunicationFilters();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.homeroom_ids.push(ObjectId.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.course_ids.push(ObjectId.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.family_ids.push(ObjectId.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.semester_ids.push(ObjectId.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.school_year_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag === 48) {
            message.audience_user_types.push(userTypeFromJSON(reader.int32()));

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.audience_user_types.push(userTypeFromJSON(reader.int32()));
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommunicationFilters {
    return {
      homeroom_ids: globalThis.Array.isArray(object?.homeroomIds)
        ? object.homeroomIds.map((e: any) => ObjectId.fromJSON(e))
        : [],
      course_ids: globalThis.Array.isArray(object?.courseIds)
        ? object.courseIds.map((e: any) => ObjectId.fromJSON(e))
        : [],
      family_ids: globalThis.Array.isArray(object?.familyIds)
        ? object.familyIds.map((e: any) => ObjectId.fromJSON(e))
        : [],
      semester_ids: globalThis.Array.isArray(object?.semesterIds)
        ? object.semesterIds.map((e: any) => ObjectId.fromJSON(e))
        : [],
      school_year_id: isSet(object.schoolYearId) ? ObjectId.fromJSON(object.schoolYearId) : undefined,
      audience_user_types: globalThis.Array.isArray(object?.audienceUserTypes)
        ? object.audienceUserTypes.map((e: any) => userTypeFromJSON(e))
        : [],
    };
  },

  toJSON(message: CommunicationFilters): unknown {
    const obj: any = {};
    if (message.homeroom_ids?.length) {
      obj.homeroomIds = message.homeroom_ids.map((e) => ObjectId.toJSON(e));
    }
    if (message.course_ids?.length) {
      obj.courseIds = message.course_ids.map((e) => ObjectId.toJSON(e));
    }
    if (message.family_ids?.length) {
      obj.familyIds = message.family_ids.map((e) => ObjectId.toJSON(e));
    }
    if (message.semester_ids?.length) {
      obj.semesterIds = message.semester_ids.map((e) => ObjectId.toJSON(e));
    }
    if (message.school_year_id !== undefined) {
      obj.schoolYearId = ObjectId.toJSON(message.school_year_id);
    }
    if (message.audience_user_types?.length) {
      obj.audienceUserTypes = message.audience_user_types.map((e) => userTypeToJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommunicationFilters>, I>>(base?: I): CommunicationFilters {
    return CommunicationFilters.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommunicationFilters>, I>>(object: I): CommunicationFilters {
    const message = createBaseCommunicationFilters();
    message.homeroom_ids = object.homeroom_ids?.map((e) => ObjectId.fromPartial(e)) || [];
    message.course_ids = object.course_ids?.map((e) => ObjectId.fromPartial(e)) || [];
    message.family_ids = object.family_ids?.map((e) => ObjectId.fromPartial(e)) || [];
    message.semester_ids = object.semester_ids?.map((e) => ObjectId.fromPartial(e)) || [];
    message.school_year_id = (object.school_year_id !== undefined && object.school_year_id !== null)
      ? ObjectId.fromPartial(object.school_year_id)
      : undefined;
    message.audience_user_types = object.audience_user_types?.map((e) => e) || [];
    return message;
  },
};

function createBaseCommunicationBroadcast(): CommunicationBroadcast {
  return { id: undefined, organization: undefined, filters: undefined, channels: [], subject: "", body: "" };
}

export const CommunicationBroadcast: MessageFns<CommunicationBroadcast> = {
  encode(message: CommunicationBroadcast, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      ObjectId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.filters !== undefined) {
      CommunicationFilters.encode(message.filters, writer.uint32(26).fork()).join();
    }
    writer.uint32(34).fork();
    for (const v of message.channels) {
      writer.int32(notificationTypeToNumber(v));
    }
    writer.join();
    if (message.subject !== "") {
      writer.uint32(42).string(message.subject);
    }
    if (message.body !== "") {
      writer.uint32(50).string(message.body);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommunicationBroadcast {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommunicationBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.filters = CommunicationFilters.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag === 32) {
            message.channels.push(notificationTypeFromJSON(reader.int32()));

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.channels.push(notificationTypeFromJSON(reader.int32()));
            }

            continue;
          }

          break;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.subject = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.body = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommunicationBroadcast {
    return {
      id: isSet(object.id) ? ObjectId.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      filters: isSet(object.filters) ? CommunicationFilters.fromJSON(object.filters) : undefined,
      channels: globalThis.Array.isArray(object?.channels)
        ? object.channels.map((e: any) => notificationTypeFromJSON(e))
        : [],
      subject: isSet(object.subject) ? globalThis.String(object.subject) : "",
      body: isSet(object.body) ? globalThis.String(object.body) : "",
    };
  },

  toJSON(message: CommunicationBroadcast): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = ObjectId.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.filters !== undefined) {
      obj.filters = CommunicationFilters.toJSON(message.filters);
    }
    if (message.channels?.length) {
      obj.channels = message.channels.map((e) => notificationTypeToJSON(e));
    }
    if (message.subject !== "") {
      obj.subject = message.subject;
    }
    if (message.body !== "") {
      obj.body = message.body;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommunicationBroadcast>, I>>(base?: I): CommunicationBroadcast {
    return CommunicationBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommunicationBroadcast>, I>>(object: I): CommunicationBroadcast {
    const message = createBaseCommunicationBroadcast();
    message.id = (object.id !== undefined && object.id !== null) ? ObjectId.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.filters = (object.filters !== undefined && object.filters !== null)
      ? CommunicationFilters.fromPartial(object.filters)
      : undefined;
    message.channels = object.channels?.map((e) => e) || [];
    message.subject = object.subject ?? "";
    message.body = object.body ?? "";
    return message;
  },
};

function createBaseCommunicationTarget(): CommunicationTarget {
  return { user_type: UserType.NONE, user_id: undefined, email: "", phone_number: undefined };
}

export const CommunicationTarget: MessageFns<CommunicationTarget> = {
  encode(message: CommunicationTarget, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user_type !== UserType.NONE) {
      writer.uint32(8).int32(userTypeToNumber(message.user_type));
    }
    if (message.user_id !== undefined) {
      ObjectId.encode(message.user_id, writer.uint32(18).fork()).join();
    }
    if (message.email !== undefined && message.email !== "") {
      writer.uint32(26).string(message.email);
    }
    if (message.phone_number !== undefined) {
      PhoneNumber.encode(message.phone_number, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommunicationTarget {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommunicationTarget();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.user_type = userTypeFromJSON(reader.int32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.user_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.email = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.phone_number = PhoneNumber.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommunicationTarget {
    return {
      user_type: isSet(object.userType) ? userTypeFromJSON(object.userType) : UserType.NONE,
      user_id: isSet(object.userId) ? ObjectId.fromJSON(object.userId) : undefined,
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      phone_number: isSet(object.phoneNumber) ? PhoneNumber.fromJSON(object.phoneNumber) : undefined,
    };
  },

  toJSON(message: CommunicationTarget): unknown {
    const obj: any = {};
    if (message.user_type !== UserType.NONE) {
      obj.userType = userTypeToJSON(message.user_type);
    }
    if (message.user_id !== undefined) {
      obj.userId = ObjectId.toJSON(message.user_id);
    }
    if (message.email !== undefined && message.email !== "") {
      obj.email = message.email;
    }
    if (message.phone_number !== undefined) {
      obj.phoneNumber = PhoneNumber.toJSON(message.phone_number);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommunicationTarget>, I>>(base?: I): CommunicationTarget {
    return CommunicationTarget.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommunicationTarget>, I>>(object: I): CommunicationTarget {
    const message = createBaseCommunicationTarget();
    message.user_type = object.user_type ?? UserType.NONE;
    message.user_id = (object.user_id !== undefined && object.user_id !== null)
      ? ObjectId.fromPartial(object.user_id)
      : undefined;
    message.email = object.email ?? "";
    message.phone_number = (object.phone_number !== undefined && object.phone_number !== null)
      ? PhoneNumber.fromPartial(object.phone_number)
      : undefined;
    return message;
  },
};

function createBaseCommunicationChannelCounts(): CommunicationChannelCounts {
  return { email_recipients: 0, sms_recipients: 0, phone_call_recipients: 0 };
}

export const CommunicationChannelCounts: MessageFns<CommunicationChannelCounts> = {
  encode(message: CommunicationChannelCounts, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email_recipients !== 0) {
      writer.uint32(8).uint32(message.email_recipients);
    }
    if (message.sms_recipients !== 0) {
      writer.uint32(16).uint32(message.sms_recipients);
    }
    if (message.phone_call_recipients !== 0) {
      writer.uint32(24).uint32(message.phone_call_recipients);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommunicationChannelCounts {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommunicationChannelCounts();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.email_recipients = reader.uint32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.sms_recipients = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.phone_call_recipients = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommunicationChannelCounts {
    return {
      email_recipients: isSet(object.emailRecipients) ? globalThis.Number(object.emailRecipients) : 0,
      sms_recipients: isSet(object.smsRecipients) ? globalThis.Number(object.smsRecipients) : 0,
      phone_call_recipients: isSet(object.phoneCallRecipients) ? globalThis.Number(object.phoneCallRecipients) : 0,
    };
  },

  toJSON(message: CommunicationChannelCounts): unknown {
    const obj: any = {};
    if (message.email_recipients !== 0) {
      obj.emailRecipients = Math.round(message.email_recipients);
    }
    if (message.sms_recipients !== 0) {
      obj.smsRecipients = Math.round(message.sms_recipients);
    }
    if (message.phone_call_recipients !== 0) {
      obj.phoneCallRecipients = Math.round(message.phone_call_recipients);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommunicationChannelCounts>, I>>(base?: I): CommunicationChannelCounts {
    return CommunicationChannelCounts.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommunicationChannelCounts>, I>>(object: I): CommunicationChannelCounts {
    const message = createBaseCommunicationChannelCounts();
    message.email_recipients = object.email_recipients ?? 0;
    message.sms_recipients = object.sms_recipients ?? 0;
    message.phone_call_recipients = object.phone_call_recipients ?? 0;
    return message;
  },
};

function createBaseBroadcastList(): BroadcastList {
  return { broadcasts: [], broadcasts_count: 0 };
}

export const BroadcastList: MessageFns<BroadcastList> = {
  encode(message: BroadcastList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.broadcasts) {
      CommunicationBroadcast.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.broadcasts_count !== 0) {
      writer.uint32(16).uint64(message.broadcasts_count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BroadcastList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBroadcastList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.broadcasts.push(CommunicationBroadcast.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.broadcasts_count = longToNumber(reader.uint64());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BroadcastList {
    return {
      broadcasts: globalThis.Array.isArray(object?.broadcasts)
        ? object.broadcasts.map((e: any) => CommunicationBroadcast.fromJSON(e))
        : [],
      broadcasts_count: isSet(object.broadcastsCount) ? globalThis.Number(object.broadcastsCount) : 0,
    };
  },

  toJSON(message: BroadcastList): unknown {
    const obj: any = {};
    if (message.broadcasts?.length) {
      obj.broadcasts = message.broadcasts.map((e) => CommunicationBroadcast.toJSON(e));
    }
    if (message.broadcasts_count !== 0) {
      obj.broadcastsCount = Math.round(message.broadcasts_count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BroadcastList>, I>>(base?: I): BroadcastList {
    return BroadcastList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BroadcastList>, I>>(object: I): BroadcastList {
    const message = createBaseBroadcastList();
    message.broadcasts = object.broadcasts?.map((e) => CommunicationBroadcast.fromPartial(e)) || [];
    message.broadcasts_count = object.broadcasts_count ?? 0;
    return message;
  },
};

function createBaseRecipientList(): RecipientList {
  return { recipients: [], recipients_count: 0 };
}

export const RecipientList: MessageFns<RecipientList> = {
  encode(message: RecipientList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.recipients) {
      Notification.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.recipients_count !== 0) {
      writer.uint32(16).uint64(message.recipients_count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecipientList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecipientList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.recipients.push(Notification.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.recipients_count = longToNumber(reader.uint64());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecipientList {
    return {
      recipients: globalThis.Array.isArray(object?.recipients)
        ? object.recipients.map((e: any) => Notification.fromJSON(e))
        : [],
      recipients_count: isSet(object.recipientsCount) ? globalThis.Number(object.recipientsCount) : 0,
    };
  },

  toJSON(message: RecipientList): unknown {
    const obj: any = {};
    if (message.recipients?.length) {
      obj.recipients = message.recipients.map((e) => Notification.toJSON(e));
    }
    if (message.recipients_count !== 0) {
      obj.recipientsCount = Math.round(message.recipients_count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RecipientList>, I>>(base?: I): RecipientList {
    return RecipientList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RecipientList>, I>>(object: I): RecipientList {
    const message = createBaseRecipientList();
    message.recipients = object.recipients?.map((e) => Notification.fromPartial(e)) || [];
    message.recipients_count = object.recipients_count ?? 0;
    return message;
  },
};

function createBasePreviewCommunicationResponse(): PreviewCommunicationResponse {
  return { total_recipients: 0, email_recipients: 0, sms_recipients: 0, phone_call_recipients: 0, estimated_cost: 0 };
}

export const PreviewCommunicationResponse: MessageFns<PreviewCommunicationResponse> = {
  encode(message: PreviewCommunicationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.total_recipients !== 0) {
      writer.uint32(8).uint32(message.total_recipients);
    }
    if (message.email_recipients !== 0) {
      writer.uint32(16).uint32(message.email_recipients);
    }
    if (message.sms_recipients !== 0) {
      writer.uint32(24).uint32(message.sms_recipients);
    }
    if (message.phone_call_recipients !== 0) {
      writer.uint32(32).uint32(message.phone_call_recipients);
    }
    if (message.estimated_cost !== 0) {
      writer.uint32(41).double(message.estimated_cost);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreviewCommunicationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePreviewCommunicationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.total_recipients = reader.uint32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.email_recipients = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.sms_recipients = reader.uint32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.phone_call_recipients = reader.uint32();
          continue;
        case 5:
          if (tag !== 41) {
            break;
          }

          message.estimated_cost = reader.double();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PreviewCommunicationResponse {
    return {
      total_recipients: isSet(object.totalRecipients) ? globalThis.Number(object.totalRecipients) : 0,
      email_recipients: isSet(object.emailRecipients) ? globalThis.Number(object.emailRecipients) : 0,
      sms_recipients: isSet(object.smsRecipients) ? globalThis.Number(object.smsRecipients) : 0,
      phone_call_recipients: isSet(object.phoneCallRecipients) ? globalThis.Number(object.phoneCallRecipients) : 0,
      estimated_cost: isSet(object.estimatedCost) ? globalThis.Number(object.estimatedCost) : 0,
    };
  },

  toJSON(message: PreviewCommunicationResponse): unknown {
    const obj: any = {};
    if (message.total_recipients !== 0) {
      obj.totalRecipients = Math.round(message.total_recipients);
    }
    if (message.email_recipients !== 0) {
      obj.emailRecipients = Math.round(message.email_recipients);
    }
    if (message.sms_recipients !== 0) {
      obj.smsRecipients = Math.round(message.sms_recipients);
    }
    if (message.phone_call_recipients !== 0) {
      obj.phoneCallRecipients = Math.round(message.phone_call_recipients);
    }
    if (message.estimated_cost !== 0) {
      obj.estimatedCost = message.estimated_cost;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PreviewCommunicationResponse>, I>>(base?: I): PreviewCommunicationResponse {
    return PreviewCommunicationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PreviewCommunicationResponse>, I>>(object: I): PreviewCommunicationResponse {
    const message = createBasePreviewCommunicationResponse();
    message.total_recipients = object.total_recipients ?? 0;
    message.email_recipients = object.email_recipients ?? 0;
    message.sms_recipients = object.sms_recipients ?? 0;
    message.phone_call_recipients = object.phone_call_recipients ?? 0;
    message.estimated_cost = object.estimated_cost ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
