// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: user_service/teacher_role.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ObjectId } from "../utils/object_id";
import { TeacherBasic } from "./teacher";
import { StaffPermission, staffPermissionFromJSON, staffPermissionToJSON, staffPermissionToNumber } from "./user_role";

export const protobufPackage = "user_service";

export interface TeacherRole {
  id: ObjectId | undefined;
  organization: ObjectId | undefined;
  name?: string | undefined;
  roles: StaffPermission[];
  is_default?: boolean | undefined;
}

export interface TeacherRoleSummary {
  teacher_role: TeacherRole | undefined;
  assigned_teachers_count?: number | undefined;
  assigned_teachers: TeacherBasic[];
}

function createBaseTeacherRole(): TeacherRole {
  return { id: undefined, organization: undefined, name: undefined, roles: [], is_default: undefined };
}

export const TeacherRole: MessageFns<TeacherRole> = {
  encode(message: TeacherRole, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      ObjectId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.name !== undefined) {
      writer.uint32(26).string(message.name);
    }
    writer.uint32(34).fork();
    for (const v of message.roles) {
      writer.int32(staffPermissionToNumber(v));
    }
    writer.join();
    if (message.is_default !== undefined) {
      writer.uint32(40).bool(message.is_default);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TeacherRole {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTeacherRole();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        case 4:
          if (tag === 32) {
            message.roles.push(staffPermissionFromJSON(reader.int32()));

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.roles.push(staffPermissionFromJSON(reader.int32()));
            }

            continue;
          }

          break;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.is_default = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TeacherRole {
    return {
      id: isSet(object.id) ? ObjectId.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      roles: globalThis.Array.isArray(object?.roles) ? object.roles.map((e: any) => staffPermissionFromJSON(e)) : [],
      is_default: isSet(object.isDefault) ? globalThis.Boolean(object.isDefault) : undefined,
    };
  },

  toJSON(message: TeacherRole): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = ObjectId.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.roles?.length) {
      obj.roles = message.roles.map((e) => staffPermissionToJSON(e));
    }
    if (message.is_default !== undefined) {
      obj.isDefault = message.is_default;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TeacherRole>, I>>(base?: I): TeacherRole {
    return TeacherRole.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TeacherRole>, I>>(object: I): TeacherRole {
    const message = createBaseTeacherRole();
    message.id = (object.id !== undefined && object.id !== null) ? ObjectId.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.name = object.name ?? undefined;
    message.roles = object.roles?.map((e) => e) || [];
    message.is_default = object.is_default ?? undefined;
    return message;
  },
};

function createBaseTeacherRoleSummary(): TeacherRoleSummary {
  return { teacher_role: undefined, assigned_teachers_count: undefined, assigned_teachers: [] };
}

export const TeacherRoleSummary: MessageFns<TeacherRoleSummary> = {
  encode(message: TeacherRoleSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.teacher_role !== undefined) {
      TeacherRole.encode(message.teacher_role, writer.uint32(10).fork()).join();
    }
    if (message.assigned_teachers_count !== undefined) {
      writer.uint32(16).uint64(message.assigned_teachers_count);
    }
    for (const v of message.assigned_teachers) {
      TeacherBasic.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TeacherRoleSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTeacherRoleSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.teacher_role = TeacherRole.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.assigned_teachers_count = longToNumber(reader.uint64());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.assigned_teachers.push(TeacherBasic.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TeacherRoleSummary {
    return {
      teacher_role: isSet(object.teacherRole) ? TeacherRole.fromJSON(object.teacherRole) : undefined,
      assigned_teachers_count: isSet(object.assignedTeachersCount)
        ? globalThis.Number(object.assignedTeachersCount)
        : undefined,
      assigned_teachers: globalThis.Array.isArray(object?.assignedTeachers)
        ? object.assignedTeachers.map((e: any) => TeacherBasic.fromJSON(e))
        : [],
    };
  },

  toJSON(message: TeacherRoleSummary): unknown {
    const obj: any = {};
    if (message.teacher_role !== undefined) {
      obj.teacherRole = TeacherRole.toJSON(message.teacher_role);
    }
    if (message.assigned_teachers_count !== undefined) {
      obj.assignedTeachersCount = Math.round(message.assigned_teachers_count);
    }
    if (message.assigned_teachers?.length) {
      obj.assignedTeachers = message.assigned_teachers.map((e) => TeacherBasic.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TeacherRoleSummary>, I>>(base?: I): TeacherRoleSummary {
    return TeacherRoleSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TeacherRoleSummary>, I>>(object: I): TeacherRoleSummary {
    const message = createBaseTeacherRoleSummary();
    message.teacher_role = (object.teacher_role !== undefined && object.teacher_role !== null)
      ? TeacherRole.fromPartial(object.teacher_role)
      : undefined;
    message.assigned_teachers_count = object.assigned_teachers_count ?? undefined;
    message.assigned_teachers = object.assigned_teachers?.map((e) => TeacherBasic.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
