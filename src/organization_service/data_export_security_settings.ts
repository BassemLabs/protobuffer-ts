// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: organization_service/data_export_security_settings.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "organization_service";

export interface DataExportSecuritySettings {
  configured?: boolean | undefined;
  allowed_networks: string[];
}

function createBaseDataExportSecuritySettings(): DataExportSecuritySettings {
  return { configured: undefined, allowed_networks: [] };
}

export const DataExportSecuritySettings: MessageFns<DataExportSecuritySettings> = {
  encode(message: DataExportSecuritySettings, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.configured !== undefined) {
      writer.uint32(8).bool(message.configured);
    }
    for (const v of message.allowed_networks) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataExportSecuritySettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataExportSecuritySettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.configured = reader.bool();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.allowed_networks.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataExportSecuritySettings {
    return {
      configured: isSet(object.configured) ? globalThis.Boolean(object.configured) : undefined,
      allowed_networks: globalThis.Array.isArray(object?.allowedNetworks)
        ? object.allowedNetworks.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: DataExportSecuritySettings): unknown {
    const obj: any = {};
    if (message.configured !== undefined) {
      obj.configured = message.configured;
    }
    if (message.allowed_networks?.length) {
      obj.allowedNetworks = message.allowed_networks;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataExportSecuritySettings>, I>>(base?: I): DataExportSecuritySettings {
    return DataExportSecuritySettings.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataExportSecuritySettings>, I>>(object: I): DataExportSecuritySettings {
    const message = createBaseDataExportSecuritySettings();
    message.configured = object.configured ?? undefined;
    message.allowed_networks = object.allowed_networks?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
