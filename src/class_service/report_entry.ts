// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: class_service/report_entry.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";
import { Student } from "../user_service/student";
import { ObjectId } from "../utils/object_id";
import {
  ReportType,
  reportTypeFromJSON,
  reportTypeToJSON,
  reportTypeToNumber,
  Semester,
  SemesterReportLayout,
} from "./semester";

export const protobufPackage = "class_service";

export enum ReportStatus {
  NOT_FILLED = "NOT_FILLED",
  FILLED = "FILLED",
  CHANGES_REQUESTED = "CHANGES_REQUESTED",
  COMPLETED = "COMPLETED",
  APPROVED = "APPROVED",
  PUBLISHED = "PUBLISHED",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function reportStatusFromJSON(object: any): ReportStatus {
  switch (object) {
    case 0:
    case "NOT_FILLED":
      return ReportStatus.NOT_FILLED;
    case 1:
    case "FILLED":
      return ReportStatus.FILLED;
    case 2:
    case "CHANGES_REQUESTED":
      return ReportStatus.CHANGES_REQUESTED;
    case 3:
    case "COMPLETED":
      return ReportStatus.COMPLETED;
    case 4:
    case "APPROVED":
      return ReportStatus.APPROVED;
    case 5:
    case "PUBLISHED":
      return ReportStatus.PUBLISHED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ReportStatus.UNRECOGNIZED;
  }
}

export function reportStatusToJSON(object: ReportStatus): string {
  switch (object) {
    case ReportStatus.NOT_FILLED:
      return "NOT_FILLED";
    case ReportStatus.FILLED:
      return "FILLED";
    case ReportStatus.CHANGES_REQUESTED:
      return "CHANGES_REQUESTED";
    case ReportStatus.COMPLETED:
      return "COMPLETED";
    case ReportStatus.APPROVED:
      return "APPROVED";
    case ReportStatus.PUBLISHED:
      return "PUBLISHED";
    case ReportStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function reportStatusToNumber(object: ReportStatus): number {
  switch (object) {
    case ReportStatus.NOT_FILLED:
      return 0;
    case ReportStatus.FILLED:
      return 1;
    case ReportStatus.CHANGES_REQUESTED:
      return 2;
    case ReportStatus.COMPLETED:
      return 3;
    case ReportStatus.APPROVED:
      return 4;
    case ReportStatus.PUBLISHED:
      return 5;
    case ReportStatus.UNRECOGNIZED:
    default:
      return -1;
  }
}

export interface ReportEntryCheckBox {
  check_box_id: ObjectId | undefined;
  state: boolean;
}

export interface ReportEntrySection {
  layout_id: ObjectId | undefined;
  mark: string;
}

export interface ReportEntryLearningSkill {
  learning_skill_id: ObjectId | undefined;
  mark: string;
}

export interface GuardianSignatureHeader {
  key: string;
  value: string;
}

export interface GuardianSignatureSessionMetadata {
  ip_address?: string | undefined;
  peer_addr?: string | undefined;
  scheme?: string | undefined;
  headers: GuardianSignatureHeader[];
}

export interface GuardianSignature {
  guardian_id: ObjectId | undefined;
  guardian_name: string;
  guardian_signed_at: Date | undefined;
  session_metadata?: GuardianSignatureSessionMetadata | undefined;
}

export interface ReportEntry {
  id: ObjectId | undefined;
  organization: ObjectId | undefined;
  student: ObjectId | undefined;
  course?: ObjectId | undefined;
  homeroom?: ObjectId | undefined;
  report_type: ReportType;
  comment: string;
  checkboxes: ReportEntryCheckBox[];
  sections: ReportEntrySection[];
  review_status: ReportStatus;
  requested_changes: string;
  learning_skills: ReportEntryLearningSkill[];
  credit_earned: boolean;
  guardian_signature?: GuardianSignature | undefined;
}

export interface ReportEntryView {
  entry: ReportEntry | undefined;
  class_name: string;
  semester: Semester | undefined;
  semester_report_layout: SemesterReportLayout | undefined;
  signed_by_guardian_name?: string | undefined;
}

export interface ReportEntrySectionMedian {
  section_layout_id: ObjectId | undefined;
  median_mark?: string | undefined;
}

export interface ReportEntryMedian {
  report_entry: ObjectId | undefined;
  section_medians: ReportEntrySectionMedian[];
}

export interface ParentStudentReportSummary {
  student: Student | undefined;
  total_reports: number;
  unsigned_reports_count: number;
}

function createBaseReportEntryCheckBox(): ReportEntryCheckBox {
  return { check_box_id: undefined, state: false };
}

export const ReportEntryCheckBox: MessageFns<ReportEntryCheckBox> = {
  encode(message: ReportEntryCheckBox, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.check_box_id !== undefined) {
      ObjectId.encode(message.check_box_id, writer.uint32(10).fork()).join();
    }
    if (message.state !== false) {
      writer.uint32(16).bool(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportEntryCheckBox {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportEntryCheckBox();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.check_box_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.state = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportEntryCheckBox {
    return {
      check_box_id: isSet(object.checkBoxId) ? ObjectId.fromJSON(object.checkBoxId) : undefined,
      state: isSet(object.state) ? globalThis.Boolean(object.state) : false,
    };
  },

  toJSON(message: ReportEntryCheckBox): unknown {
    const obj: any = {};
    if (message.check_box_id !== undefined) {
      obj.checkBoxId = ObjectId.toJSON(message.check_box_id);
    }
    if (message.state !== false) {
      obj.state = message.state;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportEntryCheckBox>, I>>(base?: I): ReportEntryCheckBox {
    return ReportEntryCheckBox.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportEntryCheckBox>, I>>(object: I): ReportEntryCheckBox {
    const message = createBaseReportEntryCheckBox();
    message.check_box_id = (object.check_box_id !== undefined && object.check_box_id !== null)
      ? ObjectId.fromPartial(object.check_box_id)
      : undefined;
    message.state = object.state ?? false;
    return message;
  },
};

function createBaseReportEntrySection(): ReportEntrySection {
  return { layout_id: undefined, mark: "" };
}

export const ReportEntrySection: MessageFns<ReportEntrySection> = {
  encode(message: ReportEntrySection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.layout_id !== undefined) {
      ObjectId.encode(message.layout_id, writer.uint32(10).fork()).join();
    }
    if (message.mark !== "") {
      writer.uint32(18).string(message.mark);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportEntrySection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportEntrySection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.layout_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.mark = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportEntrySection {
    return {
      layout_id: isSet(object.layoutId) ? ObjectId.fromJSON(object.layoutId) : undefined,
      mark: isSet(object.mark) ? globalThis.String(object.mark) : "",
    };
  },

  toJSON(message: ReportEntrySection): unknown {
    const obj: any = {};
    if (message.layout_id !== undefined) {
      obj.layoutId = ObjectId.toJSON(message.layout_id);
    }
    if (message.mark !== "") {
      obj.mark = message.mark;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportEntrySection>, I>>(base?: I): ReportEntrySection {
    return ReportEntrySection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportEntrySection>, I>>(object: I): ReportEntrySection {
    const message = createBaseReportEntrySection();
    message.layout_id = (object.layout_id !== undefined && object.layout_id !== null)
      ? ObjectId.fromPartial(object.layout_id)
      : undefined;
    message.mark = object.mark ?? "";
    return message;
  },
};

function createBaseReportEntryLearningSkill(): ReportEntryLearningSkill {
  return { learning_skill_id: undefined, mark: "" };
}

export const ReportEntryLearningSkill: MessageFns<ReportEntryLearningSkill> = {
  encode(message: ReportEntryLearningSkill, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.learning_skill_id !== undefined) {
      ObjectId.encode(message.learning_skill_id, writer.uint32(10).fork()).join();
    }
    if (message.mark !== "") {
      writer.uint32(18).string(message.mark);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportEntryLearningSkill {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportEntryLearningSkill();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.learning_skill_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.mark = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportEntryLearningSkill {
    return {
      learning_skill_id: isSet(object.learningSkillId) ? ObjectId.fromJSON(object.learningSkillId) : undefined,
      mark: isSet(object.mark) ? globalThis.String(object.mark) : "",
    };
  },

  toJSON(message: ReportEntryLearningSkill): unknown {
    const obj: any = {};
    if (message.learning_skill_id !== undefined) {
      obj.learningSkillId = ObjectId.toJSON(message.learning_skill_id);
    }
    if (message.mark !== "") {
      obj.mark = message.mark;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportEntryLearningSkill>, I>>(base?: I): ReportEntryLearningSkill {
    return ReportEntryLearningSkill.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportEntryLearningSkill>, I>>(object: I): ReportEntryLearningSkill {
    const message = createBaseReportEntryLearningSkill();
    message.learning_skill_id = (object.learning_skill_id !== undefined && object.learning_skill_id !== null)
      ? ObjectId.fromPartial(object.learning_skill_id)
      : undefined;
    message.mark = object.mark ?? "";
    return message;
  },
};

function createBaseGuardianSignatureHeader(): GuardianSignatureHeader {
  return { key: "", value: "" };
}

export const GuardianSignatureHeader: MessageFns<GuardianSignatureHeader> = {
  encode(message: GuardianSignatureHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GuardianSignatureHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGuardianSignatureHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GuardianSignatureHeader {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: GuardianSignatureHeader): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GuardianSignatureHeader>, I>>(base?: I): GuardianSignatureHeader {
    return GuardianSignatureHeader.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GuardianSignatureHeader>, I>>(object: I): GuardianSignatureHeader {
    const message = createBaseGuardianSignatureHeader();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseGuardianSignatureSessionMetadata(): GuardianSignatureSessionMetadata {
  return { ip_address: "", peer_addr: "", scheme: "", headers: [] };
}

export const GuardianSignatureSessionMetadata: MessageFns<GuardianSignatureSessionMetadata> = {
  encode(message: GuardianSignatureSessionMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ip_address !== undefined && message.ip_address !== "") {
      writer.uint32(10).string(message.ip_address);
    }
    if (message.peer_addr !== undefined && message.peer_addr !== "") {
      writer.uint32(18).string(message.peer_addr);
    }
    if (message.scheme !== undefined && message.scheme !== "") {
      writer.uint32(26).string(message.scheme);
    }
    for (const v of message.headers) {
      GuardianSignatureHeader.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GuardianSignatureSessionMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGuardianSignatureSessionMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ip_address = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.peer_addr = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheme = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.headers.push(GuardianSignatureHeader.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GuardianSignatureSessionMetadata {
    return {
      ip_address: isSet(object.ipAddress) ? globalThis.String(object.ipAddress) : "",
      peer_addr: isSet(object.peerAddr) ? globalThis.String(object.peerAddr) : "",
      scheme: isSet(object.scheme) ? globalThis.String(object.scheme) : "",
      headers: globalThis.Array.isArray(object?.headers)
        ? object.headers.map((e: any) => GuardianSignatureHeader.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GuardianSignatureSessionMetadata): unknown {
    const obj: any = {};
    if (message.ip_address !== undefined && message.ip_address !== "") {
      obj.ipAddress = message.ip_address;
    }
    if (message.peer_addr !== undefined && message.peer_addr !== "") {
      obj.peerAddr = message.peer_addr;
    }
    if (message.scheme !== undefined && message.scheme !== "") {
      obj.scheme = message.scheme;
    }
    if (message.headers?.length) {
      obj.headers = message.headers.map((e) => GuardianSignatureHeader.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GuardianSignatureSessionMetadata>, I>>(
    base?: I,
  ): GuardianSignatureSessionMetadata {
    return GuardianSignatureSessionMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GuardianSignatureSessionMetadata>, I>>(
    object: I,
  ): GuardianSignatureSessionMetadata {
    const message = createBaseGuardianSignatureSessionMetadata();
    message.ip_address = object.ip_address ?? "";
    message.peer_addr = object.peer_addr ?? "";
    message.scheme = object.scheme ?? "";
    message.headers = object.headers?.map((e) => GuardianSignatureHeader.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGuardianSignature(): GuardianSignature {
  return { guardian_id: undefined, guardian_name: "", guardian_signed_at: undefined, session_metadata: undefined };
}

export const GuardianSignature: MessageFns<GuardianSignature> = {
  encode(message: GuardianSignature, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.guardian_id !== undefined) {
      ObjectId.encode(message.guardian_id, writer.uint32(10).fork()).join();
    }
    if (message.guardian_name !== "") {
      writer.uint32(18).string(message.guardian_name);
    }
    if (message.guardian_signed_at !== undefined) {
      Timestamp.encode(toTimestamp(message.guardian_signed_at), writer.uint32(26).fork()).join();
    }
    if (message.session_metadata !== undefined) {
      GuardianSignatureSessionMetadata.encode(message.session_metadata, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GuardianSignature {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGuardianSignature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.guardian_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.guardian_name = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.guardian_signed_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.session_metadata = GuardianSignatureSessionMetadata.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GuardianSignature {
    return {
      guardian_id: isSet(object.guardianId) ? ObjectId.fromJSON(object.guardianId) : undefined,
      guardian_name: isSet(object.guardianName) ? globalThis.String(object.guardianName) : "",
      guardian_signed_at: isSet(object.guardianSignedAt) ? fromJsonTimestamp(object.guardianSignedAt) : undefined,
      session_metadata: isSet(object.sessionMetadata)
        ? GuardianSignatureSessionMetadata.fromJSON(object.sessionMetadata)
        : undefined,
    };
  },

  toJSON(message: GuardianSignature): unknown {
    const obj: any = {};
    if (message.guardian_id !== undefined) {
      obj.guardianId = ObjectId.toJSON(message.guardian_id);
    }
    if (message.guardian_name !== "") {
      obj.guardianName = message.guardian_name;
    }
    if (message.guardian_signed_at !== undefined) {
      obj.guardianSignedAt = message.guardian_signed_at.toISOString();
    }
    if (message.session_metadata !== undefined) {
      obj.sessionMetadata = GuardianSignatureSessionMetadata.toJSON(message.session_metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GuardianSignature>, I>>(base?: I): GuardianSignature {
    return GuardianSignature.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GuardianSignature>, I>>(object: I): GuardianSignature {
    const message = createBaseGuardianSignature();
    message.guardian_id = (object.guardian_id !== undefined && object.guardian_id !== null)
      ? ObjectId.fromPartial(object.guardian_id)
      : undefined;
    message.guardian_name = object.guardian_name ?? "";
    message.guardian_signed_at = object.guardian_signed_at ?? undefined;
    message.session_metadata = (object.session_metadata !== undefined && object.session_metadata !== null)
      ? GuardianSignatureSessionMetadata.fromPartial(object.session_metadata)
      : undefined;
    return message;
  },
};

function createBaseReportEntry(): ReportEntry {
  return {
    id: undefined,
    organization: undefined,
    student: undefined,
    course: undefined,
    homeroom: undefined,
    report_type: ReportType.Progress,
    comment: "",
    checkboxes: [],
    sections: [],
    review_status: ReportStatus.NOT_FILLED,
    requested_changes: "",
    learning_skills: [],
    credit_earned: false,
    guardian_signature: undefined,
  };
}

export const ReportEntry: MessageFns<ReportEntry> = {
  encode(message: ReportEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      ObjectId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.student !== undefined) {
      ObjectId.encode(message.student, writer.uint32(26).fork()).join();
    }
    if (message.course !== undefined) {
      ObjectId.encode(message.course, writer.uint32(34).fork()).join();
    }
    if (message.homeroom !== undefined) {
      ObjectId.encode(message.homeroom, writer.uint32(42).fork()).join();
    }
    if (message.report_type !== ReportType.Progress) {
      writer.uint32(48).int32(reportTypeToNumber(message.report_type));
    }
    if (message.comment !== "") {
      writer.uint32(58).string(message.comment);
    }
    for (const v of message.checkboxes) {
      ReportEntryCheckBox.encode(v!, writer.uint32(66).fork()).join();
    }
    for (const v of message.sections) {
      ReportEntrySection.encode(v!, writer.uint32(74).fork()).join();
    }
    if (message.review_status !== ReportStatus.NOT_FILLED) {
      writer.uint32(80).int32(reportStatusToNumber(message.review_status));
    }
    if (message.requested_changes !== "") {
      writer.uint32(90).string(message.requested_changes);
    }
    for (const v of message.learning_skills) {
      ReportEntryLearningSkill.encode(v!, writer.uint32(98).fork()).join();
    }
    if (message.credit_earned !== false) {
      writer.uint32(104).bool(message.credit_earned);
    }
    if (message.guardian_signature !== undefined) {
      GuardianSignature.encode(message.guardian_signature, writer.uint32(114).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.student = ObjectId.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.course = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.homeroom = ObjectId.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.report_type = reportTypeFromJSON(reader.int32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.comment = reader.string();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.checkboxes.push(ReportEntryCheckBox.decode(reader, reader.uint32()));
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.sections.push(ReportEntrySection.decode(reader, reader.uint32()));
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.review_status = reportStatusFromJSON(reader.int32());
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.requested_changes = reader.string();
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.learning_skills.push(ReportEntryLearningSkill.decode(reader, reader.uint32()));
          continue;
        case 13:
          if (tag !== 104) {
            break;
          }

          message.credit_earned = reader.bool();
          continue;
        case 14:
          if (tag !== 114) {
            break;
          }

          message.guardian_signature = GuardianSignature.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportEntry {
    return {
      id: isSet(object.id) ? ObjectId.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      student: isSet(object.student) ? ObjectId.fromJSON(object.student) : undefined,
      course: isSet(object.course) ? ObjectId.fromJSON(object.course) : undefined,
      homeroom: isSet(object.homeroom) ? ObjectId.fromJSON(object.homeroom) : undefined,
      report_type: isSet(object.reportType) ? reportTypeFromJSON(object.reportType) : ReportType.Progress,
      comment: isSet(object.comment) ? globalThis.String(object.comment) : "",
      checkboxes: globalThis.Array.isArray(object?.checkboxes)
        ? object.checkboxes.map((e: any) => ReportEntryCheckBox.fromJSON(e))
        : [],
      sections: globalThis.Array.isArray(object?.sections)
        ? object.sections.map((e: any) => ReportEntrySection.fromJSON(e))
        : [],
      review_status: isSet(object.reviewStatus) ? reportStatusFromJSON(object.reviewStatus) : ReportStatus.NOT_FILLED,
      requested_changes: isSet(object.requestedChanges) ? globalThis.String(object.requestedChanges) : "",
      learning_skills: globalThis.Array.isArray(object?.learningSkills)
        ? object.learningSkills.map((e: any) => ReportEntryLearningSkill.fromJSON(e))
        : [],
      credit_earned: isSet(object.creditEarned) ? globalThis.Boolean(object.creditEarned) : false,
      guardian_signature: isSet(object.guardianSignature)
        ? GuardianSignature.fromJSON(object.guardianSignature)
        : undefined,
    };
  },

  toJSON(message: ReportEntry): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = ObjectId.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.student !== undefined) {
      obj.student = ObjectId.toJSON(message.student);
    }
    if (message.course !== undefined) {
      obj.course = ObjectId.toJSON(message.course);
    }
    if (message.homeroom !== undefined) {
      obj.homeroom = ObjectId.toJSON(message.homeroom);
    }
    if (message.report_type !== ReportType.Progress) {
      obj.reportType = reportTypeToJSON(message.report_type);
    }
    if (message.comment !== "") {
      obj.comment = message.comment;
    }
    if (message.checkboxes?.length) {
      obj.checkboxes = message.checkboxes.map((e) => ReportEntryCheckBox.toJSON(e));
    }
    if (message.sections?.length) {
      obj.sections = message.sections.map((e) => ReportEntrySection.toJSON(e));
    }
    if (message.review_status !== ReportStatus.NOT_FILLED) {
      obj.reviewStatus = reportStatusToJSON(message.review_status);
    }
    if (message.requested_changes !== "") {
      obj.requestedChanges = message.requested_changes;
    }
    if (message.learning_skills?.length) {
      obj.learningSkills = message.learning_skills.map((e) => ReportEntryLearningSkill.toJSON(e));
    }
    if (message.credit_earned !== false) {
      obj.creditEarned = message.credit_earned;
    }
    if (message.guardian_signature !== undefined) {
      obj.guardianSignature = GuardianSignature.toJSON(message.guardian_signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportEntry>, I>>(base?: I): ReportEntry {
    return ReportEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportEntry>, I>>(object: I): ReportEntry {
    const message = createBaseReportEntry();
    message.id = (object.id !== undefined && object.id !== null) ? ObjectId.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.student = (object.student !== undefined && object.student !== null)
      ? ObjectId.fromPartial(object.student)
      : undefined;
    message.course = (object.course !== undefined && object.course !== null)
      ? ObjectId.fromPartial(object.course)
      : undefined;
    message.homeroom = (object.homeroom !== undefined && object.homeroom !== null)
      ? ObjectId.fromPartial(object.homeroom)
      : undefined;
    message.report_type = object.report_type ?? ReportType.Progress;
    message.comment = object.comment ?? "";
    message.checkboxes = object.checkboxes?.map((e) => ReportEntryCheckBox.fromPartial(e)) || [];
    message.sections = object.sections?.map((e) => ReportEntrySection.fromPartial(e)) || [];
    message.review_status = object.review_status ?? ReportStatus.NOT_FILLED;
    message.requested_changes = object.requested_changes ?? "";
    message.learning_skills = object.learning_skills?.map((e) => ReportEntryLearningSkill.fromPartial(e)) || [];
    message.credit_earned = object.credit_earned ?? false;
    message.guardian_signature = (object.guardian_signature !== undefined && object.guardian_signature !== null)
      ? GuardianSignature.fromPartial(object.guardian_signature)
      : undefined;
    return message;
  },
};

function createBaseReportEntryView(): ReportEntryView {
  return {
    entry: undefined,
    class_name: "",
    semester: undefined,
    semester_report_layout: undefined,
    signed_by_guardian_name: "",
  };
}

export const ReportEntryView: MessageFns<ReportEntryView> = {
  encode(message: ReportEntryView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entry !== undefined) {
      ReportEntry.encode(message.entry, writer.uint32(10).fork()).join();
    }
    if (message.class_name !== "") {
      writer.uint32(18).string(message.class_name);
    }
    if (message.semester !== undefined) {
      Semester.encode(message.semester, writer.uint32(26).fork()).join();
    }
    if (message.semester_report_layout !== undefined) {
      SemesterReportLayout.encode(message.semester_report_layout, writer.uint32(34).fork()).join();
    }
    if (message.signed_by_guardian_name !== undefined && message.signed_by_guardian_name !== "") {
      writer.uint32(42).string(message.signed_by_guardian_name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportEntryView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportEntryView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.entry = ReportEntry.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.class_name = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.semester = Semester.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.semester_report_layout = SemesterReportLayout.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.signed_by_guardian_name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportEntryView {
    return {
      entry: isSet(object.entry) ? ReportEntry.fromJSON(object.entry) : undefined,
      class_name: isSet(object.className) ? globalThis.String(object.className) : "",
      semester: isSet(object.semester) ? Semester.fromJSON(object.semester) : undefined,
      semester_report_layout: isSet(object.semesterReportLayout)
        ? SemesterReportLayout.fromJSON(object.semesterReportLayout)
        : undefined,
      signed_by_guardian_name: isSet(object.signedByGuardianName) ? globalThis.String(object.signedByGuardianName) : "",
    };
  },

  toJSON(message: ReportEntryView): unknown {
    const obj: any = {};
    if (message.entry !== undefined) {
      obj.entry = ReportEntry.toJSON(message.entry);
    }
    if (message.class_name !== "") {
      obj.className = message.class_name;
    }
    if (message.semester !== undefined) {
      obj.semester = Semester.toJSON(message.semester);
    }
    if (message.semester_report_layout !== undefined) {
      obj.semesterReportLayout = SemesterReportLayout.toJSON(message.semester_report_layout);
    }
    if (message.signed_by_guardian_name !== undefined && message.signed_by_guardian_name !== "") {
      obj.signedByGuardianName = message.signed_by_guardian_name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportEntryView>, I>>(base?: I): ReportEntryView {
    return ReportEntryView.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportEntryView>, I>>(object: I): ReportEntryView {
    const message = createBaseReportEntryView();
    message.entry = (object.entry !== undefined && object.entry !== null)
      ? ReportEntry.fromPartial(object.entry)
      : undefined;
    message.class_name = object.class_name ?? "";
    message.semester = (object.semester !== undefined && object.semester !== null)
      ? Semester.fromPartial(object.semester)
      : undefined;
    message.semester_report_layout =
      (object.semester_report_layout !== undefined && object.semester_report_layout !== null)
        ? SemesterReportLayout.fromPartial(object.semester_report_layout)
        : undefined;
    message.signed_by_guardian_name = object.signed_by_guardian_name ?? "";
    return message;
  },
};

function createBaseReportEntrySectionMedian(): ReportEntrySectionMedian {
  return { section_layout_id: undefined, median_mark: "" };
}

export const ReportEntrySectionMedian: MessageFns<ReportEntrySectionMedian> = {
  encode(message: ReportEntrySectionMedian, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.section_layout_id !== undefined) {
      ObjectId.encode(message.section_layout_id, writer.uint32(10).fork()).join();
    }
    if (message.median_mark !== undefined && message.median_mark !== "") {
      writer.uint32(18).string(message.median_mark);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportEntrySectionMedian {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportEntrySectionMedian();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.section_layout_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.median_mark = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportEntrySectionMedian {
    return {
      section_layout_id: isSet(object.sectionLayoutId) ? ObjectId.fromJSON(object.sectionLayoutId) : undefined,
      median_mark: isSet(object.medianMark) ? globalThis.String(object.medianMark) : "",
    };
  },

  toJSON(message: ReportEntrySectionMedian): unknown {
    const obj: any = {};
    if (message.section_layout_id !== undefined) {
      obj.sectionLayoutId = ObjectId.toJSON(message.section_layout_id);
    }
    if (message.median_mark !== undefined && message.median_mark !== "") {
      obj.medianMark = message.median_mark;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportEntrySectionMedian>, I>>(base?: I): ReportEntrySectionMedian {
    return ReportEntrySectionMedian.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportEntrySectionMedian>, I>>(object: I): ReportEntrySectionMedian {
    const message = createBaseReportEntrySectionMedian();
    message.section_layout_id = (object.section_layout_id !== undefined && object.section_layout_id !== null)
      ? ObjectId.fromPartial(object.section_layout_id)
      : undefined;
    message.median_mark = object.median_mark ?? "";
    return message;
  },
};

function createBaseReportEntryMedian(): ReportEntryMedian {
  return { report_entry: undefined, section_medians: [] };
}

export const ReportEntryMedian: MessageFns<ReportEntryMedian> = {
  encode(message: ReportEntryMedian, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.report_entry !== undefined) {
      ObjectId.encode(message.report_entry, writer.uint32(10).fork()).join();
    }
    for (const v of message.section_medians) {
      ReportEntrySectionMedian.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportEntryMedian {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportEntryMedian();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.report_entry = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.section_medians.push(ReportEntrySectionMedian.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportEntryMedian {
    return {
      report_entry: isSet(object.reportEntry) ? ObjectId.fromJSON(object.reportEntry) : undefined,
      section_medians: globalThis.Array.isArray(object?.sectionMedians)
        ? object.sectionMedians.map((e: any) => ReportEntrySectionMedian.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ReportEntryMedian): unknown {
    const obj: any = {};
    if (message.report_entry !== undefined) {
      obj.reportEntry = ObjectId.toJSON(message.report_entry);
    }
    if (message.section_medians?.length) {
      obj.sectionMedians = message.section_medians.map((e) => ReportEntrySectionMedian.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportEntryMedian>, I>>(base?: I): ReportEntryMedian {
    return ReportEntryMedian.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportEntryMedian>, I>>(object: I): ReportEntryMedian {
    const message = createBaseReportEntryMedian();
    message.report_entry = (object.report_entry !== undefined && object.report_entry !== null)
      ? ObjectId.fromPartial(object.report_entry)
      : undefined;
    message.section_medians = object.section_medians?.map((e) => ReportEntrySectionMedian.fromPartial(e)) || [];
    return message;
  },
};

function createBaseParentStudentReportSummary(): ParentStudentReportSummary {
  return { student: undefined, total_reports: 0, unsigned_reports_count: 0 };
}

export const ParentStudentReportSummary: MessageFns<ParentStudentReportSummary> = {
  encode(message: ParentStudentReportSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.student !== undefined) {
      Student.encode(message.student, writer.uint32(10).fork()).join();
    }
    if (message.total_reports !== 0) {
      writer.uint32(16).uint32(message.total_reports);
    }
    if (message.unsigned_reports_count !== 0) {
      writer.uint32(24).uint32(message.unsigned_reports_count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParentStudentReportSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParentStudentReportSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.student = Student.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.total_reports = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.unsigned_reports_count = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParentStudentReportSummary {
    return {
      student: isSet(object.student) ? Student.fromJSON(object.student) : undefined,
      total_reports: isSet(object.totalReports) ? globalThis.Number(object.totalReports) : 0,
      unsigned_reports_count: isSet(object.unsignedReportsCount) ? globalThis.Number(object.unsignedReportsCount) : 0,
    };
  },

  toJSON(message: ParentStudentReportSummary): unknown {
    const obj: any = {};
    if (message.student !== undefined) {
      obj.student = Student.toJSON(message.student);
    }
    if (message.total_reports !== 0) {
      obj.totalReports = Math.round(message.total_reports);
    }
    if (message.unsigned_reports_count !== 0) {
      obj.unsignedReportsCount = Math.round(message.unsigned_reports_count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParentStudentReportSummary>, I>>(base?: I): ParentStudentReportSummary {
    return ParentStudentReportSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParentStudentReportSummary>, I>>(object: I): ParentStudentReportSummary {
    const message = createBaseParentStudentReportSummary();
    message.student = (object.student !== undefined && object.student !== null)
      ? Student.fromPartial(object.student)
      : undefined;
    message.total_reports = object.total_reports ?? 0;
    message.unsigned_reports_count = object.unsigned_reports_count ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
