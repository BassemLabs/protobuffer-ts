// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: class_service/async_operation.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ObjectId } from "../utils/object_id";
import { RequestContext } from "../utils/request_context";

export const protobufPackage = "class_service.async_operation";

export enum AsyncOperationStatus {
  QUEUED = "QUEUED",
  RUNNING = "RUNNING",
  SUCCEEDED = "SUCCEEDED",
  PARTIALLY_SUCCEEDED = "PARTIALLY_SUCCEEDED",
  FAILED = "FAILED",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function asyncOperationStatusFromJSON(object: any): AsyncOperationStatus {
  switch (object) {
    case 0:
    case "QUEUED":
      return AsyncOperationStatus.QUEUED;
    case 1:
    case "RUNNING":
      return AsyncOperationStatus.RUNNING;
    case 2:
    case "SUCCEEDED":
      return AsyncOperationStatus.SUCCEEDED;
    case 3:
    case "PARTIALLY_SUCCEEDED":
      return AsyncOperationStatus.PARTIALLY_SUCCEEDED;
    case 4:
    case "FAILED":
      return AsyncOperationStatus.FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AsyncOperationStatus.UNRECOGNIZED;
  }
}

export function asyncOperationStatusToJSON(object: AsyncOperationStatus): string {
  switch (object) {
    case AsyncOperationStatus.QUEUED:
      return "QUEUED";
    case AsyncOperationStatus.RUNNING:
      return "RUNNING";
    case AsyncOperationStatus.SUCCEEDED:
      return "SUCCEEDED";
    case AsyncOperationStatus.PARTIALLY_SUCCEEDED:
      return "PARTIALLY_SUCCEEDED";
    case AsyncOperationStatus.FAILED:
      return "FAILED";
    case AsyncOperationStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function asyncOperationStatusToNumber(object: AsyncOperationStatus): number {
  switch (object) {
    case AsyncOperationStatus.QUEUED:
      return 0;
    case AsyncOperationStatus.RUNNING:
      return 1;
    case AsyncOperationStatus.SUCCEEDED:
      return 2;
    case AsyncOperationStatus.PARTIALLY_SUCCEEDED:
      return 3;
    case AsyncOperationStatus.FAILED:
      return 4;
    case AsyncOperationStatus.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum AsyncOperationTargetType {
  COURSE = "COURSE",
  HOMEROOM = "HOMEROOM",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function asyncOperationTargetTypeFromJSON(object: any): AsyncOperationTargetType {
  switch (object) {
    case 0:
    case "COURSE":
      return AsyncOperationTargetType.COURSE;
    case 1:
    case "HOMEROOM":
      return AsyncOperationTargetType.HOMEROOM;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AsyncOperationTargetType.UNRECOGNIZED;
  }
}

export function asyncOperationTargetTypeToJSON(object: AsyncOperationTargetType): string {
  switch (object) {
    case AsyncOperationTargetType.COURSE:
      return "COURSE";
    case AsyncOperationTargetType.HOMEROOM:
      return "HOMEROOM";
    case AsyncOperationTargetType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function asyncOperationTargetTypeToNumber(object: AsyncOperationTargetType): number {
  switch (object) {
    case AsyncOperationTargetType.COURSE:
      return 0;
    case AsyncOperationTargetType.HOMEROOM:
      return 1;
    case AsyncOperationTargetType.UNRECOGNIZED:
    default:
      return -1;
  }
}

export interface AsyncOperationFailure {
  student_id: ObjectId | undefined;
  reason?: string | undefined;
}

export interface AsyncOperation {
  id?: string | undefined;
  status?: AsyncOperationStatus | undefined;
  target_type?: AsyncOperationTargetType | undefined;
  target_id: ObjectId | undefined;
  total_items?: number | undefined;
  processed_items?: number | undefined;
  failures: AsyncOperationFailure[];
}

export interface GetAsyncOperationRequest {
  context: RequestContext | undefined;
  operation_id?: string | undefined;
}

function createBaseAsyncOperationFailure(): AsyncOperationFailure {
  return { student_id: undefined, reason: undefined };
}

export const AsyncOperationFailure: MessageFns<AsyncOperationFailure> = {
  encode(message: AsyncOperationFailure, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.student_id !== undefined) {
      ObjectId.encode(message.student_id, writer.uint32(10).fork()).join();
    }
    if (message.reason !== undefined) {
      writer.uint32(18).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AsyncOperationFailure {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAsyncOperationFailure();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.student_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AsyncOperationFailure {
    return {
      student_id: isSet(object.studentId) ? ObjectId.fromJSON(object.studentId) : undefined,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : undefined,
    };
  },

  toJSON(message: AsyncOperationFailure): unknown {
    const obj: any = {};
    if (message.student_id !== undefined) {
      obj.studentId = ObjectId.toJSON(message.student_id);
    }
    if (message.reason !== undefined) {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AsyncOperationFailure>, I>>(base?: I): AsyncOperationFailure {
    return AsyncOperationFailure.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AsyncOperationFailure>, I>>(object: I): AsyncOperationFailure {
    const message = createBaseAsyncOperationFailure();
    message.student_id = (object.student_id !== undefined && object.student_id !== null)
      ? ObjectId.fromPartial(object.student_id)
      : undefined;
    message.reason = object.reason ?? undefined;
    return message;
  },
};

function createBaseAsyncOperation(): AsyncOperation {
  return {
    id: undefined,
    status: undefined,
    target_type: undefined,
    target_id: undefined,
    total_items: undefined,
    processed_items: undefined,
    failures: [],
  };
}

export const AsyncOperation: MessageFns<AsyncOperation> = {
  encode(message: AsyncOperation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    if (message.status !== undefined) {
      writer.uint32(16).int32(asyncOperationStatusToNumber(message.status));
    }
    if (message.target_type !== undefined) {
      writer.uint32(24).int32(asyncOperationTargetTypeToNumber(message.target_type));
    }
    if (message.target_id !== undefined) {
      ObjectId.encode(message.target_id, writer.uint32(34).fork()).join();
    }
    if (message.total_items !== undefined) {
      writer.uint32(40).uint32(message.total_items);
    }
    if (message.processed_items !== undefined) {
      writer.uint32(48).uint32(message.processed_items);
    }
    for (const v of message.failures) {
      AsyncOperationFailure.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AsyncOperation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAsyncOperation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.status = asyncOperationStatusFromJSON(reader.int32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.target_type = asyncOperationTargetTypeFromJSON(reader.int32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.target_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.total_items = reader.uint32();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.processed_items = reader.uint32();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.failures.push(AsyncOperationFailure.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AsyncOperation {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      status: isSet(object.status) ? asyncOperationStatusFromJSON(object.status) : undefined,
      target_type: isSet(object.targetType) ? asyncOperationTargetTypeFromJSON(object.targetType) : undefined,
      target_id: isSet(object.targetId) ? ObjectId.fromJSON(object.targetId) : undefined,
      total_items: isSet(object.totalItems) ? globalThis.Number(object.totalItems) : undefined,
      processed_items: isSet(object.processedItems) ? globalThis.Number(object.processedItems) : undefined,
      failures: globalThis.Array.isArray(object?.failures)
        ? object.failures.map((e: any) => AsyncOperationFailure.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AsyncOperation): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.status !== undefined) {
      obj.status = asyncOperationStatusToJSON(message.status);
    }
    if (message.target_type !== undefined) {
      obj.targetType = asyncOperationTargetTypeToJSON(message.target_type);
    }
    if (message.target_id !== undefined) {
      obj.targetId = ObjectId.toJSON(message.target_id);
    }
    if (message.total_items !== undefined) {
      obj.totalItems = Math.round(message.total_items);
    }
    if (message.processed_items !== undefined) {
      obj.processedItems = Math.round(message.processed_items);
    }
    if (message.failures?.length) {
      obj.failures = message.failures.map((e) => AsyncOperationFailure.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AsyncOperation>, I>>(base?: I): AsyncOperation {
    return AsyncOperation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AsyncOperation>, I>>(object: I): AsyncOperation {
    const message = createBaseAsyncOperation();
    message.id = object.id ?? undefined;
    message.status = object.status ?? undefined;
    message.target_type = object.target_type ?? undefined;
    message.target_id = (object.target_id !== undefined && object.target_id !== null)
      ? ObjectId.fromPartial(object.target_id)
      : undefined;
    message.total_items = object.total_items ?? undefined;
    message.processed_items = object.processed_items ?? undefined;
    message.failures = object.failures?.map((e) => AsyncOperationFailure.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetAsyncOperationRequest(): GetAsyncOperationRequest {
  return { context: undefined, operation_id: undefined };
}

export const GetAsyncOperationRequest: MessageFns<GetAsyncOperationRequest> = {
  encode(message: GetAsyncOperationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.operation_id !== undefined) {
      writer.uint32(18).string(message.operation_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAsyncOperationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAsyncOperationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.operation_id = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAsyncOperationRequest {
    return {
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      operation_id: isSet(object.operationId) ? globalThis.String(object.operationId) : undefined,
    };
  },

  toJSON(message: GetAsyncOperationRequest): unknown {
    const obj: any = {};
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.operation_id !== undefined) {
      obj.operationId = message.operation_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetAsyncOperationRequest>, I>>(base?: I): GetAsyncOperationRequest {
    return GetAsyncOperationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetAsyncOperationRequest>, I>>(object: I): GetAsyncOperationRequest {
    const message = createBaseGetAsyncOperationRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.operation_id = object.operation_id ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
