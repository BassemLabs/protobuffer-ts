// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: class_service/scheduling.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { DayOfWeek, dayOfWeekFromJSON, dayOfWeekToJSON, dayOfWeekToNumber } from "../google/type/dayofweek";
import { Campus } from "../organization_service/campus";
import { StudentGrade, studentGradeFromJSON, studentGradeToJSON, studentGradeToNumber } from "../user_service/student";
import { TeacherBasic } from "../user_service/teacher";
import { ObjectId } from "../utils/object_id";
import { Uuid } from "../utils/uuid";
import { AbstractCourse } from "./abstract_course";
import { Semester } from "./semester";

export const protobufPackage = "class_service";

export enum SchedulingPreparationStep {
  SCHEDULING_PREPARATION_STEP_SEMESTERS = "SCHEDULING_PREPARATION_STEP_SEMESTERS",
  SCHEDULING_PREPARATION_STEP_PERIOD_TIMES = "SCHEDULING_PREPARATION_STEP_PERIOD_TIMES",
  SCHEDULING_PREPARATION_STEP_CLASSES = "SCHEDULING_PREPARATION_STEP_CLASSES",
  SCHEDULING_PREPARATION_STEP_TEACHERS = "SCHEDULING_PREPARATION_STEP_TEACHERS",
  SCHEDULING_PREPARATION_STEP_CLASS_ASSIGNMENTS = "SCHEDULING_PREPARATION_STEP_CLASS_ASSIGNMENTS",
  SCHEDULING_PREPARATION_STEP_REVIEW = "SCHEDULING_PREPARATION_STEP_REVIEW",
  SCHEDULING_PREPARATION_STEP_GENERATION = "SCHEDULING_PREPARATION_STEP_GENERATION",
  SCHEDULING_PREPARATION_STEP_PREVIEW_EXPORT = "SCHEDULING_PREPARATION_STEP_PREVIEW_EXPORT",
  SCHEDULING_PREPARATION_STEP_ADJUSTMENTS = "SCHEDULING_PREPARATION_STEP_ADJUSTMENTS",
  SCHEDULING_PREPARATION_STEP_OFFERED_GRADES = "SCHEDULING_PREPARATION_STEP_OFFERED_GRADES",
  SCHEDULING_PREPARATION_STEP_CLASS_GROUPS = "SCHEDULING_PREPARATION_STEP_CLASS_GROUPS",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function schedulingPreparationStepFromJSON(object: any): SchedulingPreparationStep {
  switch (object) {
    case 1:
    case "SCHEDULING_PREPARATION_STEP_SEMESTERS":
      return SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_SEMESTERS;
    case 2:
    case "SCHEDULING_PREPARATION_STEP_PERIOD_TIMES":
      return SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_PERIOD_TIMES;
    case 3:
    case "SCHEDULING_PREPARATION_STEP_CLASSES":
      return SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_CLASSES;
    case 4:
    case "SCHEDULING_PREPARATION_STEP_TEACHERS":
      return SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_TEACHERS;
    case 5:
    case "SCHEDULING_PREPARATION_STEP_CLASS_ASSIGNMENTS":
      return SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_CLASS_ASSIGNMENTS;
    case 6:
    case "SCHEDULING_PREPARATION_STEP_REVIEW":
      return SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_REVIEW;
    case 7:
    case "SCHEDULING_PREPARATION_STEP_GENERATION":
      return SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_GENERATION;
    case 8:
    case "SCHEDULING_PREPARATION_STEP_PREVIEW_EXPORT":
      return SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_PREVIEW_EXPORT;
    case 9:
    case "SCHEDULING_PREPARATION_STEP_ADJUSTMENTS":
      return SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_ADJUSTMENTS;
    case 10:
    case "SCHEDULING_PREPARATION_STEP_OFFERED_GRADES":
      return SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_OFFERED_GRADES;
    case 11:
    case "SCHEDULING_PREPARATION_STEP_CLASS_GROUPS":
      return SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_CLASS_GROUPS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SchedulingPreparationStep.UNRECOGNIZED;
  }
}

export function schedulingPreparationStepToJSON(object: SchedulingPreparationStep): string {
  switch (object) {
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_SEMESTERS:
      return "SCHEDULING_PREPARATION_STEP_SEMESTERS";
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_PERIOD_TIMES:
      return "SCHEDULING_PREPARATION_STEP_PERIOD_TIMES";
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_CLASSES:
      return "SCHEDULING_PREPARATION_STEP_CLASSES";
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_TEACHERS:
      return "SCHEDULING_PREPARATION_STEP_TEACHERS";
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_CLASS_ASSIGNMENTS:
      return "SCHEDULING_PREPARATION_STEP_CLASS_ASSIGNMENTS";
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_REVIEW:
      return "SCHEDULING_PREPARATION_STEP_REVIEW";
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_GENERATION:
      return "SCHEDULING_PREPARATION_STEP_GENERATION";
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_PREVIEW_EXPORT:
      return "SCHEDULING_PREPARATION_STEP_PREVIEW_EXPORT";
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_ADJUSTMENTS:
      return "SCHEDULING_PREPARATION_STEP_ADJUSTMENTS";
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_OFFERED_GRADES:
      return "SCHEDULING_PREPARATION_STEP_OFFERED_GRADES";
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_CLASS_GROUPS:
      return "SCHEDULING_PREPARATION_STEP_CLASS_GROUPS";
    case SchedulingPreparationStep.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function schedulingPreparationStepToNumber(object: SchedulingPreparationStep): number {
  switch (object) {
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_SEMESTERS:
      return 1;
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_PERIOD_TIMES:
      return 2;
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_CLASSES:
      return 3;
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_TEACHERS:
      return 4;
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_CLASS_ASSIGNMENTS:
      return 5;
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_REVIEW:
      return 6;
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_GENERATION:
      return 7;
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_PREVIEW_EXPORT:
      return 8;
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_ADJUSTMENTS:
      return 9;
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_OFFERED_GRADES:
      return 10;
    case SchedulingPreparationStep.SCHEDULING_PREPARATION_STEP_CLASS_GROUPS:
      return 11;
    case SchedulingPreparationStep.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum SchedulingPreparationStepStatus {
  SCHEDULING_PREPARATION_STEP_STATUS_BLOCKED = "SCHEDULING_PREPARATION_STEP_STATUS_BLOCKED",
  SCHEDULING_PREPARATION_STEP_STATUS_READY = "SCHEDULING_PREPARATION_STEP_STATUS_READY",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function schedulingPreparationStepStatusFromJSON(object: any): SchedulingPreparationStepStatus {
  switch (object) {
    case 1:
    case "SCHEDULING_PREPARATION_STEP_STATUS_BLOCKED":
      return SchedulingPreparationStepStatus.SCHEDULING_PREPARATION_STEP_STATUS_BLOCKED;
    case 2:
    case "SCHEDULING_PREPARATION_STEP_STATUS_READY":
      return SchedulingPreparationStepStatus.SCHEDULING_PREPARATION_STEP_STATUS_READY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SchedulingPreparationStepStatus.UNRECOGNIZED;
  }
}

export function schedulingPreparationStepStatusToJSON(object: SchedulingPreparationStepStatus): string {
  switch (object) {
    case SchedulingPreparationStepStatus.SCHEDULING_PREPARATION_STEP_STATUS_BLOCKED:
      return "SCHEDULING_PREPARATION_STEP_STATUS_BLOCKED";
    case SchedulingPreparationStepStatus.SCHEDULING_PREPARATION_STEP_STATUS_READY:
      return "SCHEDULING_PREPARATION_STEP_STATUS_READY";
    case SchedulingPreparationStepStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function schedulingPreparationStepStatusToNumber(object: SchedulingPreparationStepStatus): number {
  switch (object) {
    case SchedulingPreparationStepStatus.SCHEDULING_PREPARATION_STEP_STATUS_BLOCKED:
      return 1;
    case SchedulingPreparationStepStatus.SCHEDULING_PREPARATION_STEP_STATUS_READY:
      return 2;
    case SchedulingPreparationStepStatus.UNRECOGNIZED:
    default:
      return -1;
  }
}

/** How a subject/course may use back-to-back (double) periods during generation. */
export enum SchedulingDoublePeriodMode {
  /** SCHEDULING_DOUBLE_PERIOD_MODE_NONE - Never schedule two consecutive periods of this class on the same day. */
  SCHEDULING_DOUBLE_PERIOD_MODE_NONE = "SCHEDULING_DOUBLE_PERIOD_MODE_NONE",
  /** SCHEDULING_DOUBLE_PERIOD_MODE_ALLOW - Consecutive periods are permitted but not required. */
  SCHEDULING_DOUBLE_PERIOD_MODE_ALLOW = "SCHEDULING_DOUBLE_PERIOD_MODE_ALLOW",
  /** SCHEDULING_DOUBLE_PERIOD_MODE_REQUIRE - Weekly periods must be scheduled as consecutive pairs (requires an even count). */
  SCHEDULING_DOUBLE_PERIOD_MODE_REQUIRE = "SCHEDULING_DOUBLE_PERIOD_MODE_REQUIRE",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function schedulingDoublePeriodModeFromJSON(object: any): SchedulingDoublePeriodMode {
  switch (object) {
    case 1:
    case "SCHEDULING_DOUBLE_PERIOD_MODE_NONE":
      return SchedulingDoublePeriodMode.SCHEDULING_DOUBLE_PERIOD_MODE_NONE;
    case 2:
    case "SCHEDULING_DOUBLE_PERIOD_MODE_ALLOW":
      return SchedulingDoublePeriodMode.SCHEDULING_DOUBLE_PERIOD_MODE_ALLOW;
    case 3:
    case "SCHEDULING_DOUBLE_PERIOD_MODE_REQUIRE":
      return SchedulingDoublePeriodMode.SCHEDULING_DOUBLE_PERIOD_MODE_REQUIRE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SchedulingDoublePeriodMode.UNRECOGNIZED;
  }
}

export function schedulingDoublePeriodModeToJSON(object: SchedulingDoublePeriodMode): string {
  switch (object) {
    case SchedulingDoublePeriodMode.SCHEDULING_DOUBLE_PERIOD_MODE_NONE:
      return "SCHEDULING_DOUBLE_PERIOD_MODE_NONE";
    case SchedulingDoublePeriodMode.SCHEDULING_DOUBLE_PERIOD_MODE_ALLOW:
      return "SCHEDULING_DOUBLE_PERIOD_MODE_ALLOW";
    case SchedulingDoublePeriodMode.SCHEDULING_DOUBLE_PERIOD_MODE_REQUIRE:
      return "SCHEDULING_DOUBLE_PERIOD_MODE_REQUIRE";
    case SchedulingDoublePeriodMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function schedulingDoublePeriodModeToNumber(object: SchedulingDoublePeriodMode): number {
  switch (object) {
    case SchedulingDoublePeriodMode.SCHEDULING_DOUBLE_PERIOD_MODE_NONE:
      return 1;
    case SchedulingDoublePeriodMode.SCHEDULING_DOUBLE_PERIOD_MODE_ALLOW:
      return 2;
    case SchedulingDoublePeriodMode.SCHEDULING_DOUBLE_PERIOD_MODE_REQUIRE:
      return 3;
    case SchedulingDoublePeriodMode.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum SchedulingGradeWeeklyAllocationStatus {
  SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_PERIOD_SETUP =
    "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_PERIOD_SETUP",
  SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_REQUIREMENTS =
    "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_REQUIREMENTS",
  SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_CAMPUS_CAPACITY_CONFLICT =
    "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_CAMPUS_CAPACITY_CONFLICT",
  SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_UNDER_ALLOCATED =
    "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_UNDER_ALLOCATED",
  SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_BALANCED = "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_BALANCED",
  SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_OVER_ALLOCATED = "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_OVER_ALLOCATED",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function schedulingGradeWeeklyAllocationStatusFromJSON(object: any): SchedulingGradeWeeklyAllocationStatus {
  switch (object) {
    case 1:
    case "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_PERIOD_SETUP":
      return SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_PERIOD_SETUP;
    case 2:
    case "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_REQUIREMENTS":
      return SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_REQUIREMENTS;
    case 3:
    case "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_CAMPUS_CAPACITY_CONFLICT":
      return SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_CAMPUS_CAPACITY_CONFLICT;
    case 4:
    case "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_UNDER_ALLOCATED":
      return SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_UNDER_ALLOCATED;
    case 5:
    case "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_BALANCED":
      return SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_BALANCED;
    case 6:
    case "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_OVER_ALLOCATED":
      return SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_OVER_ALLOCATED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SchedulingGradeWeeklyAllocationStatus.UNRECOGNIZED;
  }
}

export function schedulingGradeWeeklyAllocationStatusToJSON(object: SchedulingGradeWeeklyAllocationStatus): string {
  switch (object) {
    case SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_PERIOD_SETUP:
      return "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_PERIOD_SETUP";
    case SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_REQUIREMENTS:
      return "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_REQUIREMENTS";
    case SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_CAMPUS_CAPACITY_CONFLICT:
      return "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_CAMPUS_CAPACITY_CONFLICT";
    case SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_UNDER_ALLOCATED:
      return "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_UNDER_ALLOCATED";
    case SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_BALANCED:
      return "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_BALANCED";
    case SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_OVER_ALLOCATED:
      return "SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_OVER_ALLOCATED";
    case SchedulingGradeWeeklyAllocationStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function schedulingGradeWeeklyAllocationStatusToNumber(object: SchedulingGradeWeeklyAllocationStatus): number {
  switch (object) {
    case SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_PERIOD_SETUP:
      return 1;
    case SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_AWAITING_REQUIREMENTS:
      return 2;
    case SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_CAMPUS_CAPACITY_CONFLICT:
      return 3;
    case SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_UNDER_ALLOCATED:
      return 4;
    case SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_BALANCED:
      return 5;
    case SchedulingGradeWeeklyAllocationStatus.SCHEDULING_GRADE_WEEKLY_ALLOCATION_STATUS_OVER_ALLOCATED:
      return 6;
    case SchedulingGradeWeeklyAllocationStatus.UNRECOGNIZED:
    default:
      return -1;
  }
}

/**
 * Lifecycle of one generation run. Terminal states are SUCCEEDED and FAILED;
 * CANCELLED is reserved for future use (nothing sets it yet).
 */
export enum SchedulingGenerationRunStatus {
  /** SCHEDULING_GENERATION_RUN_STATUS_PENDING - Created, not yet picked up by the background executor. */
  SCHEDULING_GENERATION_RUN_STATUS_PENDING = "SCHEDULING_GENERATION_RUN_STATUS_PENDING",
  /** SCHEDULING_GENERATION_RUN_STATUS_RUNNING - Preflight/solve in progress. */
  SCHEDULING_GENERATION_RUN_STATUS_RUNNING = "SCHEDULING_GENERATION_RUN_STATUS_RUNNING",
  /** SCHEDULING_GENERATION_RUN_STATUS_SUCCEEDED - Solver produced a schedule (see generated_schedule_id). */
  SCHEDULING_GENERATION_RUN_STATUS_SUCCEEDED = "SCHEDULING_GENERATION_RUN_STATUS_SUCCEEDED",
  /** SCHEDULING_GENERATION_RUN_STATUS_FAILED - Preflight blocked, solver infeasible/timed out, or an internal error (see error_message/blockers). */
  SCHEDULING_GENERATION_RUN_STATUS_FAILED = "SCHEDULING_GENERATION_RUN_STATUS_FAILED",
  /** SCHEDULING_GENERATION_RUN_STATUS_CANCELLED - Reserved for future cancellation support. */
  SCHEDULING_GENERATION_RUN_STATUS_CANCELLED = "SCHEDULING_GENERATION_RUN_STATUS_CANCELLED",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function schedulingGenerationRunStatusFromJSON(object: any): SchedulingGenerationRunStatus {
  switch (object) {
    case 1:
    case "SCHEDULING_GENERATION_RUN_STATUS_PENDING":
      return SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_PENDING;
    case 2:
    case "SCHEDULING_GENERATION_RUN_STATUS_RUNNING":
      return SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_RUNNING;
    case 3:
    case "SCHEDULING_GENERATION_RUN_STATUS_SUCCEEDED":
      return SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_SUCCEEDED;
    case 4:
    case "SCHEDULING_GENERATION_RUN_STATUS_FAILED":
      return SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_FAILED;
    case 5:
    case "SCHEDULING_GENERATION_RUN_STATUS_CANCELLED":
      return SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_CANCELLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SchedulingGenerationRunStatus.UNRECOGNIZED;
  }
}

export function schedulingGenerationRunStatusToJSON(object: SchedulingGenerationRunStatus): string {
  switch (object) {
    case SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_PENDING:
      return "SCHEDULING_GENERATION_RUN_STATUS_PENDING";
    case SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_RUNNING:
      return "SCHEDULING_GENERATION_RUN_STATUS_RUNNING";
    case SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_SUCCEEDED:
      return "SCHEDULING_GENERATION_RUN_STATUS_SUCCEEDED";
    case SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_FAILED:
      return "SCHEDULING_GENERATION_RUN_STATUS_FAILED";
    case SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_CANCELLED:
      return "SCHEDULING_GENERATION_RUN_STATUS_CANCELLED";
    case SchedulingGenerationRunStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function schedulingGenerationRunStatusToNumber(object: SchedulingGenerationRunStatus): number {
  switch (object) {
    case SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_PENDING:
      return 1;
    case SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_RUNNING:
      return 2;
    case SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_SUCCEEDED:
      return 3;
    case SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_FAILED:
      return 4;
    case SchedulingGenerationRunStatus.SCHEDULING_GENERATION_RUN_STATUS_CANCELLED:
      return 5;
    case SchedulingGenerationRunStatus.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum SchedulingGenerationPurpose {
  SCHEDULING_GENERATION_PURPOSE_CANDIDATE = "SCHEDULING_GENERATION_PURPOSE_CANDIDATE",
  SCHEDULING_GENERATION_PURPOSE_WORKING_REVISION_PROPOSAL = "SCHEDULING_GENERATION_PURPOSE_WORKING_REVISION_PROPOSAL",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function schedulingGenerationPurposeFromJSON(object: any): SchedulingGenerationPurpose {
  switch (object) {
    case 1:
    case "SCHEDULING_GENERATION_PURPOSE_CANDIDATE":
      return SchedulingGenerationPurpose.SCHEDULING_GENERATION_PURPOSE_CANDIDATE;
    case 2:
    case "SCHEDULING_GENERATION_PURPOSE_WORKING_REVISION_PROPOSAL":
      return SchedulingGenerationPurpose.SCHEDULING_GENERATION_PURPOSE_WORKING_REVISION_PROPOSAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SchedulingGenerationPurpose.UNRECOGNIZED;
  }
}

export function schedulingGenerationPurposeToJSON(object: SchedulingGenerationPurpose): string {
  switch (object) {
    case SchedulingGenerationPurpose.SCHEDULING_GENERATION_PURPOSE_CANDIDATE:
      return "SCHEDULING_GENERATION_PURPOSE_CANDIDATE";
    case SchedulingGenerationPurpose.SCHEDULING_GENERATION_PURPOSE_WORKING_REVISION_PROPOSAL:
      return "SCHEDULING_GENERATION_PURPOSE_WORKING_REVISION_PROPOSAL";
    case SchedulingGenerationPurpose.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function schedulingGenerationPurposeToNumber(object: SchedulingGenerationPurpose): number {
  switch (object) {
    case SchedulingGenerationPurpose.SCHEDULING_GENERATION_PURPOSE_CANDIDATE:
      return 1;
    case SchedulingGenerationPurpose.SCHEDULING_GENERATION_PURPOSE_WORKING_REVISION_PROPOSAL:
      return 2;
    case SchedulingGenerationPurpose.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum SchedulingScheduleSetupCompatibility {
  /** SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_CURRENT - The schedule's validation snapshot matches the currently reviewed setup. */
  SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_CURRENT = "SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_CURRENT",
  /** SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_SETUP_CHANGED - Preparation data changed after this schedule was validated. */
  SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_SETUP_CHANGED = "SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_SETUP_CHANGED",
  /** SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_REVIEW_REQUIRED - Review is not currently ready, so compatibility cannot be established. */
  SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_REVIEW_REQUIRED = "SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_REVIEW_REQUIRED",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function schedulingScheduleSetupCompatibilityFromJSON(object: any): SchedulingScheduleSetupCompatibility {
  switch (object) {
    case 1:
    case "SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_CURRENT":
      return SchedulingScheduleSetupCompatibility.SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_CURRENT;
    case 2:
    case "SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_SETUP_CHANGED":
      return SchedulingScheduleSetupCompatibility.SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_SETUP_CHANGED;
    case 3:
    case "SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_REVIEW_REQUIRED":
      return SchedulingScheduleSetupCompatibility.SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_REVIEW_REQUIRED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SchedulingScheduleSetupCompatibility.UNRECOGNIZED;
  }
}

export function schedulingScheduleSetupCompatibilityToJSON(object: SchedulingScheduleSetupCompatibility): string {
  switch (object) {
    case SchedulingScheduleSetupCompatibility.SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_CURRENT:
      return "SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_CURRENT";
    case SchedulingScheduleSetupCompatibility.SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_SETUP_CHANGED:
      return "SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_SETUP_CHANGED";
    case SchedulingScheduleSetupCompatibility.SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_REVIEW_REQUIRED:
      return "SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_REVIEW_REQUIRED";
    case SchedulingScheduleSetupCompatibility.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function schedulingScheduleSetupCompatibilityToNumber(object: SchedulingScheduleSetupCompatibility): number {
  switch (object) {
    case SchedulingScheduleSetupCompatibility.SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_CURRENT:
      return 1;
    case SchedulingScheduleSetupCompatibility.SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_SETUP_CHANGED:
      return 2;
    case SchedulingScheduleSetupCompatibility.SCHEDULING_SCHEDULE_SETUP_COMPATIBILITY_REVIEW_REQUIRED:
      return 3;
    case SchedulingScheduleSetupCompatibility.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum SchedulingScheduleRevisionKind {
  SCHEDULING_SCHEDULE_REVISION_KIND_BASE_SELECTED = "SCHEDULING_SCHEDULE_REVISION_KIND_BASE_SELECTED",
  SCHEDULING_SCHEDULE_REVISION_KIND_SETUP_REVALIDATED = "SCHEDULING_SCHEDULE_REVISION_KIND_SETUP_REVALIDATED",
  SCHEDULING_SCHEDULE_REVISION_KIND_RESTORED = "SCHEDULING_SCHEDULE_REVISION_KIND_RESTORED",
  SCHEDULING_SCHEDULE_REVISION_KIND_CHECKPOINT = "SCHEDULING_SCHEDULE_REVISION_KIND_CHECKPOINT",
  /** SCHEDULING_SCHEDULE_REVISION_KIND_MOVE - Preserved so historical move revisions remain readable. */
  SCHEDULING_SCHEDULE_REVISION_KIND_MOVE = "SCHEDULING_SCHEDULE_REVISION_KIND_MOVE",
  SCHEDULING_SCHEDULE_REVISION_KIND_SWAP = "SCHEDULING_SCHEDULE_REVISION_KIND_SWAP",
  SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REGENERATED = "SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REGENERATED",
  SCHEDULING_SCHEDULE_REVISION_KIND_RETURNED = "SCHEDULING_SCHEDULE_REVISION_KIND_RETURNED",
  SCHEDULING_SCHEDULE_REVISION_KIND_UNDO = "SCHEDULING_SCHEDULE_REVISION_KIND_UNDO",
  SCHEDULING_SCHEDULE_REVISION_KIND_REDO = "SCHEDULING_SCHEDULE_REVISION_KIND_REDO",
  SCHEDULING_SCHEDULE_REVISION_KIND_PIN_ADDED = "SCHEDULING_SCHEDULE_REVISION_KIND_PIN_ADDED",
  SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REMOVED = "SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REMOVED",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function schedulingScheduleRevisionKindFromJSON(object: any): SchedulingScheduleRevisionKind {
  switch (object) {
    case 1:
    case "SCHEDULING_SCHEDULE_REVISION_KIND_BASE_SELECTED":
      return SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_BASE_SELECTED;
    case 2:
    case "SCHEDULING_SCHEDULE_REVISION_KIND_SETUP_REVALIDATED":
      return SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_SETUP_REVALIDATED;
    case 3:
    case "SCHEDULING_SCHEDULE_REVISION_KIND_RESTORED":
      return SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_RESTORED;
    case 4:
    case "SCHEDULING_SCHEDULE_REVISION_KIND_CHECKPOINT":
      return SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_CHECKPOINT;
    case 5:
    case "SCHEDULING_SCHEDULE_REVISION_KIND_MOVE":
      return SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_MOVE;
    case 6:
    case "SCHEDULING_SCHEDULE_REVISION_KIND_SWAP":
      return SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_SWAP;
    case 7:
    case "SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REGENERATED":
      return SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REGENERATED;
    case 8:
    case "SCHEDULING_SCHEDULE_REVISION_KIND_RETURNED":
      return SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_RETURNED;
    case 9:
    case "SCHEDULING_SCHEDULE_REVISION_KIND_UNDO":
      return SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_UNDO;
    case 10:
    case "SCHEDULING_SCHEDULE_REVISION_KIND_REDO":
      return SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_REDO;
    case 11:
    case "SCHEDULING_SCHEDULE_REVISION_KIND_PIN_ADDED":
      return SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_PIN_ADDED;
    case 12:
    case "SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REMOVED":
      return SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REMOVED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SchedulingScheduleRevisionKind.UNRECOGNIZED;
  }
}

export function schedulingScheduleRevisionKindToJSON(object: SchedulingScheduleRevisionKind): string {
  switch (object) {
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_BASE_SELECTED:
      return "SCHEDULING_SCHEDULE_REVISION_KIND_BASE_SELECTED";
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_SETUP_REVALIDATED:
      return "SCHEDULING_SCHEDULE_REVISION_KIND_SETUP_REVALIDATED";
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_RESTORED:
      return "SCHEDULING_SCHEDULE_REVISION_KIND_RESTORED";
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_CHECKPOINT:
      return "SCHEDULING_SCHEDULE_REVISION_KIND_CHECKPOINT";
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_MOVE:
      return "SCHEDULING_SCHEDULE_REVISION_KIND_MOVE";
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_SWAP:
      return "SCHEDULING_SCHEDULE_REVISION_KIND_SWAP";
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REGENERATED:
      return "SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REGENERATED";
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_RETURNED:
      return "SCHEDULING_SCHEDULE_REVISION_KIND_RETURNED";
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_UNDO:
      return "SCHEDULING_SCHEDULE_REVISION_KIND_UNDO";
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_REDO:
      return "SCHEDULING_SCHEDULE_REVISION_KIND_REDO";
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_PIN_ADDED:
      return "SCHEDULING_SCHEDULE_REVISION_KIND_PIN_ADDED";
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REMOVED:
      return "SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REMOVED";
    case SchedulingScheduleRevisionKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function schedulingScheduleRevisionKindToNumber(object: SchedulingScheduleRevisionKind): number {
  switch (object) {
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_BASE_SELECTED:
      return 1;
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_SETUP_REVALIDATED:
      return 2;
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_RESTORED:
      return 3;
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_CHECKPOINT:
      return 4;
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_MOVE:
      return 5;
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_SWAP:
      return 6;
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REGENERATED:
      return 7;
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_RETURNED:
      return 8;
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_UNDO:
      return 9;
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_REDO:
      return 10;
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_PIN_ADDED:
      return 11;
    case SchedulingScheduleRevisionKind.SCHEDULING_SCHEDULE_REVISION_KIND_PIN_REMOVED:
      return 12;
    case SchedulingScheduleRevisionKind.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum SchedulingScheduleAdjustmentIssueSeverity {
  SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_BLOCKING = "SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_BLOCKING",
  SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_WARNING = "SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_WARNING",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function schedulingScheduleAdjustmentIssueSeverityFromJSON(
  object: any,
): SchedulingScheduleAdjustmentIssueSeverity {
  switch (object) {
    case 1:
    case "SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_BLOCKING":
      return SchedulingScheduleAdjustmentIssueSeverity.SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_BLOCKING;
    case 2:
    case "SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_WARNING":
      return SchedulingScheduleAdjustmentIssueSeverity.SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_WARNING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SchedulingScheduleAdjustmentIssueSeverity.UNRECOGNIZED;
  }
}

export function schedulingScheduleAdjustmentIssueSeverityToJSON(
  object: SchedulingScheduleAdjustmentIssueSeverity,
): string {
  switch (object) {
    case SchedulingScheduleAdjustmentIssueSeverity.SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_BLOCKING:
      return "SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_BLOCKING";
    case SchedulingScheduleAdjustmentIssueSeverity.SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_WARNING:
      return "SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_WARNING";
    case SchedulingScheduleAdjustmentIssueSeverity.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function schedulingScheduleAdjustmentIssueSeverityToNumber(
  object: SchedulingScheduleAdjustmentIssueSeverity,
): number {
  switch (object) {
    case SchedulingScheduleAdjustmentIssueSeverity.SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_BLOCKING:
      return 1;
    case SchedulingScheduleAdjustmentIssueSeverity.SCHEDULING_SCHEDULE_ADJUSTMENT_ISSUE_SEVERITY_WARNING:
      return 2;
    case SchedulingScheduleAdjustmentIssueSeverity.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum SchedulingScheduleAdjustmentKind {
  SCHEDULING_SCHEDULE_ADJUSTMENT_KIND_SWAP = "SCHEDULING_SCHEDULE_ADJUSTMENT_KIND_SWAP",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function schedulingScheduleAdjustmentKindFromJSON(object: any): SchedulingScheduleAdjustmentKind {
  switch (object) {
    case 2:
    case "SCHEDULING_SCHEDULE_ADJUSTMENT_KIND_SWAP":
      return SchedulingScheduleAdjustmentKind.SCHEDULING_SCHEDULE_ADJUSTMENT_KIND_SWAP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SchedulingScheduleAdjustmentKind.UNRECOGNIZED;
  }
}

export function schedulingScheduleAdjustmentKindToJSON(object: SchedulingScheduleAdjustmentKind): string {
  switch (object) {
    case SchedulingScheduleAdjustmentKind.SCHEDULING_SCHEDULE_ADJUSTMENT_KIND_SWAP:
      return "SCHEDULING_SCHEDULE_ADJUSTMENT_KIND_SWAP";
    case SchedulingScheduleAdjustmentKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function schedulingScheduleAdjustmentKindToNumber(object: SchedulingScheduleAdjustmentKind): number {
  switch (object) {
    case SchedulingScheduleAdjustmentKind.SCHEDULING_SCHEDULE_ADJUSTMENT_KIND_SWAP:
      return 2;
    case SchedulingScheduleAdjustmentKind.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum SchedulingSchedulePinScope {
  SCHEDULING_SCHEDULE_PIN_SCOPE_PLACEMENT = "SCHEDULING_SCHEDULE_PIN_SCOPE_PLACEMENT",
  SCHEDULING_SCHEDULE_PIN_SCOPE_CLASS = "SCHEDULING_SCHEDULE_PIN_SCOPE_CLASS",
  SCHEDULING_SCHEDULE_PIN_SCOPE_SECTION = "SCHEDULING_SCHEDULE_PIN_SCOPE_SECTION",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function schedulingSchedulePinScopeFromJSON(object: any): SchedulingSchedulePinScope {
  switch (object) {
    case 1:
    case "SCHEDULING_SCHEDULE_PIN_SCOPE_PLACEMENT":
      return SchedulingSchedulePinScope.SCHEDULING_SCHEDULE_PIN_SCOPE_PLACEMENT;
    case 2:
    case "SCHEDULING_SCHEDULE_PIN_SCOPE_CLASS":
      return SchedulingSchedulePinScope.SCHEDULING_SCHEDULE_PIN_SCOPE_CLASS;
    case 3:
    case "SCHEDULING_SCHEDULE_PIN_SCOPE_SECTION":
      return SchedulingSchedulePinScope.SCHEDULING_SCHEDULE_PIN_SCOPE_SECTION;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SchedulingSchedulePinScope.UNRECOGNIZED;
  }
}

export function schedulingSchedulePinScopeToJSON(object: SchedulingSchedulePinScope): string {
  switch (object) {
    case SchedulingSchedulePinScope.SCHEDULING_SCHEDULE_PIN_SCOPE_PLACEMENT:
      return "SCHEDULING_SCHEDULE_PIN_SCOPE_PLACEMENT";
    case SchedulingSchedulePinScope.SCHEDULING_SCHEDULE_PIN_SCOPE_CLASS:
      return "SCHEDULING_SCHEDULE_PIN_SCOPE_CLASS";
    case SchedulingSchedulePinScope.SCHEDULING_SCHEDULE_PIN_SCOPE_SECTION:
      return "SCHEDULING_SCHEDULE_PIN_SCOPE_SECTION";
    case SchedulingSchedulePinScope.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function schedulingSchedulePinScopeToNumber(object: SchedulingSchedulePinScope): number {
  switch (object) {
    case SchedulingSchedulePinScope.SCHEDULING_SCHEDULE_PIN_SCOPE_PLACEMENT:
      return 1;
    case SchedulingSchedulePinScope.SCHEDULING_SCHEDULE_PIN_SCOPE_CLASS:
      return 2;
    case SchedulingSchedulePinScope.SCHEDULING_SCHEDULE_PIN_SCOPE_SECTION:
      return 3;
    case SchedulingSchedulePinScope.UNRECOGNIZED:
    default:
      return -1;
  }
}

/**
 * The setup step a review section maps back to, so an issue can deep-link the user to
 * the exact page to fix.
 */
export enum SchedulingReviewSectionType {
  SCHEDULING_REVIEW_SECTION_TYPE_SEMESTERS_GRADES = "SCHEDULING_REVIEW_SECTION_TYPE_SEMESTERS_GRADES",
  SCHEDULING_REVIEW_SECTION_TYPE_PERIOD_TIMES = "SCHEDULING_REVIEW_SECTION_TYPE_PERIOD_TIMES",
  SCHEDULING_REVIEW_SECTION_TYPE_CLASSES = "SCHEDULING_REVIEW_SECTION_TYPE_CLASSES",
  SCHEDULING_REVIEW_SECTION_TYPE_TEACHERS = "SCHEDULING_REVIEW_SECTION_TYPE_TEACHERS",
  SCHEDULING_REVIEW_SECTION_TYPE_CLASS_ASSIGNMENTS = "SCHEDULING_REVIEW_SECTION_TYPE_CLASS_ASSIGNMENTS",
  SCHEDULING_REVIEW_SECTION_TYPE_CLASS_GROUPS = "SCHEDULING_REVIEW_SECTION_TYPE_CLASS_GROUPS",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function schedulingReviewSectionTypeFromJSON(object: any): SchedulingReviewSectionType {
  switch (object) {
    case 1:
    case "SCHEDULING_REVIEW_SECTION_TYPE_SEMESTERS_GRADES":
      return SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_SEMESTERS_GRADES;
    case 2:
    case "SCHEDULING_REVIEW_SECTION_TYPE_PERIOD_TIMES":
      return SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_PERIOD_TIMES;
    case 3:
    case "SCHEDULING_REVIEW_SECTION_TYPE_CLASSES":
      return SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_CLASSES;
    case 4:
    case "SCHEDULING_REVIEW_SECTION_TYPE_TEACHERS":
      return SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_TEACHERS;
    case 5:
    case "SCHEDULING_REVIEW_SECTION_TYPE_CLASS_ASSIGNMENTS":
      return SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_CLASS_ASSIGNMENTS;
    case 6:
    case "SCHEDULING_REVIEW_SECTION_TYPE_CLASS_GROUPS":
      return SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_CLASS_GROUPS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SchedulingReviewSectionType.UNRECOGNIZED;
  }
}

export function schedulingReviewSectionTypeToJSON(object: SchedulingReviewSectionType): string {
  switch (object) {
    case SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_SEMESTERS_GRADES:
      return "SCHEDULING_REVIEW_SECTION_TYPE_SEMESTERS_GRADES";
    case SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_PERIOD_TIMES:
      return "SCHEDULING_REVIEW_SECTION_TYPE_PERIOD_TIMES";
    case SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_CLASSES:
      return "SCHEDULING_REVIEW_SECTION_TYPE_CLASSES";
    case SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_TEACHERS:
      return "SCHEDULING_REVIEW_SECTION_TYPE_TEACHERS";
    case SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_CLASS_ASSIGNMENTS:
      return "SCHEDULING_REVIEW_SECTION_TYPE_CLASS_ASSIGNMENTS";
    case SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_CLASS_GROUPS:
      return "SCHEDULING_REVIEW_SECTION_TYPE_CLASS_GROUPS";
    case SchedulingReviewSectionType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function schedulingReviewSectionTypeToNumber(object: SchedulingReviewSectionType): number {
  switch (object) {
    case SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_SEMESTERS_GRADES:
      return 1;
    case SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_PERIOD_TIMES:
      return 2;
    case SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_CLASSES:
      return 3;
    case SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_TEACHERS:
      return 4;
    case SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_CLASS_ASSIGNMENTS:
      return 5;
    case SchedulingReviewSectionType.SCHEDULING_REVIEW_SECTION_TYPE_CLASS_GROUPS:
      return 6;
    case SchedulingReviewSectionType.UNRECOGNIZED:
    default:
      return -1;
  }
}

/**
 * Where the user must go to resolve a review issue that cannot be fixed inside
 * the scheduling preparation wizard.
 */
export enum SchedulingReviewActionDestination {
  /** SCHEDULING_REVIEW_ACTION_DESTINATION_PREPARATION_STEP - Default: open the preparation step named by action_step. */
  SCHEDULING_REVIEW_ACTION_DESTINATION_PREPARATION_STEP = "SCHEDULING_REVIEW_ACTION_DESTINATION_PREPARATION_STEP",
  /** SCHEDULING_REVIEW_ACTION_DESTINATION_ORGANIZATION_ROOMS - Organization settings -> Rooms (missing/archived rooms or categories). */
  SCHEDULING_REVIEW_ACTION_DESTINATION_ORGANIZATION_ROOMS = "SCHEDULING_REVIEW_ACTION_DESTINATION_ORGANIZATION_ROOMS",
  /** SCHEDULING_REVIEW_ACTION_DESTINATION_ABSTRACT_CATALOG - Classes -> abstract catalog (course room-eligibility problems). */
  SCHEDULING_REVIEW_ACTION_DESTINATION_ABSTRACT_CATALOG = "SCHEDULING_REVIEW_ACTION_DESTINATION_ABSTRACT_CATALOG",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function schedulingReviewActionDestinationFromJSON(object: any): SchedulingReviewActionDestination {
  switch (object) {
    case 0:
    case "SCHEDULING_REVIEW_ACTION_DESTINATION_PREPARATION_STEP":
      return SchedulingReviewActionDestination.SCHEDULING_REVIEW_ACTION_DESTINATION_PREPARATION_STEP;
    case 1:
    case "SCHEDULING_REVIEW_ACTION_DESTINATION_ORGANIZATION_ROOMS":
      return SchedulingReviewActionDestination.SCHEDULING_REVIEW_ACTION_DESTINATION_ORGANIZATION_ROOMS;
    case 2:
    case "SCHEDULING_REVIEW_ACTION_DESTINATION_ABSTRACT_CATALOG":
      return SchedulingReviewActionDestination.SCHEDULING_REVIEW_ACTION_DESTINATION_ABSTRACT_CATALOG;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SchedulingReviewActionDestination.UNRECOGNIZED;
  }
}

export function schedulingReviewActionDestinationToJSON(object: SchedulingReviewActionDestination): string {
  switch (object) {
    case SchedulingReviewActionDestination.SCHEDULING_REVIEW_ACTION_DESTINATION_PREPARATION_STEP:
      return "SCHEDULING_REVIEW_ACTION_DESTINATION_PREPARATION_STEP";
    case SchedulingReviewActionDestination.SCHEDULING_REVIEW_ACTION_DESTINATION_ORGANIZATION_ROOMS:
      return "SCHEDULING_REVIEW_ACTION_DESTINATION_ORGANIZATION_ROOMS";
    case SchedulingReviewActionDestination.SCHEDULING_REVIEW_ACTION_DESTINATION_ABSTRACT_CATALOG:
      return "SCHEDULING_REVIEW_ACTION_DESTINATION_ABSTRACT_CATALOG";
    case SchedulingReviewActionDestination.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function schedulingReviewActionDestinationToNumber(object: SchedulingReviewActionDestination): number {
  switch (object) {
    case SchedulingReviewActionDestination.SCHEDULING_REVIEW_ACTION_DESTINATION_PREPARATION_STEP:
      return 0;
    case SchedulingReviewActionDestination.SCHEDULING_REVIEW_ACTION_DESTINATION_ORGANIZATION_ROOMS:
      return 1;
    case SchedulingReviewActionDestination.SCHEDULING_REVIEW_ACTION_DESTINATION_ABSTRACT_CATALOG:
      return 2;
    case SchedulingReviewActionDestination.UNRECOGNIZED:
    default:
      return -1;
  }
}

export interface SchedulingWorkspace {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  school_year: ObjectId | undefined;
  completed_steps: SchedulingPreparationStep[];
  /**
   * Whether this year's generation enforces room assignments. When false, room
   * setup is advisory: Review reports room problems as warnings and the solver
   * input carries no room data.
   */
  enforce_room_assignments?: boolean | undefined;
}

export interface SchedulingPreparationIssue {
  message?: string | undefined;
}

export interface SchedulingPreparationStepState {
  step?: SchedulingPreparationStep | undefined;
  status?: SchedulingPreparationStepStatus | undefined;
  issues: SchedulingPreparationIssue[];
  completed?: boolean | undefined;
}

export interface SchedulingPreparation {
  workspace?: SchedulingWorkspace | undefined;
  semesters: Semester[];
  step_states: SchedulingPreparationStepState[];
}

export interface SchedulingPeriodDefinition {
  sequence?: number | undefined;
  label?: string | undefined;
  start_time?: string | undefined;
  end_time?: string | undefined;
  is_rest_period?: boolean | undefined;
}

export interface SchedulingOfferedGrade {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  campus_id: ObjectId | undefined;
  grade?: StudentGrade | undefined;
  semester_ids: ObjectId[];
}

export interface SchedulingOfferedGradeSetup {
  campuses: Campus[];
  semesters: Semester[];
  offered_grades: SchedulingOfferedGrade[];
}

export interface SchedulingPeriodTimeSetupTemplate {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  name?: string | undefined;
  periods: SchedulingPeriodDefinition[];
}

export interface SchedulingPeriodTimeSetupWeekdayOverride {
  day?: DayOfWeek | undefined;
  period_time_setup_template: Uuid | undefined;
}

export interface SchedulingSemesterPeriodTimeSetup {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  semester:
    | ObjectId
    | undefined;
  /** Default template for every active weekday without an override. */
  period_time_setup_template: Uuid | undefined;
  weekday_overrides: SchedulingPeriodTimeSetupWeekdayOverride[];
}

export interface SchedulingWeekdayPreviewDay {
  day?: DayOfWeek | undefined;
  is_weekday?: boolean | undefined;
}

export interface SchedulingPeriodTimeSetup {
  templates: SchedulingPeriodTimeSetupTemplate[];
  semester_setups: SchedulingSemesterPeriodTimeSetup[];
  semesters: Semester[];
  weekday_preview: SchedulingWeekdayPreviewDay[];
}

export interface SchedulingTeacherProfile {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  teacher_id: ObjectId | undefined;
  abstract_course_ids: ObjectId[];
  /** When false, semester_ids is ignored and the teacher can teach in any semester. */
  has_semester_restrictions?: boolean | undefined;
  semester_ids: ObjectId[];
  /** When false, availability_windows is ignored and the teacher is available full-time. */
  has_time_availability_restrictions?: boolean | undefined;
  availability_windows: SchedulingTeacherAvailabilityWindow[];
}

export interface SchedulingTeacherAvailabilityWindow {
  day?: DayOfWeek | undefined;
  start_time?: string | undefined;
  end_time?: string | undefined;
}

export interface SchedulingTeacherSetup {
  teachers: TeacherBasic[];
  semesters: Semester[];
  abstract_courses: AbstractCourse[];
  profiles: SchedulingTeacherProfile[];
  offered_grades: StudentGrade[];
}

/** One teacher's exact scheduling-only share of a class's weekly periods. */
export interface SchedulingTeacherPeriodAllocation {
  teacher_id: ObjectId | undefined;
  periods_per_week?: number | undefined;
}

export interface SchedulingHighSchoolCourseSetup {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  abstract_course_id: ObjectId | undefined;
  is_offered?: boolean | undefined;
  semester_option_groups: SchedulingSemesterOptionGroup[];
}

/**
 * One selectable semester option for a high-school course offering. Generation
 * schedules the offering in exactly one semester chosen from `semester_ids`.
 */
export interface SchedulingSemesterOptionGroup {
  /** Candidate semesters for this offering; generation picks one. */
  semester_ids: ObjectId[];
  id:
    | ObjectId
    | undefined;
  /**
   * Mutually exclusive teacher modes: teacher_id fixes one teacher for every
   * period; two or more allocations split the exact weekly total; leaving both
   * empty selects Auto.
   */
  teacher_id?: ObjectId | undefined;
  teacher_period_allocations: SchedulingTeacherPeriodAllocation[];
}

/**
 * A per-day restriction on which period sequences a class may occupy. Rules expand
 * into a class's allowed slots when its instructional requirement is applied.
 */
export interface SchedulingCoursePeriodRule {
  /** Absent day makes this the every-day rule; days with their own rule override it. */
  day?:
    | DayOfWeek
    | undefined;
  /** Allowed period sequences. Empty with a day set means no meetings on that day. */
  allowed_periods: number[];
}

/**
 * How much instruction a subject (at a grade) or high-school course needs per week,
 * plus its double-period and allowed-period rules. This is the curriculum demand the
 * solver must satisfy.
 */
export interface SchedulingInstructionalRequirement {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  abstract_course_id:
    | ObjectId
    | undefined;
  /** Set for a subject-at-grade requirement; absent for a high-school course requirement. */
  grade?: StudentGrade | undefined;
  periods_per_week?: number | undefined;
  double_period_mode?:
    | SchedulingDoublePeriodMode
    | undefined;
  /** Optional per-day allowed-period restrictions; empty means no slot restriction. */
  period_rules: SchedulingCoursePeriodRule[];
}

export interface SchedulingGradeCampusWeeklyCapacity {
  campus_id: ObjectId | undefined;
  weekly_capacity?: number | undefined;
}

export interface SchedulingGradeWeeklyAllocation {
  grade?: StudentGrade | undefined;
  assigned_periods?: number | undefined;
  weekly_capacity?: number | undefined;
  missing_requirement_count?: number | undefined;
  campus_capacities: SchedulingGradeCampusWeeklyCapacity[];
  status?: SchedulingGradeWeeklyAllocationStatus | undefined;
}

/**
 * Full state of the Classes setup step: the curriculum catalog plus its configured
 * offerings and instructional requirements.
 */
export interface SchedulingClassesSetup {
  subjects: AbstractCourse[];
  high_school_courses: AbstractCourse[];
  semesters: Semester[];
  high_school_course_setups: SchedulingHighSchoolCourseSetup[];
  offered_grades: StudentGrade[];
  instructional_requirements: SchedulingInstructionalRequirement[];
  grade_weekly_allocations: SchedulingGradeWeeklyAllocation[];
}

/** A student selectable for a high-school course roster (display fields included). */
export interface SchedulingStudentAssignmentOption {
  student_id: ObjectId | undefined;
  first_name?: string | undefined;
  last_name?: string | undefined;
  grade?: StudentGrade | undefined;
}

/**
 * Read-only summary of the elementary/middle subjects a grade takes, with its student
 * count. These rosters are implied by the grade (no manual student selection).
 */
export interface SchedulingSubjectAssignmentPreview {
  grade?: StudentGrade | undefined;
  subjects: AbstractCourse[];
  student_count?: number | undefined;
  students: SchedulingStudentAssignmentOption[];
}

/** The stored student roster for one high-school course offering. */
export interface SchedulingHighSchoolCourseStudentAssignment {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  abstract_course_id: ObjectId | undefined;
  student_ids: ObjectId[];
  offering_id: ObjectId | undefined;
}

/**
 * Full state of the Class Assignment step: read-only subject previews (elementary/
 * middle) plus the editable high-school course rosters and their catalog.
 */
export interface SchedulingClassAssignmentSetup {
  subject_previews: SchedulingSubjectAssignmentPreview[];
  high_school_courses: AbstractCourse[];
  high_school_course_setups: SchedulingHighSchoolCourseSetup[];
  high_school_course_assignments: SchedulingHighSchoolCourseStudentAssignment[];
  high_school_students: SchedulingStudentAssignmentOption[];
  semesters: Semester[];
}

/**
 * An elementary/middle grade-semester section. The group is the solver's student proxy:
 * its classes cannot collide in the same slot.
 */
export interface SchedulingClassGroup {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  campus_id: ObjectId | undefined;
  grade?: StudentGrade | undefined;
  semester_id:
    | ObjectId
    | undefined;
  /** Human-readable group name, e.g. "Grade 3 - Fall". */
  label?:
    | string
    | undefined;
  /** True when the group's campus/grade is no longer offered; the user removes stale groups. */
  stale?: boolean | undefined;
}

/**
 * The teacher assigned to teach one subject within one class group. Generation must
 * honor this assignment (it fixes the class's teacher).
 */
export interface SchedulingClassGroupTeacherAssignment {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  class_group_id: Uuid | undefined;
  abstract_course_id: ObjectId | undefined;
  teacher_id: ObjectId | undefined;
}

/** One teacher's persisted exact share of a class-group subject's weekly periods. */
export interface SchedulingClassGroupTeacherPeriodAllocation {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  class_group_id: Uuid | undefined;
  abstract_course_id: ObjectId | undefined;
  teacher_id: ObjectId | undefined;
  periods_per_week?: number | undefined;
}

/** One elementary/middle subject taught together across two or more class groups. */
export interface SchedulingSharedLessonMember {
  class_group_id: Uuid | undefined;
  abstract_course_id: ObjectId | undefined;
}

/**
 * The complete shared-lesson aggregate. Teacher modes match class-group subjects:
 * absent teacher_id plus no allocations is Auto; teacher_id selects one teacher;
 * two or more allocations define an exact weekly split.
 */
export interface SchedulingSharedLesson {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  members: SchedulingSharedLessonMember[];
  teacher_id?: ObjectId | undefined;
  teacher_period_allocations: SchedulingTeacherPeriodAllocation[];
}

/**
 * Full state of the Class Groups step: the groups, the subjects each needs, and their
 * standalone or shared teacher rules, plus the UI catalog.
 */
export interface SchedulingClassGroupSetup {
  groups: SchedulingClassGroup[];
  subjects: AbstractCourse[];
  assignments: SchedulingClassGroupTeacherAssignment[];
  teachers: TeacherBasic[];
  teacher_profiles: SchedulingTeacherProfile[];
  semesters: Semester[];
  high_school_courses: AbstractCourse[];
  high_school_course_setups: SchedulingHighSchoolCourseSetup[];
  high_school_course_assignments: SchedulingHighSchoolCourseStudentAssignment[];
  teacher_period_allocations: SchedulingClassGroupTeacherPeriodAllocation[];
  instructional_requirements: SchedulingInstructionalRequirement[];
  shared_lessons: SchedulingSharedLesson[];
}

/** A preflight problem that prevents generation from starting. */
export interface SchedulingGenerationBlocker {
  /** Admin-readable explanation of the problem and how to resolve it. */
  message?:
    | string
    | undefined;
  /** Stable machine-readable identifier for frontend behavior and localization. */
  code?: string | undefined;
}

/**
 * One schedule-generation attempt. A run is bound to the immutable solver-input
 * snapshot it used, so a failed run can always be inspected against its exact input.
 */
export interface SchedulingGenerationRun {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  school_year:
    | ObjectId
    | undefined;
  /** The frozen input this run solved against. */
  solver_input_snapshot_id:
    | Uuid
    | undefined;
  /** Snapshot schema version, copied onto the run for compatibility auditing. */
  schema_version?: number | undefined;
  status?:
    | SchedulingGenerationRunStatus
    | undefined;
  /** RFC3339; set once the run starts executing. */
  started_at?:
    | string
    | undefined;
  /** RFC3339; set on any terminal state. */
  finished_at?:
    | string
    | undefined;
  /** Admin-readable failure summary; present on FAILED runs. */
  error_message?:
    | string
    | undefined;
  /** Preflight blockers; present when preflight failed the run. */
  blockers: SchedulingGenerationBlocker[];
  /** Set on SUCCEEDED runs, referencing the stored generated schedule. */
  generated_schedule_id?: ObjectId | undefined;
  created_by:
    | ObjectId
    | undefined;
  /** RFC3339 creation time; also the sort key for the runs list. */
  created_at?:
    | string
    | undefined;
  /** Admin-readable infeasibility reasons reported by the solver. */
  solver_reasons: string[];
  /**
   * Global generation creates a candidate. Pinned working-schedule regeneration
   * creates a hidden proposal that can be applied as a revision after preview.
   */
  purpose?: SchedulingGenerationPurpose | undefined;
  source_working_revision_id?: Uuid | undefined;
  pin_count?: number | undefined;
}

/** Runs for a workspace, newest first (used for status polling and history). */
export interface SchedulingGenerationRunList {
  runs: SchedulingGenerationRun[];
}

/** One immutable placement produced by a successful generation run. */
export interface SchedulingGeneratedScheduleEntry {
  class_id?: string | undefined;
  class_type?: string | undefined;
  abstract_course_id: ObjectId | undefined;
  grade_group_id?: Uuid | undefined;
  offering_id?: ObjectId | undefined;
  teacher_id: ObjectId | undefined;
  semester_id: ObjectId | undefined;
  day?: DayOfWeek | undefined;
  period_sequence?: number | undefined;
  slot_id?:
    | string
    | undefined;
  /**
   * Room hosting this meeting. Absent for placements generated without room
   * enforcement and for ordinary elementary/middle meetings.
   */
  room_id?:
    | ObjectId
    | undefined;
  /**
   * Stable identity for this recurring weekly placement. The same value is
   * preserved when the placement is copied into working-schedule revisions.
   */
  id?: Uuid | undefined;
}

/** The immutable schedule artifact stored for one successful generation run. */
export interface SchedulingGeneratedSchedule {
  id: ObjectId | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  school_year: ObjectId | undefined;
  generation_run_id: Uuid | undefined;
  solver_input_snapshot_id: Uuid | undefined;
  created_at?:
    | string
    | undefined;
  /** Display metadata may change; placements and provenance remain immutable. */
  name?: string | undefined;
  note?:
    | string
    | undefined;
  /** Canonical hash of sorted class/slot/teacher/room placement tuples. */
  content_hash?: string | undefined;
  updated_by?: ObjectId | undefined;
  updated_at?: string | undefined;
}

/**
 * The one editable pre-publication schedule for a workspace. Phase 1 changes
 * its current revision only through explicit base selection, revalidation, or
 * restore operations.
 */
export interface SchedulingWorkingSchedule {
  id: Uuid | undefined;
  organization: ObjectId | undefined;
  scheduling_workspace: Uuid | undefined;
  school_year: ObjectId | undefined;
  base_generated_schedule_id: ObjectId | undefined;
  validation_snapshot_id: Uuid | undefined;
  current_revision_id: Uuid | undefined;
  version?: number | undefined;
  created_by: ObjectId | undefined;
  created_at?: string | undefined;
  updated_by: ObjectId | undefined;
  updated_at?: string | undefined;
}

export interface SchedulingScheduleRevision {
  id: Uuid | undefined;
  working_schedule_id: Uuid | undefined;
  base_generated_schedule_id: ObjectId | undefined;
  validation_snapshot_id: Uuid | undefined;
  kind?: SchedulingScheduleRevisionKind | undefined;
  sequence?: number | undefined;
  summary?: string | undefined;
  label?: string | undefined;
  created_by: ObjectId | undefined;
  created_at?: string | undefined;
  copied_from_revision_id?: Uuid | undefined;
  undone_revision_id?: Uuid | undefined;
}

export interface SchedulingScheduleReference {
  generated_schedule_id?: ObjectId | undefined;
  working_revision_id?: Uuid | undefined;
}

export interface SchedulingScheduleSource {
  reference: SchedulingScheduleReference | undefined;
  generated_schedule?: SchedulingGeneratedSchedule | undefined;
  working_schedule?: SchedulingWorkingSchedule | undefined;
  working_revision?: SchedulingScheduleRevision | undefined;
}

export interface SchedulingGeneratedScheduleSummary {
  schedule: SchedulingGeneratedSchedule | undefined;
  input_hash?: string | undefined;
  setup_compatibility?: SchedulingScheduleSetupCompatibility | undefined;
  is_working_base?: boolean | undefined;
  placement_count?:
    | number
    | undefined;
  /** Present when an older result has the same input and placement hashes. */
  duplicate_of_schedule_id?: ObjectId | undefined;
}

export interface SchedulingGeneratedSchedulePage {
  schedules: SchedulingGeneratedScheduleSummary[];
  next_cursor?: string | undefined;
}

export interface SchedulingScheduleRevisionPage {
  revisions: SchedulingScheduleRevision[];
  next_cursor?: string | undefined;
}

export interface SchedulingScheduleCompatibilityIssue {
  code?: string | undefined;
  message?: string | undefined;
}

/** Display companions assembled from the run's frozen snapshot at read time. */
export interface SchedulingScheduleSubjectMemberInfo {
  class_group_id: Uuid | undefined;
  abstract_course_id: ObjectId | undefined;
  group_label?: string | undefined;
  subject_name?: string | undefined;
  color?: string | undefined;
  grade?: StudentGrade | undefined;
}

export interface SchedulingScheduleClassInfo {
  class_id?: string | undefined;
  name?: string | undefined;
  class_type?: string | undefined;
  group_label?: string | undefined;
  periods_per_week?: number | undefined;
  student_ids: ObjectId[];
  color?: string | undefined;
  subject_members: SchedulingScheduleSubjectMemberInfo[];
  shared_lesson_id?: Uuid | undefined;
}

export interface SchedulingScheduleTeacherInfo {
  id: ObjectId | undefined;
  display_name?: string | undefined;
}

export interface SchedulingScheduleSlotInfo {
  slot_id?: string | undefined;
  semester_id: ObjectId | undefined;
  day?: DayOfWeek | undefined;
  period_sequence?: number | undefined;
  period_label?: string | undefined;
  start_time?: string | undefined;
  end_time?: string | undefined;
  is_rest_period?: boolean | undefined;
}

export interface SchedulingScheduleSemesterInfo {
  id: ObjectId | undefined;
  name?: string | undefined;
  campus_id?: ObjectId | undefined;
  campus_name?: string | undefined;
}

export interface SchedulingScheduleStudentInfo {
  id: ObjectId | undefined;
  display_name?: string | undefined;
  grade?: StudentGrade | undefined;
}

/** Frozen room display data from the run's snapshot, never live room records. */
export interface SchedulingScheduleRoomInfo {
  id: ObjectId | undefined;
  name?: string | undefined;
  campus_id:
    | ObjectId
    | undefined;
  /** Frozen category name when the room was a special room at snapshot time. */
  category_name?: string | undefined;
}

export interface SchedulingScheduleSectionInfo {
  id: Uuid | undefined;
  campus_id: ObjectId | undefined;
  grade?: StudentGrade | undefined;
  semester_id: ObjectId | undefined;
  label?: string | undefined;
  campus_name?: string | undefined;
}

/** Latest generated schedule plus the frozen display context needed to render it. */
export interface SchedulingGeneratedScheduleView {
  schedule: SchedulingGeneratedSchedule | undefined;
  entries: SchedulingGeneratedScheduleEntry[];
  classes: SchedulingScheduleClassInfo[];
  teachers: SchedulingScheduleTeacherInfo[];
  slots: SchedulingScheduleSlotInfo[];
  semesters: SchedulingScheduleSemesterInfo[];
  sections: SchedulingScheduleSectionInfo[];
  students: SchedulingScheduleStudentInfo[];
  rooms: SchedulingScheduleRoomInfo[];
  /** Immutable room-enforcement setting captured when this schedule was generated. */
  room_assignments_enforced?: boolean | undefined;
}

/**
 * Common explicit schedule view used by both generated history and the current
 * working revision. Display companions always come from the immutable snapshot
 * referenced by the source.
 */
export interface SchedulingScheduleView {
  source: SchedulingScheduleSource | undefined;
  entries: SchedulingGeneratedScheduleEntry[];
  classes: SchedulingScheduleClassInfo[];
  teachers: SchedulingScheduleTeacherInfo[];
  slots: SchedulingScheduleSlotInfo[];
  semesters: SchedulingScheduleSemesterInfo[];
  sections: SchedulingScheduleSectionInfo[];
  students: SchedulingScheduleStudentInfo[];
  rooms: SchedulingScheduleRoomInfo[];
  room_assignments_enforced?: boolean | undefined;
  setup_compatibility?: SchedulingScheduleSetupCompatibility | undefined;
}

/** Empty when no working schedule has been selected yet. */
export interface SchedulingWorkingScheduleState {
  working_schedule?: SchedulingWorkingSchedule | undefined;
  current_revision?: SchedulingScheduleRevision | undefined;
  view?: SchedulingScheduleView | undefined;
  can_undo?: boolean | undefined;
  can_redo?: boolean | undefined;
}

/**
 * Selection, revalidation, and restore are expected compatibility operations:
 * an unapplied result carries guided issues without partially changing state.
 */
export interface SchedulingWorkingScheduleMutationResult {
  applied?: boolean | undefined;
  state?: SchedulingWorkingScheduleState | undefined;
  issues: SchedulingScheduleCompatibilityIssue[];
}

export interface SchedulingScheduleSwap {
  /** Swaps always exchange the two individually selected scheduled periods. */
  placement_id: Uuid | undefined;
  target_placement_id: Uuid | undefined;
}

export interface SchedulingScheduleAdjustmentOperation {
  swap?: SchedulingScheduleSwap | undefined;
}

export interface SchedulingScheduleAdjustmentIssue {
  code?: string | undefined;
  message?: string | undefined;
  severity?: SchedulingScheduleAdjustmentIssueSeverity | undefined;
  placement_ids: Uuid[];
  resource_ids: string[];
}

export interface SchedulingScheduleCellChange {
  placement_id: Uuid | undefined;
  class_id?: string | undefined;
  from_slot_id?: string | undefined;
  to_slot_id?: string | undefined;
}

export interface SchedulingScheduleAdjustmentImpact {
  cell_changes: SchedulingScheduleCellChange[];
  class_ids: string[];
  section_ids: Uuid[];
  teacher_ids: ObjectId[];
  student_ids: ObjectId[];
  room_ids: ObjectId[];
}

export interface SchedulingScheduleAdjustmentOption {
  kind?: SchedulingScheduleAdjustmentKind | undefined;
  target_placement_id?: Uuid | undefined;
  valid?: boolean | undefined;
  primary_issue?: SchedulingScheduleAdjustmentIssue | undefined;
}

export interface SchedulingScheduleAdjustmentOptions {
  source_placement_ids: Uuid[];
  options: SchedulingScheduleAdjustmentOption[];
}

export interface SchedulingScheduleAdjustmentPreview {
  valid?: boolean | undefined;
  current_revision_id: Uuid | undefined;
  proposed_entries: SchedulingGeneratedScheduleEntry[];
  issues: SchedulingScheduleAdjustmentIssue[];
  impact: SchedulingScheduleAdjustmentImpact | undefined;
}

export interface SchedulingScheduleAdjustmentMutationResult {
  applied?: boolean | undefined;
  state?: SchedulingWorkingScheduleState | undefined;
  issues: SchedulingScheduleAdjustmentIssue[];
  impact?: SchedulingScheduleAdjustmentImpact | undefined;
}

export interface SchedulingSchedulePin {
  id: Uuid | undefined;
  pin_group_id: Uuid | undefined;
  placement_id: Uuid | undefined;
  class_id?: string | undefined;
  slot_id?: string | undefined;
  teacher_id: ObjectId | undefined;
  room_id?: ObjectId | undefined;
}

export interface SchedulingSchedulePinGroup {
  id: Uuid | undefined;
  working_schedule_id: Uuid | undefined;
  revision_id: Uuid | undefined;
  scope?: SchedulingSchedulePinScope | undefined;
  label?: string | undefined;
  pins: SchedulingSchedulePin[];
  created_by: ObjectId | undefined;
  created_at?: string | undefined;
}

export interface SchedulingSchedulePinGroupList {
  groups: SchedulingSchedulePinGroup[];
  current_revision_id: Uuid | undefined;
}

export interface SchedulingPinnedRegenerationProposal {
  run: SchedulingGenerationRun | undefined;
  proposed_view?: SchedulingScheduleView | undefined;
  issues: SchedulingScheduleAdjustmentIssue[];
}

/** A headline count shown on a review section (e.g. "Teachers", 12). */
export interface SchedulingReviewMetric {
  label?: string | undefined;
  value?: number | undefined;
}

/** A single readiness problem, with the step the user should open to resolve it. */
export interface SchedulingReviewIssue {
  action_step?: SchedulingPreparationStep | undefined;
  message?:
    | string
    | undefined;
  /** Stable machine-readable identifier for frontend behavior and localization. */
  code?:
    | string
    | undefined;
  /** Absent means PREPARATION_STEP (resolve via action_step). */
  destination?:
    | SchedulingReviewActionDestination
    | undefined;
  /**
   * True when the issue is advisory only (room enforcement disabled) and must
   * not block Review completion or generation.
   */
  warning_only?: boolean | undefined;
}

/** One setup area's review: its summary metrics and any outstanding issues. */
export interface SchedulingReviewSection {
  section?: SchedulingReviewSectionType | undefined;
  metrics: SchedulingReviewMetric[];
  issues: SchedulingReviewIssue[];
}

/**
 * Whole-workspace readiness. `ready` is true only when no section or cross-step issue
 * remains; generation should be gated on it.
 */
export interface SchedulingReview {
  sections: SchedulingReviewSection[];
  /** Issues spanning multiple steps (not attributable to a single section). */
  cross_step_issues: SchedulingReviewIssue[];
  ready?: boolean | undefined;
}

function createBaseSchedulingWorkspace(): SchedulingWorkspace {
  return {
    id: undefined,
    organization: undefined,
    school_year: undefined,
    completed_steps: [],
    enforce_room_assignments: undefined,
  };
}

export const SchedulingWorkspace: MessageFns<SchedulingWorkspace> = {
  encode(message: SchedulingWorkspace, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.school_year !== undefined) {
      ObjectId.encode(message.school_year, writer.uint32(26).fork()).join();
    }
    writer.uint32(34).fork();
    for (const v of message.completed_steps) {
      writer.int32(schedulingPreparationStepToNumber(v));
    }
    writer.join();
    if (message.enforce_room_assignments !== undefined) {
      writer.uint32(40).bool(message.enforce_room_assignments);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingWorkspace {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingWorkspace();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.school_year = ObjectId.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag === 32) {
            message.completed_steps.push(schedulingPreparationStepFromJSON(reader.int32()));

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.completed_steps.push(schedulingPreparationStepFromJSON(reader.int32()));
            }

            continue;
          }

          break;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.enforce_room_assignments = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingWorkspace {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      school_year: isSet(object.schoolYear) ? ObjectId.fromJSON(object.schoolYear) : undefined,
      completed_steps: globalThis.Array.isArray(object?.completedSteps)
        ? object.completedSteps.map((e: any) => schedulingPreparationStepFromJSON(e))
        : [],
      enforce_room_assignments: isSet(object.enforceRoomAssignments)
        ? globalThis.Boolean(object.enforceRoomAssignments)
        : undefined,
    };
  },

  toJSON(message: SchedulingWorkspace): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.school_year !== undefined) {
      obj.schoolYear = ObjectId.toJSON(message.school_year);
    }
    if (message.completed_steps?.length) {
      obj.completedSteps = message.completed_steps.map((e) => schedulingPreparationStepToJSON(e));
    }
    if (message.enforce_room_assignments !== undefined) {
      obj.enforceRoomAssignments = message.enforce_room_assignments;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingWorkspace>, I>>(base?: I): SchedulingWorkspace {
    return SchedulingWorkspace.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingWorkspace>, I>>(object: I): SchedulingWorkspace {
    const message = createBaseSchedulingWorkspace();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.school_year = (object.school_year !== undefined && object.school_year !== null)
      ? ObjectId.fromPartial(object.school_year)
      : undefined;
    message.completed_steps = object.completed_steps?.map((e) => e) || [];
    message.enforce_room_assignments = object.enforce_room_assignments ?? undefined;
    return message;
  },
};

function createBaseSchedulingPreparationIssue(): SchedulingPreparationIssue {
  return { message: undefined };
}

export const SchedulingPreparationIssue: MessageFns<SchedulingPreparationIssue> = {
  encode(message: SchedulingPreparationIssue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== undefined) {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingPreparationIssue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingPreparationIssue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingPreparationIssue {
    return { message: isSet(object.message) ? globalThis.String(object.message) : undefined };
  },

  toJSON(message: SchedulingPreparationIssue): unknown {
    const obj: any = {};
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingPreparationIssue>, I>>(base?: I): SchedulingPreparationIssue {
    return SchedulingPreparationIssue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingPreparationIssue>, I>>(object: I): SchedulingPreparationIssue {
    const message = createBaseSchedulingPreparationIssue();
    message.message = object.message ?? undefined;
    return message;
  },
};

function createBaseSchedulingPreparationStepState(): SchedulingPreparationStepState {
  return { step: undefined, status: undefined, issues: [], completed: undefined };
}

export const SchedulingPreparationStepState: MessageFns<SchedulingPreparationStepState> = {
  encode(message: SchedulingPreparationStepState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.step !== undefined) {
      writer.uint32(8).int32(schedulingPreparationStepToNumber(message.step));
    }
    if (message.status !== undefined) {
      writer.uint32(16).int32(schedulingPreparationStepStatusToNumber(message.status));
    }
    for (const v of message.issues) {
      SchedulingPreparationIssue.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.completed !== undefined) {
      writer.uint32(32).bool(message.completed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingPreparationStepState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingPreparationStepState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.step = schedulingPreparationStepFromJSON(reader.int32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.status = schedulingPreparationStepStatusFromJSON(reader.int32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.issues.push(SchedulingPreparationIssue.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.completed = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingPreparationStepState {
    return {
      step: isSet(object.step) ? schedulingPreparationStepFromJSON(object.step) : undefined,
      status: isSet(object.status) ? schedulingPreparationStepStatusFromJSON(object.status) : undefined,
      issues: globalThis.Array.isArray(object?.issues)
        ? object.issues.map((e: any) => SchedulingPreparationIssue.fromJSON(e))
        : [],
      completed: isSet(object.completed) ? globalThis.Boolean(object.completed) : undefined,
    };
  },

  toJSON(message: SchedulingPreparationStepState): unknown {
    const obj: any = {};
    if (message.step !== undefined) {
      obj.step = schedulingPreparationStepToJSON(message.step);
    }
    if (message.status !== undefined) {
      obj.status = schedulingPreparationStepStatusToJSON(message.status);
    }
    if (message.issues?.length) {
      obj.issues = message.issues.map((e) => SchedulingPreparationIssue.toJSON(e));
    }
    if (message.completed !== undefined) {
      obj.completed = message.completed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingPreparationStepState>, I>>(base?: I): SchedulingPreparationStepState {
    return SchedulingPreparationStepState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingPreparationStepState>, I>>(
    object: I,
  ): SchedulingPreparationStepState {
    const message = createBaseSchedulingPreparationStepState();
    message.step = object.step ?? undefined;
    message.status = object.status ?? undefined;
    message.issues = object.issues?.map((e) => SchedulingPreparationIssue.fromPartial(e)) || [];
    message.completed = object.completed ?? undefined;
    return message;
  },
};

function createBaseSchedulingPreparation(): SchedulingPreparation {
  return { workspace: undefined, semesters: [], step_states: [] };
}

export const SchedulingPreparation: MessageFns<SchedulingPreparation> = {
  encode(message: SchedulingPreparation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspace !== undefined) {
      SchedulingWorkspace.encode(message.workspace, writer.uint32(10).fork()).join();
    }
    for (const v of message.semesters) {
      Semester.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.step_states) {
      SchedulingPreparationStepState.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingPreparation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingPreparation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.workspace = SchedulingWorkspace.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.semesters.push(Semester.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.step_states.push(SchedulingPreparationStepState.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingPreparation {
    return {
      workspace: isSet(object.workspace) ? SchedulingWorkspace.fromJSON(object.workspace) : undefined,
      semesters: globalThis.Array.isArray(object?.semesters)
        ? object.semesters.map((e: any) => Semester.fromJSON(e))
        : [],
      step_states: globalThis.Array.isArray(object?.stepStates)
        ? object.stepStates.map((e: any) => SchedulingPreparationStepState.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingPreparation): unknown {
    const obj: any = {};
    if (message.workspace !== undefined) {
      obj.workspace = SchedulingWorkspace.toJSON(message.workspace);
    }
    if (message.semesters?.length) {
      obj.semesters = message.semesters.map((e) => Semester.toJSON(e));
    }
    if (message.step_states?.length) {
      obj.stepStates = message.step_states.map((e) => SchedulingPreparationStepState.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingPreparation>, I>>(base?: I): SchedulingPreparation {
    return SchedulingPreparation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingPreparation>, I>>(object: I): SchedulingPreparation {
    const message = createBaseSchedulingPreparation();
    message.workspace = (object.workspace !== undefined && object.workspace !== null)
      ? SchedulingWorkspace.fromPartial(object.workspace)
      : undefined;
    message.semesters = object.semesters?.map((e) => Semester.fromPartial(e)) || [];
    message.step_states = object.step_states?.map((e) => SchedulingPreparationStepState.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingPeriodDefinition(): SchedulingPeriodDefinition {
  return {
    sequence: undefined,
    label: undefined,
    start_time: undefined,
    end_time: undefined,
    is_rest_period: undefined,
  };
}

export const SchedulingPeriodDefinition: MessageFns<SchedulingPeriodDefinition> = {
  encode(message: SchedulingPeriodDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sequence !== undefined) {
      writer.uint32(8).int32(message.sequence);
    }
    if (message.label !== undefined) {
      writer.uint32(18).string(message.label);
    }
    if (message.start_time !== undefined) {
      writer.uint32(26).string(message.start_time);
    }
    if (message.end_time !== undefined) {
      writer.uint32(34).string(message.end_time);
    }
    if (message.is_rest_period !== undefined) {
      writer.uint32(40).bool(message.is_rest_period);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingPeriodDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingPeriodDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.sequence = reader.int32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.label = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.start_time = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.end_time = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.is_rest_period = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingPeriodDefinition {
    return {
      sequence: isSet(object.sequence) ? globalThis.Number(object.sequence) : undefined,
      label: isSet(object.label) ? globalThis.String(object.label) : undefined,
      start_time: isSet(object.startTime) ? globalThis.String(object.startTime) : undefined,
      end_time: isSet(object.endTime) ? globalThis.String(object.endTime) : undefined,
      is_rest_period: isSet(object.isRestPeriod) ? globalThis.Boolean(object.isRestPeriod) : undefined,
    };
  },

  toJSON(message: SchedulingPeriodDefinition): unknown {
    const obj: any = {};
    if (message.sequence !== undefined) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.label !== undefined) {
      obj.label = message.label;
    }
    if (message.start_time !== undefined) {
      obj.startTime = message.start_time;
    }
    if (message.end_time !== undefined) {
      obj.endTime = message.end_time;
    }
    if (message.is_rest_period !== undefined) {
      obj.isRestPeriod = message.is_rest_period;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingPeriodDefinition>, I>>(base?: I): SchedulingPeriodDefinition {
    return SchedulingPeriodDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingPeriodDefinition>, I>>(object: I): SchedulingPeriodDefinition {
    const message = createBaseSchedulingPeriodDefinition();
    message.sequence = object.sequence ?? undefined;
    message.label = object.label ?? undefined;
    message.start_time = object.start_time ?? undefined;
    message.end_time = object.end_time ?? undefined;
    message.is_rest_period = object.is_rest_period ?? undefined;
    return message;
  },
};

function createBaseSchedulingOfferedGrade(): SchedulingOfferedGrade {
  return {
    id: undefined,
    organization: undefined,
    scheduling_workspace: undefined,
    campus_id: undefined,
    grade: undefined,
    semester_ids: [],
  };
}

export const SchedulingOfferedGrade: MessageFns<SchedulingOfferedGrade> = {
  encode(message: SchedulingOfferedGrade, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    if (message.campus_id !== undefined) {
      ObjectId.encode(message.campus_id, writer.uint32(34).fork()).join();
    }
    if (message.grade !== undefined) {
      writer.uint32(40).int32(studentGradeToNumber(message.grade));
    }
    for (const v of message.semester_ids) {
      ObjectId.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingOfferedGrade {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingOfferedGrade();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.campus_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.grade = studentGradeFromJSON(reader.int32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.semester_ids.push(ObjectId.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingOfferedGrade {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      campus_id: isSet(object.campusId) ? ObjectId.fromJSON(object.campusId) : undefined,
      grade: isSet(object.grade) ? studentGradeFromJSON(object.grade) : undefined,
      semester_ids: globalThis.Array.isArray(object?.semesterIds)
        ? object.semesterIds.map((e: any) => ObjectId.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingOfferedGrade): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.campus_id !== undefined) {
      obj.campusId = ObjectId.toJSON(message.campus_id);
    }
    if (message.grade !== undefined) {
      obj.grade = studentGradeToJSON(message.grade);
    }
    if (message.semester_ids?.length) {
      obj.semesterIds = message.semester_ids.map((e) => ObjectId.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingOfferedGrade>, I>>(base?: I): SchedulingOfferedGrade {
    return SchedulingOfferedGrade.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingOfferedGrade>, I>>(object: I): SchedulingOfferedGrade {
    const message = createBaseSchedulingOfferedGrade();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.campus_id = (object.campus_id !== undefined && object.campus_id !== null)
      ? ObjectId.fromPartial(object.campus_id)
      : undefined;
    message.grade = object.grade ?? undefined;
    message.semester_ids = object.semester_ids?.map((e) => ObjectId.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingOfferedGradeSetup(): SchedulingOfferedGradeSetup {
  return { campuses: [], semesters: [], offered_grades: [] };
}

export const SchedulingOfferedGradeSetup: MessageFns<SchedulingOfferedGradeSetup> = {
  encode(message: SchedulingOfferedGradeSetup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.campuses) {
      Campus.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.semesters) {
      Semester.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.offered_grades) {
      SchedulingOfferedGrade.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingOfferedGradeSetup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingOfferedGradeSetup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.campuses.push(Campus.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.semesters.push(Semester.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.offered_grades.push(SchedulingOfferedGrade.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingOfferedGradeSetup {
    return {
      campuses: globalThis.Array.isArray(object?.campuses) ? object.campuses.map((e: any) => Campus.fromJSON(e)) : [],
      semesters: globalThis.Array.isArray(object?.semesters)
        ? object.semesters.map((e: any) => Semester.fromJSON(e))
        : [],
      offered_grades: globalThis.Array.isArray(object?.offeredGrades)
        ? object.offeredGrades.map((e: any) => SchedulingOfferedGrade.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingOfferedGradeSetup): unknown {
    const obj: any = {};
    if (message.campuses?.length) {
      obj.campuses = message.campuses.map((e) => Campus.toJSON(e));
    }
    if (message.semesters?.length) {
      obj.semesters = message.semesters.map((e) => Semester.toJSON(e));
    }
    if (message.offered_grades?.length) {
      obj.offeredGrades = message.offered_grades.map((e) => SchedulingOfferedGrade.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingOfferedGradeSetup>, I>>(base?: I): SchedulingOfferedGradeSetup {
    return SchedulingOfferedGradeSetup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingOfferedGradeSetup>, I>>(object: I): SchedulingOfferedGradeSetup {
    const message = createBaseSchedulingOfferedGradeSetup();
    message.campuses = object.campuses?.map((e) => Campus.fromPartial(e)) || [];
    message.semesters = object.semesters?.map((e) => Semester.fromPartial(e)) || [];
    message.offered_grades = object.offered_grades?.map((e) => SchedulingOfferedGrade.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingPeriodTimeSetupTemplate(): SchedulingPeriodTimeSetupTemplate {
  return { id: undefined, organization: undefined, scheduling_workspace: undefined, name: undefined, periods: [] };
}

export const SchedulingPeriodTimeSetupTemplate: MessageFns<SchedulingPeriodTimeSetupTemplate> = {
  encode(message: SchedulingPeriodTimeSetupTemplate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    if (message.name !== undefined) {
      writer.uint32(34).string(message.name);
    }
    for (const v of message.periods) {
      SchedulingPeriodDefinition.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingPeriodTimeSetupTemplate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingPeriodTimeSetupTemplate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.name = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.periods.push(SchedulingPeriodDefinition.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingPeriodTimeSetupTemplate {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      periods: globalThis.Array.isArray(object?.periods)
        ? object.periods.map((e: any) => SchedulingPeriodDefinition.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingPeriodTimeSetupTemplate): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.periods?.length) {
      obj.periods = message.periods.map((e) => SchedulingPeriodDefinition.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingPeriodTimeSetupTemplate>, I>>(
    base?: I,
  ): SchedulingPeriodTimeSetupTemplate {
    return SchedulingPeriodTimeSetupTemplate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingPeriodTimeSetupTemplate>, I>>(
    object: I,
  ): SchedulingPeriodTimeSetupTemplate {
    const message = createBaseSchedulingPeriodTimeSetupTemplate();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.name = object.name ?? undefined;
    message.periods = object.periods?.map((e) => SchedulingPeriodDefinition.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingPeriodTimeSetupWeekdayOverride(): SchedulingPeriodTimeSetupWeekdayOverride {
  return { day: undefined, period_time_setup_template: undefined };
}

export const SchedulingPeriodTimeSetupWeekdayOverride: MessageFns<SchedulingPeriodTimeSetupWeekdayOverride> = {
  encode(message: SchedulingPeriodTimeSetupWeekdayOverride, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.day !== undefined) {
      writer.uint32(8).int32(dayOfWeekToNumber(message.day));
    }
    if (message.period_time_setup_template !== undefined) {
      Uuid.encode(message.period_time_setup_template, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingPeriodTimeSetupWeekdayOverride {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingPeriodTimeSetupWeekdayOverride();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.day = dayOfWeekFromJSON(reader.int32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.period_time_setup_template = Uuid.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingPeriodTimeSetupWeekdayOverride {
    return {
      day: isSet(object.day) ? dayOfWeekFromJSON(object.day) : undefined,
      period_time_setup_template: isSet(object.periodTimeSetupTemplate)
        ? Uuid.fromJSON(object.periodTimeSetupTemplate)
        : undefined,
    };
  },

  toJSON(message: SchedulingPeriodTimeSetupWeekdayOverride): unknown {
    const obj: any = {};
    if (message.day !== undefined) {
      obj.day = dayOfWeekToJSON(message.day);
    }
    if (message.period_time_setup_template !== undefined) {
      obj.periodTimeSetupTemplate = Uuid.toJSON(message.period_time_setup_template);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingPeriodTimeSetupWeekdayOverride>, I>>(
    base?: I,
  ): SchedulingPeriodTimeSetupWeekdayOverride {
    return SchedulingPeriodTimeSetupWeekdayOverride.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingPeriodTimeSetupWeekdayOverride>, I>>(
    object: I,
  ): SchedulingPeriodTimeSetupWeekdayOverride {
    const message = createBaseSchedulingPeriodTimeSetupWeekdayOverride();
    message.day = object.day ?? undefined;
    message.period_time_setup_template =
      (object.period_time_setup_template !== undefined && object.period_time_setup_template !== null)
        ? Uuid.fromPartial(object.period_time_setup_template)
        : undefined;
    return message;
  },
};

function createBaseSchedulingSemesterPeriodTimeSetup(): SchedulingSemesterPeriodTimeSetup {
  return {
    id: undefined,
    organization: undefined,
    scheduling_workspace: undefined,
    semester: undefined,
    period_time_setup_template: undefined,
    weekday_overrides: [],
  };
}

export const SchedulingSemesterPeriodTimeSetup: MessageFns<SchedulingSemesterPeriodTimeSetup> = {
  encode(message: SchedulingSemesterPeriodTimeSetup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    if (message.semester !== undefined) {
      ObjectId.encode(message.semester, writer.uint32(34).fork()).join();
    }
    if (message.period_time_setup_template !== undefined) {
      Uuid.encode(message.period_time_setup_template, writer.uint32(42).fork()).join();
    }
    for (const v of message.weekday_overrides) {
      SchedulingPeriodTimeSetupWeekdayOverride.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingSemesterPeriodTimeSetup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingSemesterPeriodTimeSetup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.semester = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.period_time_setup_template = Uuid.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.weekday_overrides.push(SchedulingPeriodTimeSetupWeekdayOverride.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingSemesterPeriodTimeSetup {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      semester: isSet(object.semester) ? ObjectId.fromJSON(object.semester) : undefined,
      period_time_setup_template: isSet(object.periodTimeSetupTemplate)
        ? Uuid.fromJSON(object.periodTimeSetupTemplate)
        : undefined,
      weekday_overrides: globalThis.Array.isArray(object?.weekdayOverrides)
        ? object.weekdayOverrides.map((e: any) => SchedulingPeriodTimeSetupWeekdayOverride.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingSemesterPeriodTimeSetup): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.semester !== undefined) {
      obj.semester = ObjectId.toJSON(message.semester);
    }
    if (message.period_time_setup_template !== undefined) {
      obj.periodTimeSetupTemplate = Uuid.toJSON(message.period_time_setup_template);
    }
    if (message.weekday_overrides?.length) {
      obj.weekdayOverrides = message.weekday_overrides.map((e) => SchedulingPeriodTimeSetupWeekdayOverride.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingSemesterPeriodTimeSetup>, I>>(
    base?: I,
  ): SchedulingSemesterPeriodTimeSetup {
    return SchedulingSemesterPeriodTimeSetup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingSemesterPeriodTimeSetup>, I>>(
    object: I,
  ): SchedulingSemesterPeriodTimeSetup {
    const message = createBaseSchedulingSemesterPeriodTimeSetup();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.semester = (object.semester !== undefined && object.semester !== null)
      ? ObjectId.fromPartial(object.semester)
      : undefined;
    message.period_time_setup_template =
      (object.period_time_setup_template !== undefined && object.period_time_setup_template !== null)
        ? Uuid.fromPartial(object.period_time_setup_template)
        : undefined;
    message.weekday_overrides =
      object.weekday_overrides?.map((e) => SchedulingPeriodTimeSetupWeekdayOverride.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingWeekdayPreviewDay(): SchedulingWeekdayPreviewDay {
  return { day: undefined, is_weekday: undefined };
}

export const SchedulingWeekdayPreviewDay: MessageFns<SchedulingWeekdayPreviewDay> = {
  encode(message: SchedulingWeekdayPreviewDay, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.day !== undefined) {
      writer.uint32(8).int32(dayOfWeekToNumber(message.day));
    }
    if (message.is_weekday !== undefined) {
      writer.uint32(16).bool(message.is_weekday);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingWeekdayPreviewDay {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingWeekdayPreviewDay();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.day = dayOfWeekFromJSON(reader.int32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.is_weekday = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingWeekdayPreviewDay {
    return {
      day: isSet(object.day) ? dayOfWeekFromJSON(object.day) : undefined,
      is_weekday: isSet(object.isWeekday) ? globalThis.Boolean(object.isWeekday) : undefined,
    };
  },

  toJSON(message: SchedulingWeekdayPreviewDay): unknown {
    const obj: any = {};
    if (message.day !== undefined) {
      obj.day = dayOfWeekToJSON(message.day);
    }
    if (message.is_weekday !== undefined) {
      obj.isWeekday = message.is_weekday;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingWeekdayPreviewDay>, I>>(base?: I): SchedulingWeekdayPreviewDay {
    return SchedulingWeekdayPreviewDay.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingWeekdayPreviewDay>, I>>(object: I): SchedulingWeekdayPreviewDay {
    const message = createBaseSchedulingWeekdayPreviewDay();
    message.day = object.day ?? undefined;
    message.is_weekday = object.is_weekday ?? undefined;
    return message;
  },
};

function createBaseSchedulingPeriodTimeSetup(): SchedulingPeriodTimeSetup {
  return { templates: [], semester_setups: [], semesters: [], weekday_preview: [] };
}

export const SchedulingPeriodTimeSetup: MessageFns<SchedulingPeriodTimeSetup> = {
  encode(message: SchedulingPeriodTimeSetup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.templates) {
      SchedulingPeriodTimeSetupTemplate.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.semester_setups) {
      SchedulingSemesterPeriodTimeSetup.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.semesters) {
      Semester.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.weekday_preview) {
      SchedulingWeekdayPreviewDay.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingPeriodTimeSetup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingPeriodTimeSetup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.templates.push(SchedulingPeriodTimeSetupTemplate.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.semester_setups.push(SchedulingSemesterPeriodTimeSetup.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.semesters.push(Semester.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.weekday_preview.push(SchedulingWeekdayPreviewDay.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingPeriodTimeSetup {
    return {
      templates: globalThis.Array.isArray(object?.templates)
        ? object.templates.map((e: any) => SchedulingPeriodTimeSetupTemplate.fromJSON(e))
        : [],
      semester_setups: globalThis.Array.isArray(object?.semesterSetups)
        ? object.semesterSetups.map((e: any) => SchedulingSemesterPeriodTimeSetup.fromJSON(e))
        : [],
      semesters: globalThis.Array.isArray(object?.semesters)
        ? object.semesters.map((e: any) => Semester.fromJSON(e))
        : [],
      weekday_preview: globalThis.Array.isArray(object?.weekdayPreview)
        ? object.weekdayPreview.map((e: any) => SchedulingWeekdayPreviewDay.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingPeriodTimeSetup): unknown {
    const obj: any = {};
    if (message.templates?.length) {
      obj.templates = message.templates.map((e) => SchedulingPeriodTimeSetupTemplate.toJSON(e));
    }
    if (message.semester_setups?.length) {
      obj.semesterSetups = message.semester_setups.map((e) => SchedulingSemesterPeriodTimeSetup.toJSON(e));
    }
    if (message.semesters?.length) {
      obj.semesters = message.semesters.map((e) => Semester.toJSON(e));
    }
    if (message.weekday_preview?.length) {
      obj.weekdayPreview = message.weekday_preview.map((e) => SchedulingWeekdayPreviewDay.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingPeriodTimeSetup>, I>>(base?: I): SchedulingPeriodTimeSetup {
    return SchedulingPeriodTimeSetup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingPeriodTimeSetup>, I>>(object: I): SchedulingPeriodTimeSetup {
    const message = createBaseSchedulingPeriodTimeSetup();
    message.templates = object.templates?.map((e) => SchedulingPeriodTimeSetupTemplate.fromPartial(e)) || [];
    message.semester_setups = object.semester_setups?.map((e) => SchedulingSemesterPeriodTimeSetup.fromPartial(e)) ||
      [];
    message.semesters = object.semesters?.map((e) => Semester.fromPartial(e)) || [];
    message.weekday_preview = object.weekday_preview?.map((e) => SchedulingWeekdayPreviewDay.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingTeacherProfile(): SchedulingTeacherProfile {
  return {
    id: undefined,
    organization: undefined,
    scheduling_workspace: undefined,
    teacher_id: undefined,
    abstract_course_ids: [],
    has_semester_restrictions: undefined,
    semester_ids: [],
    has_time_availability_restrictions: undefined,
    availability_windows: [],
  };
}

export const SchedulingTeacherProfile: MessageFns<SchedulingTeacherProfile> = {
  encode(message: SchedulingTeacherProfile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    if (message.teacher_id !== undefined) {
      ObjectId.encode(message.teacher_id, writer.uint32(34).fork()).join();
    }
    for (const v of message.abstract_course_ids) {
      ObjectId.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.has_semester_restrictions !== undefined) {
      writer.uint32(48).bool(message.has_semester_restrictions);
    }
    for (const v of message.semester_ids) {
      ObjectId.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.has_time_availability_restrictions !== undefined) {
      writer.uint32(64).bool(message.has_time_availability_restrictions);
    }
    for (const v of message.availability_windows) {
      SchedulingTeacherAvailabilityWindow.encode(v!, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingTeacherProfile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingTeacherProfile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.teacher_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.abstract_course_ids.push(ObjectId.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.has_semester_restrictions = reader.bool();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.semester_ids.push(ObjectId.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.has_time_availability_restrictions = reader.bool();
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.availability_windows.push(SchedulingTeacherAvailabilityWindow.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingTeacherProfile {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      teacher_id: isSet(object.teacherId) ? ObjectId.fromJSON(object.teacherId) : undefined,
      abstract_course_ids: globalThis.Array.isArray(object?.abstractCourseIds)
        ? object.abstractCourseIds.map((e: any) => ObjectId.fromJSON(e))
        : [],
      has_semester_restrictions: isSet(object.hasSemesterRestrictions)
        ? globalThis.Boolean(object.hasSemesterRestrictions)
        : undefined,
      semester_ids: globalThis.Array.isArray(object?.semesterIds)
        ? object.semesterIds.map((e: any) => ObjectId.fromJSON(e))
        : [],
      has_time_availability_restrictions: isSet(object.hasTimeAvailabilityRestrictions)
        ? globalThis.Boolean(object.hasTimeAvailabilityRestrictions)
        : undefined,
      availability_windows: globalThis.Array.isArray(object?.availabilityWindows)
        ? object.availabilityWindows.map((e: any) => SchedulingTeacherAvailabilityWindow.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingTeacherProfile): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.teacher_id !== undefined) {
      obj.teacherId = ObjectId.toJSON(message.teacher_id);
    }
    if (message.abstract_course_ids?.length) {
      obj.abstractCourseIds = message.abstract_course_ids.map((e) => ObjectId.toJSON(e));
    }
    if (message.has_semester_restrictions !== undefined) {
      obj.hasSemesterRestrictions = message.has_semester_restrictions;
    }
    if (message.semester_ids?.length) {
      obj.semesterIds = message.semester_ids.map((e) => ObjectId.toJSON(e));
    }
    if (message.has_time_availability_restrictions !== undefined) {
      obj.hasTimeAvailabilityRestrictions = message.has_time_availability_restrictions;
    }
    if (message.availability_windows?.length) {
      obj.availabilityWindows = message.availability_windows.map((e) => SchedulingTeacherAvailabilityWindow.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingTeacherProfile>, I>>(base?: I): SchedulingTeacherProfile {
    return SchedulingTeacherProfile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingTeacherProfile>, I>>(object: I): SchedulingTeacherProfile {
    const message = createBaseSchedulingTeacherProfile();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.teacher_id = (object.teacher_id !== undefined && object.teacher_id !== null)
      ? ObjectId.fromPartial(object.teacher_id)
      : undefined;
    message.abstract_course_ids = object.abstract_course_ids?.map((e) => ObjectId.fromPartial(e)) || [];
    message.has_semester_restrictions = object.has_semester_restrictions ?? undefined;
    message.semester_ids = object.semester_ids?.map((e) => ObjectId.fromPartial(e)) || [];
    message.has_time_availability_restrictions = object.has_time_availability_restrictions ?? undefined;
    message.availability_windows =
      object.availability_windows?.map((e) => SchedulingTeacherAvailabilityWindow.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingTeacherAvailabilityWindow(): SchedulingTeacherAvailabilityWindow {
  return { day: undefined, start_time: undefined, end_time: undefined };
}

export const SchedulingTeacherAvailabilityWindow: MessageFns<SchedulingTeacherAvailabilityWindow> = {
  encode(message: SchedulingTeacherAvailabilityWindow, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.day !== undefined) {
      writer.uint32(8).int32(dayOfWeekToNumber(message.day));
    }
    if (message.start_time !== undefined) {
      writer.uint32(18).string(message.start_time);
    }
    if (message.end_time !== undefined) {
      writer.uint32(26).string(message.end_time);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingTeacherAvailabilityWindow {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingTeacherAvailabilityWindow();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.day = dayOfWeekFromJSON(reader.int32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.start_time = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.end_time = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingTeacherAvailabilityWindow {
    return {
      day: isSet(object.day) ? dayOfWeekFromJSON(object.day) : undefined,
      start_time: isSet(object.startTime) ? globalThis.String(object.startTime) : undefined,
      end_time: isSet(object.endTime) ? globalThis.String(object.endTime) : undefined,
    };
  },

  toJSON(message: SchedulingTeacherAvailabilityWindow): unknown {
    const obj: any = {};
    if (message.day !== undefined) {
      obj.day = dayOfWeekToJSON(message.day);
    }
    if (message.start_time !== undefined) {
      obj.startTime = message.start_time;
    }
    if (message.end_time !== undefined) {
      obj.endTime = message.end_time;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingTeacherAvailabilityWindow>, I>>(
    base?: I,
  ): SchedulingTeacherAvailabilityWindow {
    return SchedulingTeacherAvailabilityWindow.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingTeacherAvailabilityWindow>, I>>(
    object: I,
  ): SchedulingTeacherAvailabilityWindow {
    const message = createBaseSchedulingTeacherAvailabilityWindow();
    message.day = object.day ?? undefined;
    message.start_time = object.start_time ?? undefined;
    message.end_time = object.end_time ?? undefined;
    return message;
  },
};

function createBaseSchedulingTeacherSetup(): SchedulingTeacherSetup {
  return { teachers: [], semesters: [], abstract_courses: [], profiles: [], offered_grades: [] };
}

export const SchedulingTeacherSetup: MessageFns<SchedulingTeacherSetup> = {
  encode(message: SchedulingTeacherSetup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.teachers) {
      TeacherBasic.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.semesters) {
      Semester.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.abstract_courses) {
      AbstractCourse.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.profiles) {
      SchedulingTeacherProfile.encode(v!, writer.uint32(34).fork()).join();
    }
    writer.uint32(42).fork();
    for (const v of message.offered_grades) {
      writer.int32(studentGradeToNumber(v));
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingTeacherSetup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingTeacherSetup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.teachers.push(TeacherBasic.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.semesters.push(Semester.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.abstract_courses.push(AbstractCourse.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.profiles.push(SchedulingTeacherProfile.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag === 40) {
            message.offered_grades.push(studentGradeFromJSON(reader.int32()));

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.offered_grades.push(studentGradeFromJSON(reader.int32()));
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingTeacherSetup {
    return {
      teachers: globalThis.Array.isArray(object?.teachers)
        ? object.teachers.map((e: any) => TeacherBasic.fromJSON(e))
        : [],
      semesters: globalThis.Array.isArray(object?.semesters)
        ? object.semesters.map((e: any) => Semester.fromJSON(e))
        : [],
      abstract_courses: globalThis.Array.isArray(object?.abstractCourses)
        ? object.abstractCourses.map((e: any) => AbstractCourse.fromJSON(e))
        : [],
      profiles: globalThis.Array.isArray(object?.profiles)
        ? object.profiles.map((e: any) => SchedulingTeacherProfile.fromJSON(e))
        : [],
      offered_grades: globalThis.Array.isArray(object?.offeredGrades)
        ? object.offeredGrades.map((e: any) => studentGradeFromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingTeacherSetup): unknown {
    const obj: any = {};
    if (message.teachers?.length) {
      obj.teachers = message.teachers.map((e) => TeacherBasic.toJSON(e));
    }
    if (message.semesters?.length) {
      obj.semesters = message.semesters.map((e) => Semester.toJSON(e));
    }
    if (message.abstract_courses?.length) {
      obj.abstractCourses = message.abstract_courses.map((e) => AbstractCourse.toJSON(e));
    }
    if (message.profiles?.length) {
      obj.profiles = message.profiles.map((e) => SchedulingTeacherProfile.toJSON(e));
    }
    if (message.offered_grades?.length) {
      obj.offeredGrades = message.offered_grades.map((e) => studentGradeToJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingTeacherSetup>, I>>(base?: I): SchedulingTeacherSetup {
    return SchedulingTeacherSetup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingTeacherSetup>, I>>(object: I): SchedulingTeacherSetup {
    const message = createBaseSchedulingTeacherSetup();
    message.teachers = object.teachers?.map((e) => TeacherBasic.fromPartial(e)) || [];
    message.semesters = object.semesters?.map((e) => Semester.fromPartial(e)) || [];
    message.abstract_courses = object.abstract_courses?.map((e) => AbstractCourse.fromPartial(e)) || [];
    message.profiles = object.profiles?.map((e) => SchedulingTeacherProfile.fromPartial(e)) || [];
    message.offered_grades = object.offered_grades?.map((e) => e) || [];
    return message;
  },
};

function createBaseSchedulingTeacherPeriodAllocation(): SchedulingTeacherPeriodAllocation {
  return { teacher_id: undefined, periods_per_week: undefined };
}

export const SchedulingTeacherPeriodAllocation: MessageFns<SchedulingTeacherPeriodAllocation> = {
  encode(message: SchedulingTeacherPeriodAllocation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.teacher_id !== undefined) {
      ObjectId.encode(message.teacher_id, writer.uint32(10).fork()).join();
    }
    if (message.periods_per_week !== undefined) {
      writer.uint32(16).uint32(message.periods_per_week);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingTeacherPeriodAllocation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingTeacherPeriodAllocation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.teacher_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.periods_per_week = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingTeacherPeriodAllocation {
    return {
      teacher_id: isSet(object.teacherId) ? ObjectId.fromJSON(object.teacherId) : undefined,
      periods_per_week: isSet(object.periodsPerWeek) ? globalThis.Number(object.periodsPerWeek) : undefined,
    };
  },

  toJSON(message: SchedulingTeacherPeriodAllocation): unknown {
    const obj: any = {};
    if (message.teacher_id !== undefined) {
      obj.teacherId = ObjectId.toJSON(message.teacher_id);
    }
    if (message.periods_per_week !== undefined) {
      obj.periodsPerWeek = Math.round(message.periods_per_week);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingTeacherPeriodAllocation>, I>>(
    base?: I,
  ): SchedulingTeacherPeriodAllocation {
    return SchedulingTeacherPeriodAllocation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingTeacherPeriodAllocation>, I>>(
    object: I,
  ): SchedulingTeacherPeriodAllocation {
    const message = createBaseSchedulingTeacherPeriodAllocation();
    message.teacher_id = (object.teacher_id !== undefined && object.teacher_id !== null)
      ? ObjectId.fromPartial(object.teacher_id)
      : undefined;
    message.periods_per_week = object.periods_per_week ?? undefined;
    return message;
  },
};

function createBaseSchedulingHighSchoolCourseSetup(): SchedulingHighSchoolCourseSetup {
  return {
    id: undefined,
    organization: undefined,
    scheduling_workspace: undefined,
    abstract_course_id: undefined,
    is_offered: undefined,
    semester_option_groups: [],
  };
}

export const SchedulingHighSchoolCourseSetup: MessageFns<SchedulingHighSchoolCourseSetup> = {
  encode(message: SchedulingHighSchoolCourseSetup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    if (message.abstract_course_id !== undefined) {
      ObjectId.encode(message.abstract_course_id, writer.uint32(34).fork()).join();
    }
    if (message.is_offered !== undefined) {
      writer.uint32(40).bool(message.is_offered);
    }
    for (const v of message.semester_option_groups) {
      SchedulingSemesterOptionGroup.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingHighSchoolCourseSetup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingHighSchoolCourseSetup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.abstract_course_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.is_offered = reader.bool();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.semester_option_groups.push(SchedulingSemesterOptionGroup.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingHighSchoolCourseSetup {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      abstract_course_id: isSet(object.abstractCourseId) ? ObjectId.fromJSON(object.abstractCourseId) : undefined,
      is_offered: isSet(object.isOffered) ? globalThis.Boolean(object.isOffered) : undefined,
      semester_option_groups: globalThis.Array.isArray(object?.semesterOptionGroups)
        ? object.semesterOptionGroups.map((e: any) => SchedulingSemesterOptionGroup.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingHighSchoolCourseSetup): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.abstract_course_id !== undefined) {
      obj.abstractCourseId = ObjectId.toJSON(message.abstract_course_id);
    }
    if (message.is_offered !== undefined) {
      obj.isOffered = message.is_offered;
    }
    if (message.semester_option_groups?.length) {
      obj.semesterOptionGroups = message.semester_option_groups.map((e) => SchedulingSemesterOptionGroup.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingHighSchoolCourseSetup>, I>>(base?: I): SchedulingHighSchoolCourseSetup {
    return SchedulingHighSchoolCourseSetup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingHighSchoolCourseSetup>, I>>(
    object: I,
  ): SchedulingHighSchoolCourseSetup {
    const message = createBaseSchedulingHighSchoolCourseSetup();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.abstract_course_id = (object.abstract_course_id !== undefined && object.abstract_course_id !== null)
      ? ObjectId.fromPartial(object.abstract_course_id)
      : undefined;
    message.is_offered = object.is_offered ?? undefined;
    message.semester_option_groups =
      object.semester_option_groups?.map((e) => SchedulingSemesterOptionGroup.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingSemesterOptionGroup(): SchedulingSemesterOptionGroup {
  return { semester_ids: [], id: undefined, teacher_id: undefined, teacher_period_allocations: [] };
}

export const SchedulingSemesterOptionGroup: MessageFns<SchedulingSemesterOptionGroup> = {
  encode(message: SchedulingSemesterOptionGroup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.semester_ids) {
      ObjectId.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.id !== undefined) {
      ObjectId.encode(message.id, writer.uint32(18).fork()).join();
    }
    if (message.teacher_id !== undefined) {
      ObjectId.encode(message.teacher_id, writer.uint32(26).fork()).join();
    }
    for (const v of message.teacher_period_allocations) {
      SchedulingTeacherPeriodAllocation.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingSemesterOptionGroup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingSemesterOptionGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.semester_ids.push(ObjectId.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.teacher_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.teacher_period_allocations.push(SchedulingTeacherPeriodAllocation.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingSemesterOptionGroup {
    return {
      semester_ids: globalThis.Array.isArray(object?.semesterIds)
        ? object.semesterIds.map((e: any) => ObjectId.fromJSON(e))
        : [],
      id: isSet(object.id) ? ObjectId.fromJSON(object.id) : undefined,
      teacher_id: isSet(object.teacherId) ? ObjectId.fromJSON(object.teacherId) : undefined,
      teacher_period_allocations: globalThis.Array.isArray(object?.teacherPeriodAllocations)
        ? object.teacherPeriodAllocations.map((e: any) => SchedulingTeacherPeriodAllocation.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingSemesterOptionGroup): unknown {
    const obj: any = {};
    if (message.semester_ids?.length) {
      obj.semesterIds = message.semester_ids.map((e) => ObjectId.toJSON(e));
    }
    if (message.id !== undefined) {
      obj.id = ObjectId.toJSON(message.id);
    }
    if (message.teacher_id !== undefined) {
      obj.teacherId = ObjectId.toJSON(message.teacher_id);
    }
    if (message.teacher_period_allocations?.length) {
      obj.teacherPeriodAllocations = message.teacher_period_allocations.map((e) =>
        SchedulingTeacherPeriodAllocation.toJSON(e)
      );
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingSemesterOptionGroup>, I>>(base?: I): SchedulingSemesterOptionGroup {
    return SchedulingSemesterOptionGroup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingSemesterOptionGroup>, I>>(
    object: I,
  ): SchedulingSemesterOptionGroup {
    const message = createBaseSchedulingSemesterOptionGroup();
    message.semester_ids = object.semester_ids?.map((e) => ObjectId.fromPartial(e)) || [];
    message.id = (object.id !== undefined && object.id !== null) ? ObjectId.fromPartial(object.id) : undefined;
    message.teacher_id = (object.teacher_id !== undefined && object.teacher_id !== null)
      ? ObjectId.fromPartial(object.teacher_id)
      : undefined;
    message.teacher_period_allocations =
      object.teacher_period_allocations?.map((e) => SchedulingTeacherPeriodAllocation.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingCoursePeriodRule(): SchedulingCoursePeriodRule {
  return { day: undefined, allowed_periods: [] };
}

export const SchedulingCoursePeriodRule: MessageFns<SchedulingCoursePeriodRule> = {
  encode(message: SchedulingCoursePeriodRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.day !== undefined) {
      writer.uint32(8).int32(dayOfWeekToNumber(message.day));
    }
    writer.uint32(18).fork();
    for (const v of message.allowed_periods) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingCoursePeriodRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingCoursePeriodRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.day = dayOfWeekFromJSON(reader.int32());
          continue;
        case 2:
          if (tag === 16) {
            message.allowed_periods.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.allowed_periods.push(reader.int32());
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingCoursePeriodRule {
    return {
      day: isSet(object.day) ? dayOfWeekFromJSON(object.day) : undefined,
      allowed_periods: globalThis.Array.isArray(object?.allowedPeriods)
        ? object.allowedPeriods.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: SchedulingCoursePeriodRule): unknown {
    const obj: any = {};
    if (message.day !== undefined) {
      obj.day = dayOfWeekToJSON(message.day);
    }
    if (message.allowed_periods?.length) {
      obj.allowedPeriods = message.allowed_periods.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingCoursePeriodRule>, I>>(base?: I): SchedulingCoursePeriodRule {
    return SchedulingCoursePeriodRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingCoursePeriodRule>, I>>(object: I): SchedulingCoursePeriodRule {
    const message = createBaseSchedulingCoursePeriodRule();
    message.day = object.day ?? undefined;
    message.allowed_periods = object.allowed_periods?.map((e) => e) || [];
    return message;
  },
};

function createBaseSchedulingInstructionalRequirement(): SchedulingInstructionalRequirement {
  return {
    id: undefined,
    organization: undefined,
    scheduling_workspace: undefined,
    abstract_course_id: undefined,
    grade: undefined,
    periods_per_week: undefined,
    double_period_mode: undefined,
    period_rules: [],
  };
}

export const SchedulingInstructionalRequirement: MessageFns<SchedulingInstructionalRequirement> = {
  encode(message: SchedulingInstructionalRequirement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    if (message.abstract_course_id !== undefined) {
      ObjectId.encode(message.abstract_course_id, writer.uint32(34).fork()).join();
    }
    if (message.grade !== undefined) {
      writer.uint32(40).int32(studentGradeToNumber(message.grade));
    }
    if (message.periods_per_week !== undefined) {
      writer.uint32(48).uint32(message.periods_per_week);
    }
    if (message.double_period_mode !== undefined) {
      writer.uint32(56).int32(schedulingDoublePeriodModeToNumber(message.double_period_mode));
    }
    for (const v of message.period_rules) {
      SchedulingCoursePeriodRule.encode(v!, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingInstructionalRequirement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingInstructionalRequirement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.abstract_course_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.grade = studentGradeFromJSON(reader.int32());
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.periods_per_week = reader.uint32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.double_period_mode = schedulingDoublePeriodModeFromJSON(reader.int32());
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.period_rules.push(SchedulingCoursePeriodRule.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingInstructionalRequirement {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      abstract_course_id: isSet(object.abstractCourseId) ? ObjectId.fromJSON(object.abstractCourseId) : undefined,
      grade: isSet(object.grade) ? studentGradeFromJSON(object.grade) : undefined,
      periods_per_week: isSet(object.periodsPerWeek) ? globalThis.Number(object.periodsPerWeek) : undefined,
      double_period_mode: isSet(object.doublePeriodMode)
        ? schedulingDoublePeriodModeFromJSON(object.doublePeriodMode)
        : undefined,
      period_rules: globalThis.Array.isArray(object?.periodRules)
        ? object.periodRules.map((e: any) => SchedulingCoursePeriodRule.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingInstructionalRequirement): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.abstract_course_id !== undefined) {
      obj.abstractCourseId = ObjectId.toJSON(message.abstract_course_id);
    }
    if (message.grade !== undefined) {
      obj.grade = studentGradeToJSON(message.grade);
    }
    if (message.periods_per_week !== undefined) {
      obj.periodsPerWeek = Math.round(message.periods_per_week);
    }
    if (message.double_period_mode !== undefined) {
      obj.doublePeriodMode = schedulingDoublePeriodModeToJSON(message.double_period_mode);
    }
    if (message.period_rules?.length) {
      obj.periodRules = message.period_rules.map((e) => SchedulingCoursePeriodRule.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingInstructionalRequirement>, I>>(
    base?: I,
  ): SchedulingInstructionalRequirement {
    return SchedulingInstructionalRequirement.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingInstructionalRequirement>, I>>(
    object: I,
  ): SchedulingInstructionalRequirement {
    const message = createBaseSchedulingInstructionalRequirement();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.abstract_course_id = (object.abstract_course_id !== undefined && object.abstract_course_id !== null)
      ? ObjectId.fromPartial(object.abstract_course_id)
      : undefined;
    message.grade = object.grade ?? undefined;
    message.periods_per_week = object.periods_per_week ?? undefined;
    message.double_period_mode = object.double_period_mode ?? undefined;
    message.period_rules = object.period_rules?.map((e) => SchedulingCoursePeriodRule.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingGradeCampusWeeklyCapacity(): SchedulingGradeCampusWeeklyCapacity {
  return { campus_id: undefined, weekly_capacity: undefined };
}

export const SchedulingGradeCampusWeeklyCapacity: MessageFns<SchedulingGradeCampusWeeklyCapacity> = {
  encode(message: SchedulingGradeCampusWeeklyCapacity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.campus_id !== undefined) {
      ObjectId.encode(message.campus_id, writer.uint32(10).fork()).join();
    }
    if (message.weekly_capacity !== undefined) {
      writer.uint32(16).uint32(message.weekly_capacity);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingGradeCampusWeeklyCapacity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingGradeCampusWeeklyCapacity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.campus_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.weekly_capacity = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingGradeCampusWeeklyCapacity {
    return {
      campus_id: isSet(object.campusId) ? ObjectId.fromJSON(object.campusId) : undefined,
      weekly_capacity: isSet(object.weeklyCapacity) ? globalThis.Number(object.weeklyCapacity) : undefined,
    };
  },

  toJSON(message: SchedulingGradeCampusWeeklyCapacity): unknown {
    const obj: any = {};
    if (message.campus_id !== undefined) {
      obj.campusId = ObjectId.toJSON(message.campus_id);
    }
    if (message.weekly_capacity !== undefined) {
      obj.weeklyCapacity = Math.round(message.weekly_capacity);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingGradeCampusWeeklyCapacity>, I>>(
    base?: I,
  ): SchedulingGradeCampusWeeklyCapacity {
    return SchedulingGradeCampusWeeklyCapacity.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingGradeCampusWeeklyCapacity>, I>>(
    object: I,
  ): SchedulingGradeCampusWeeklyCapacity {
    const message = createBaseSchedulingGradeCampusWeeklyCapacity();
    message.campus_id = (object.campus_id !== undefined && object.campus_id !== null)
      ? ObjectId.fromPartial(object.campus_id)
      : undefined;
    message.weekly_capacity = object.weekly_capacity ?? undefined;
    return message;
  },
};

function createBaseSchedulingGradeWeeklyAllocation(): SchedulingGradeWeeklyAllocation {
  return {
    grade: undefined,
    assigned_periods: undefined,
    weekly_capacity: undefined,
    missing_requirement_count: undefined,
    campus_capacities: [],
    status: undefined,
  };
}

export const SchedulingGradeWeeklyAllocation: MessageFns<SchedulingGradeWeeklyAllocation> = {
  encode(message: SchedulingGradeWeeklyAllocation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.grade !== undefined) {
      writer.uint32(8).int32(studentGradeToNumber(message.grade));
    }
    if (message.assigned_periods !== undefined) {
      writer.uint32(16).uint32(message.assigned_periods);
    }
    if (message.weekly_capacity !== undefined) {
      writer.uint32(24).uint32(message.weekly_capacity);
    }
    if (message.missing_requirement_count !== undefined) {
      writer.uint32(32).uint32(message.missing_requirement_count);
    }
    for (const v of message.campus_capacities) {
      SchedulingGradeCampusWeeklyCapacity.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.status !== undefined) {
      writer.uint32(48).int32(schedulingGradeWeeklyAllocationStatusToNumber(message.status));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingGradeWeeklyAllocation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingGradeWeeklyAllocation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.grade = studentGradeFromJSON(reader.int32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.assigned_periods = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.weekly_capacity = reader.uint32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.missing_requirement_count = reader.uint32();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.campus_capacities.push(SchedulingGradeCampusWeeklyCapacity.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.status = schedulingGradeWeeklyAllocationStatusFromJSON(reader.int32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingGradeWeeklyAllocation {
    return {
      grade: isSet(object.grade) ? studentGradeFromJSON(object.grade) : undefined,
      assigned_periods: isSet(object.assignedPeriods) ? globalThis.Number(object.assignedPeriods) : undefined,
      weekly_capacity: isSet(object.weeklyCapacity) ? globalThis.Number(object.weeklyCapacity) : undefined,
      missing_requirement_count: isSet(object.missingRequirementCount)
        ? globalThis.Number(object.missingRequirementCount)
        : undefined,
      campus_capacities: globalThis.Array.isArray(object?.campusCapacities)
        ? object.campusCapacities.map((e: any) => SchedulingGradeCampusWeeklyCapacity.fromJSON(e))
        : [],
      status: isSet(object.status) ? schedulingGradeWeeklyAllocationStatusFromJSON(object.status) : undefined,
    };
  },

  toJSON(message: SchedulingGradeWeeklyAllocation): unknown {
    const obj: any = {};
    if (message.grade !== undefined) {
      obj.grade = studentGradeToJSON(message.grade);
    }
    if (message.assigned_periods !== undefined) {
      obj.assignedPeriods = Math.round(message.assigned_periods);
    }
    if (message.weekly_capacity !== undefined) {
      obj.weeklyCapacity = Math.round(message.weekly_capacity);
    }
    if (message.missing_requirement_count !== undefined) {
      obj.missingRequirementCount = Math.round(message.missing_requirement_count);
    }
    if (message.campus_capacities?.length) {
      obj.campusCapacities = message.campus_capacities.map((e) => SchedulingGradeCampusWeeklyCapacity.toJSON(e));
    }
    if (message.status !== undefined) {
      obj.status = schedulingGradeWeeklyAllocationStatusToJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingGradeWeeklyAllocation>, I>>(base?: I): SchedulingGradeWeeklyAllocation {
    return SchedulingGradeWeeklyAllocation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingGradeWeeklyAllocation>, I>>(
    object: I,
  ): SchedulingGradeWeeklyAllocation {
    const message = createBaseSchedulingGradeWeeklyAllocation();
    message.grade = object.grade ?? undefined;
    message.assigned_periods = object.assigned_periods ?? undefined;
    message.weekly_capacity = object.weekly_capacity ?? undefined;
    message.missing_requirement_count = object.missing_requirement_count ?? undefined;
    message.campus_capacities =
      object.campus_capacities?.map((e) => SchedulingGradeCampusWeeklyCapacity.fromPartial(e)) || [];
    message.status = object.status ?? undefined;
    return message;
  },
};

function createBaseSchedulingClassesSetup(): SchedulingClassesSetup {
  return {
    subjects: [],
    high_school_courses: [],
    semesters: [],
    high_school_course_setups: [],
    offered_grades: [],
    instructional_requirements: [],
    grade_weekly_allocations: [],
  };
}

export const SchedulingClassesSetup: MessageFns<SchedulingClassesSetup> = {
  encode(message: SchedulingClassesSetup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.subjects) {
      AbstractCourse.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.high_school_courses) {
      AbstractCourse.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.semesters) {
      Semester.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.high_school_course_setups) {
      SchedulingHighSchoolCourseSetup.encode(v!, writer.uint32(34).fork()).join();
    }
    writer.uint32(42).fork();
    for (const v of message.offered_grades) {
      writer.int32(studentGradeToNumber(v));
    }
    writer.join();
    for (const v of message.instructional_requirements) {
      SchedulingInstructionalRequirement.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.grade_weekly_allocations) {
      SchedulingGradeWeeklyAllocation.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingClassesSetup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingClassesSetup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.subjects.push(AbstractCourse.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.high_school_courses.push(AbstractCourse.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.semesters.push(Semester.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.high_school_course_setups.push(SchedulingHighSchoolCourseSetup.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag === 40) {
            message.offered_grades.push(studentGradeFromJSON(reader.int32()));

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.offered_grades.push(studentGradeFromJSON(reader.int32()));
            }

            continue;
          }

          break;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.instructional_requirements.push(SchedulingInstructionalRequirement.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.grade_weekly_allocations.push(SchedulingGradeWeeklyAllocation.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingClassesSetup {
    return {
      subjects: globalThis.Array.isArray(object?.subjects)
        ? object.subjects.map((e: any) => AbstractCourse.fromJSON(e))
        : [],
      high_school_courses: globalThis.Array.isArray(object?.highSchoolCourses)
        ? object.highSchoolCourses.map((e: any) => AbstractCourse.fromJSON(e))
        : [],
      semesters: globalThis.Array.isArray(object?.semesters)
        ? object.semesters.map((e: any) => Semester.fromJSON(e))
        : [],
      high_school_course_setups: globalThis.Array.isArray(object?.highSchoolCourseSetups)
        ? object.highSchoolCourseSetups.map((e: any) => SchedulingHighSchoolCourseSetup.fromJSON(e))
        : [],
      offered_grades: globalThis.Array.isArray(object?.offeredGrades)
        ? object.offeredGrades.map((e: any) => studentGradeFromJSON(e))
        : [],
      instructional_requirements: globalThis.Array.isArray(object?.instructionalRequirements)
        ? object.instructionalRequirements.map((e: any) => SchedulingInstructionalRequirement.fromJSON(e))
        : [],
      grade_weekly_allocations: globalThis.Array.isArray(object?.gradeWeeklyAllocations)
        ? object.gradeWeeklyAllocations.map((e: any) => SchedulingGradeWeeklyAllocation.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingClassesSetup): unknown {
    const obj: any = {};
    if (message.subjects?.length) {
      obj.subjects = message.subjects.map((e) => AbstractCourse.toJSON(e));
    }
    if (message.high_school_courses?.length) {
      obj.highSchoolCourses = message.high_school_courses.map((e) => AbstractCourse.toJSON(e));
    }
    if (message.semesters?.length) {
      obj.semesters = message.semesters.map((e) => Semester.toJSON(e));
    }
    if (message.high_school_course_setups?.length) {
      obj.highSchoolCourseSetups = message.high_school_course_setups.map((e) =>
        SchedulingHighSchoolCourseSetup.toJSON(e)
      );
    }
    if (message.offered_grades?.length) {
      obj.offeredGrades = message.offered_grades.map((e) => studentGradeToJSON(e));
    }
    if (message.instructional_requirements?.length) {
      obj.instructionalRequirements = message.instructional_requirements.map((e) =>
        SchedulingInstructionalRequirement.toJSON(e)
      );
    }
    if (message.grade_weekly_allocations?.length) {
      obj.gradeWeeklyAllocations = message.grade_weekly_allocations.map((e) =>
        SchedulingGradeWeeklyAllocation.toJSON(e)
      );
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingClassesSetup>, I>>(base?: I): SchedulingClassesSetup {
    return SchedulingClassesSetup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingClassesSetup>, I>>(object: I): SchedulingClassesSetup {
    const message = createBaseSchedulingClassesSetup();
    message.subjects = object.subjects?.map((e) => AbstractCourse.fromPartial(e)) || [];
    message.high_school_courses = object.high_school_courses?.map((e) => AbstractCourse.fromPartial(e)) || [];
    message.semesters = object.semesters?.map((e) => Semester.fromPartial(e)) || [];
    message.high_school_course_setups =
      object.high_school_course_setups?.map((e) => SchedulingHighSchoolCourseSetup.fromPartial(e)) || [];
    message.offered_grades = object.offered_grades?.map((e) => e) || [];
    message.instructional_requirements =
      object.instructional_requirements?.map((e) => SchedulingInstructionalRequirement.fromPartial(e)) || [];
    message.grade_weekly_allocations =
      object.grade_weekly_allocations?.map((e) => SchedulingGradeWeeklyAllocation.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingStudentAssignmentOption(): SchedulingStudentAssignmentOption {
  return { student_id: undefined, first_name: undefined, last_name: undefined, grade: undefined };
}

export const SchedulingStudentAssignmentOption: MessageFns<SchedulingStudentAssignmentOption> = {
  encode(message: SchedulingStudentAssignmentOption, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.student_id !== undefined) {
      ObjectId.encode(message.student_id, writer.uint32(10).fork()).join();
    }
    if (message.first_name !== undefined) {
      writer.uint32(18).string(message.first_name);
    }
    if (message.last_name !== undefined) {
      writer.uint32(26).string(message.last_name);
    }
    if (message.grade !== undefined) {
      writer.uint32(32).int32(studentGradeToNumber(message.grade));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingStudentAssignmentOption {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingStudentAssignmentOption();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.student_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.first_name = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.last_name = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.grade = studentGradeFromJSON(reader.int32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingStudentAssignmentOption {
    return {
      student_id: isSet(object.studentId) ? ObjectId.fromJSON(object.studentId) : undefined,
      first_name: isSet(object.firstName) ? globalThis.String(object.firstName) : undefined,
      last_name: isSet(object.lastName) ? globalThis.String(object.lastName) : undefined,
      grade: isSet(object.grade) ? studentGradeFromJSON(object.grade) : undefined,
    };
  },

  toJSON(message: SchedulingStudentAssignmentOption): unknown {
    const obj: any = {};
    if (message.student_id !== undefined) {
      obj.studentId = ObjectId.toJSON(message.student_id);
    }
    if (message.first_name !== undefined) {
      obj.firstName = message.first_name;
    }
    if (message.last_name !== undefined) {
      obj.lastName = message.last_name;
    }
    if (message.grade !== undefined) {
      obj.grade = studentGradeToJSON(message.grade);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingStudentAssignmentOption>, I>>(
    base?: I,
  ): SchedulingStudentAssignmentOption {
    return SchedulingStudentAssignmentOption.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingStudentAssignmentOption>, I>>(
    object: I,
  ): SchedulingStudentAssignmentOption {
    const message = createBaseSchedulingStudentAssignmentOption();
    message.student_id = (object.student_id !== undefined && object.student_id !== null)
      ? ObjectId.fromPartial(object.student_id)
      : undefined;
    message.first_name = object.first_name ?? undefined;
    message.last_name = object.last_name ?? undefined;
    message.grade = object.grade ?? undefined;
    return message;
  },
};

function createBaseSchedulingSubjectAssignmentPreview(): SchedulingSubjectAssignmentPreview {
  return { grade: undefined, subjects: [], student_count: undefined, students: [] };
}

export const SchedulingSubjectAssignmentPreview: MessageFns<SchedulingSubjectAssignmentPreview> = {
  encode(message: SchedulingSubjectAssignmentPreview, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.grade !== undefined) {
      writer.uint32(8).int32(studentGradeToNumber(message.grade));
    }
    for (const v of message.subjects) {
      AbstractCourse.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.student_count !== undefined) {
      writer.uint32(24).uint32(message.student_count);
    }
    for (const v of message.students) {
      SchedulingStudentAssignmentOption.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingSubjectAssignmentPreview {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingSubjectAssignmentPreview();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.grade = studentGradeFromJSON(reader.int32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.subjects.push(AbstractCourse.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.student_count = reader.uint32();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.students.push(SchedulingStudentAssignmentOption.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingSubjectAssignmentPreview {
    return {
      grade: isSet(object.grade) ? studentGradeFromJSON(object.grade) : undefined,
      subjects: globalThis.Array.isArray(object?.subjects)
        ? object.subjects.map((e: any) => AbstractCourse.fromJSON(e))
        : [],
      student_count: isSet(object.studentCount) ? globalThis.Number(object.studentCount) : undefined,
      students: globalThis.Array.isArray(object?.students)
        ? object.students.map((e: any) => SchedulingStudentAssignmentOption.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingSubjectAssignmentPreview): unknown {
    const obj: any = {};
    if (message.grade !== undefined) {
      obj.grade = studentGradeToJSON(message.grade);
    }
    if (message.subjects?.length) {
      obj.subjects = message.subjects.map((e) => AbstractCourse.toJSON(e));
    }
    if (message.student_count !== undefined) {
      obj.studentCount = Math.round(message.student_count);
    }
    if (message.students?.length) {
      obj.students = message.students.map((e) => SchedulingStudentAssignmentOption.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingSubjectAssignmentPreview>, I>>(
    base?: I,
  ): SchedulingSubjectAssignmentPreview {
    return SchedulingSubjectAssignmentPreview.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingSubjectAssignmentPreview>, I>>(
    object: I,
  ): SchedulingSubjectAssignmentPreview {
    const message = createBaseSchedulingSubjectAssignmentPreview();
    message.grade = object.grade ?? undefined;
    message.subjects = object.subjects?.map((e) => AbstractCourse.fromPartial(e)) || [];
    message.student_count = object.student_count ?? undefined;
    message.students = object.students?.map((e) => SchedulingStudentAssignmentOption.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingHighSchoolCourseStudentAssignment(): SchedulingHighSchoolCourseStudentAssignment {
  return {
    id: undefined,
    organization: undefined,
    scheduling_workspace: undefined,
    abstract_course_id: undefined,
    student_ids: [],
    offering_id: undefined,
  };
}

export const SchedulingHighSchoolCourseStudentAssignment: MessageFns<SchedulingHighSchoolCourseStudentAssignment> = {
  encode(
    message: SchedulingHighSchoolCourseStudentAssignment,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    if (message.abstract_course_id !== undefined) {
      ObjectId.encode(message.abstract_course_id, writer.uint32(34).fork()).join();
    }
    for (const v of message.student_ids) {
      ObjectId.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.offering_id !== undefined) {
      ObjectId.encode(message.offering_id, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingHighSchoolCourseStudentAssignment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingHighSchoolCourseStudentAssignment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.abstract_course_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.student_ids.push(ObjectId.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.offering_id = ObjectId.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingHighSchoolCourseStudentAssignment {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      abstract_course_id: isSet(object.abstractCourseId) ? ObjectId.fromJSON(object.abstractCourseId) : undefined,
      student_ids: globalThis.Array.isArray(object?.studentIds)
        ? object.studentIds.map((e: any) => ObjectId.fromJSON(e))
        : [],
      offering_id: isSet(object.offeringId) ? ObjectId.fromJSON(object.offeringId) : undefined,
    };
  },

  toJSON(message: SchedulingHighSchoolCourseStudentAssignment): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.abstract_course_id !== undefined) {
      obj.abstractCourseId = ObjectId.toJSON(message.abstract_course_id);
    }
    if (message.student_ids?.length) {
      obj.studentIds = message.student_ids.map((e) => ObjectId.toJSON(e));
    }
    if (message.offering_id !== undefined) {
      obj.offeringId = ObjectId.toJSON(message.offering_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingHighSchoolCourseStudentAssignment>, I>>(
    base?: I,
  ): SchedulingHighSchoolCourseStudentAssignment {
    return SchedulingHighSchoolCourseStudentAssignment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingHighSchoolCourseStudentAssignment>, I>>(
    object: I,
  ): SchedulingHighSchoolCourseStudentAssignment {
    const message = createBaseSchedulingHighSchoolCourseStudentAssignment();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.abstract_course_id = (object.abstract_course_id !== undefined && object.abstract_course_id !== null)
      ? ObjectId.fromPartial(object.abstract_course_id)
      : undefined;
    message.student_ids = object.student_ids?.map((e) => ObjectId.fromPartial(e)) || [];
    message.offering_id = (object.offering_id !== undefined && object.offering_id !== null)
      ? ObjectId.fromPartial(object.offering_id)
      : undefined;
    return message;
  },
};

function createBaseSchedulingClassAssignmentSetup(): SchedulingClassAssignmentSetup {
  return {
    subject_previews: [],
    high_school_courses: [],
    high_school_course_setups: [],
    high_school_course_assignments: [],
    high_school_students: [],
    semesters: [],
  };
}

export const SchedulingClassAssignmentSetup: MessageFns<SchedulingClassAssignmentSetup> = {
  encode(message: SchedulingClassAssignmentSetup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.subject_previews) {
      SchedulingSubjectAssignmentPreview.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.high_school_courses) {
      AbstractCourse.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.high_school_course_setups) {
      SchedulingHighSchoolCourseSetup.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.high_school_course_assignments) {
      SchedulingHighSchoolCourseStudentAssignment.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.high_school_students) {
      SchedulingStudentAssignmentOption.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.semesters) {
      Semester.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingClassAssignmentSetup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingClassAssignmentSetup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.subject_previews.push(SchedulingSubjectAssignmentPreview.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.high_school_courses.push(AbstractCourse.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.high_school_course_setups.push(SchedulingHighSchoolCourseSetup.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.high_school_course_assignments.push(
            SchedulingHighSchoolCourseStudentAssignment.decode(reader, reader.uint32()),
          );
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.high_school_students.push(SchedulingStudentAssignmentOption.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.semesters.push(Semester.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingClassAssignmentSetup {
    return {
      subject_previews: globalThis.Array.isArray(object?.subjectPreviews)
        ? object.subjectPreviews.map((e: any) => SchedulingSubjectAssignmentPreview.fromJSON(e))
        : [],
      high_school_courses: globalThis.Array.isArray(object?.highSchoolCourses)
        ? object.highSchoolCourses.map((e: any) => AbstractCourse.fromJSON(e))
        : [],
      high_school_course_setups: globalThis.Array.isArray(object?.highSchoolCourseSetups)
        ? object.highSchoolCourseSetups.map((e: any) => SchedulingHighSchoolCourseSetup.fromJSON(e))
        : [],
      high_school_course_assignments: globalThis.Array.isArray(object?.highSchoolCourseAssignments)
        ? object.highSchoolCourseAssignments.map((e: any) => SchedulingHighSchoolCourseStudentAssignment.fromJSON(e))
        : [],
      high_school_students: globalThis.Array.isArray(object?.highSchoolStudents)
        ? object.highSchoolStudents.map((e: any) => SchedulingStudentAssignmentOption.fromJSON(e))
        : [],
      semesters: globalThis.Array.isArray(object?.semesters)
        ? object.semesters.map((e: any) => Semester.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingClassAssignmentSetup): unknown {
    const obj: any = {};
    if (message.subject_previews?.length) {
      obj.subjectPreviews = message.subject_previews.map((e) => SchedulingSubjectAssignmentPreview.toJSON(e));
    }
    if (message.high_school_courses?.length) {
      obj.highSchoolCourses = message.high_school_courses.map((e) => AbstractCourse.toJSON(e));
    }
    if (message.high_school_course_setups?.length) {
      obj.highSchoolCourseSetups = message.high_school_course_setups.map((e) =>
        SchedulingHighSchoolCourseSetup.toJSON(e)
      );
    }
    if (message.high_school_course_assignments?.length) {
      obj.highSchoolCourseAssignments = message.high_school_course_assignments.map((e) =>
        SchedulingHighSchoolCourseStudentAssignment.toJSON(e)
      );
    }
    if (message.high_school_students?.length) {
      obj.highSchoolStudents = message.high_school_students.map((e) => SchedulingStudentAssignmentOption.toJSON(e));
    }
    if (message.semesters?.length) {
      obj.semesters = message.semesters.map((e) => Semester.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingClassAssignmentSetup>, I>>(base?: I): SchedulingClassAssignmentSetup {
    return SchedulingClassAssignmentSetup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingClassAssignmentSetup>, I>>(
    object: I,
  ): SchedulingClassAssignmentSetup {
    const message = createBaseSchedulingClassAssignmentSetup();
    message.subject_previews = object.subject_previews?.map((e) => SchedulingSubjectAssignmentPreview.fromPartial(e)) ||
      [];
    message.high_school_courses = object.high_school_courses?.map((e) => AbstractCourse.fromPartial(e)) || [];
    message.high_school_course_setups =
      object.high_school_course_setups?.map((e) => SchedulingHighSchoolCourseSetup.fromPartial(e)) || [];
    message.high_school_course_assignments =
      object.high_school_course_assignments?.map((e) => SchedulingHighSchoolCourseStudentAssignment.fromPartial(e)) ||
      [];
    message.high_school_students =
      object.high_school_students?.map((e) => SchedulingStudentAssignmentOption.fromPartial(e)) || [];
    message.semesters = object.semesters?.map((e) => Semester.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingClassGroup(): SchedulingClassGroup {
  return {
    id: undefined,
    organization: undefined,
    scheduling_workspace: undefined,
    campus_id: undefined,
    grade: undefined,
    semester_id: undefined,
    label: undefined,
    stale: undefined,
  };
}

export const SchedulingClassGroup: MessageFns<SchedulingClassGroup> = {
  encode(message: SchedulingClassGroup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    if (message.campus_id !== undefined) {
      ObjectId.encode(message.campus_id, writer.uint32(34).fork()).join();
    }
    if (message.grade !== undefined) {
      writer.uint32(40).int32(studentGradeToNumber(message.grade));
    }
    if (message.semester_id !== undefined) {
      ObjectId.encode(message.semester_id, writer.uint32(50).fork()).join();
    }
    if (message.label !== undefined) {
      writer.uint32(58).string(message.label);
    }
    if (message.stale !== undefined) {
      writer.uint32(64).bool(message.stale);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingClassGroup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingClassGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.campus_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.grade = studentGradeFromJSON(reader.int32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.semester_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.label = reader.string();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.stale = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingClassGroup {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      campus_id: isSet(object.campusId) ? ObjectId.fromJSON(object.campusId) : undefined,
      grade: isSet(object.grade) ? studentGradeFromJSON(object.grade) : undefined,
      semester_id: isSet(object.semesterId) ? ObjectId.fromJSON(object.semesterId) : undefined,
      label: isSet(object.label) ? globalThis.String(object.label) : undefined,
      stale: isSet(object.stale) ? globalThis.Boolean(object.stale) : undefined,
    };
  },

  toJSON(message: SchedulingClassGroup): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.campus_id !== undefined) {
      obj.campusId = ObjectId.toJSON(message.campus_id);
    }
    if (message.grade !== undefined) {
      obj.grade = studentGradeToJSON(message.grade);
    }
    if (message.semester_id !== undefined) {
      obj.semesterId = ObjectId.toJSON(message.semester_id);
    }
    if (message.label !== undefined) {
      obj.label = message.label;
    }
    if (message.stale !== undefined) {
      obj.stale = message.stale;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingClassGroup>, I>>(base?: I): SchedulingClassGroup {
    return SchedulingClassGroup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingClassGroup>, I>>(object: I): SchedulingClassGroup {
    const message = createBaseSchedulingClassGroup();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.campus_id = (object.campus_id !== undefined && object.campus_id !== null)
      ? ObjectId.fromPartial(object.campus_id)
      : undefined;
    message.grade = object.grade ?? undefined;
    message.semester_id = (object.semester_id !== undefined && object.semester_id !== null)
      ? ObjectId.fromPartial(object.semester_id)
      : undefined;
    message.label = object.label ?? undefined;
    message.stale = object.stale ?? undefined;
    return message;
  },
};

function createBaseSchedulingClassGroupTeacherAssignment(): SchedulingClassGroupTeacherAssignment {
  return {
    id: undefined,
    organization: undefined,
    scheduling_workspace: undefined,
    class_group_id: undefined,
    abstract_course_id: undefined,
    teacher_id: undefined,
  };
}

export const SchedulingClassGroupTeacherAssignment: MessageFns<SchedulingClassGroupTeacherAssignment> = {
  encode(message: SchedulingClassGroupTeacherAssignment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    if (message.class_group_id !== undefined) {
      Uuid.encode(message.class_group_id, writer.uint32(34).fork()).join();
    }
    if (message.abstract_course_id !== undefined) {
      ObjectId.encode(message.abstract_course_id, writer.uint32(42).fork()).join();
    }
    if (message.teacher_id !== undefined) {
      ObjectId.encode(message.teacher_id, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingClassGroupTeacherAssignment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingClassGroupTeacherAssignment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.class_group_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.abstract_course_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.teacher_id = ObjectId.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingClassGroupTeacherAssignment {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      class_group_id: isSet(object.classGroupId) ? Uuid.fromJSON(object.classGroupId) : undefined,
      abstract_course_id: isSet(object.abstractCourseId) ? ObjectId.fromJSON(object.abstractCourseId) : undefined,
      teacher_id: isSet(object.teacherId) ? ObjectId.fromJSON(object.teacherId) : undefined,
    };
  },

  toJSON(message: SchedulingClassGroupTeacherAssignment): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.class_group_id !== undefined) {
      obj.classGroupId = Uuid.toJSON(message.class_group_id);
    }
    if (message.abstract_course_id !== undefined) {
      obj.abstractCourseId = ObjectId.toJSON(message.abstract_course_id);
    }
    if (message.teacher_id !== undefined) {
      obj.teacherId = ObjectId.toJSON(message.teacher_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingClassGroupTeacherAssignment>, I>>(
    base?: I,
  ): SchedulingClassGroupTeacherAssignment {
    return SchedulingClassGroupTeacherAssignment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingClassGroupTeacherAssignment>, I>>(
    object: I,
  ): SchedulingClassGroupTeacherAssignment {
    const message = createBaseSchedulingClassGroupTeacherAssignment();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.class_group_id = (object.class_group_id !== undefined && object.class_group_id !== null)
      ? Uuid.fromPartial(object.class_group_id)
      : undefined;
    message.abstract_course_id = (object.abstract_course_id !== undefined && object.abstract_course_id !== null)
      ? ObjectId.fromPartial(object.abstract_course_id)
      : undefined;
    message.teacher_id = (object.teacher_id !== undefined && object.teacher_id !== null)
      ? ObjectId.fromPartial(object.teacher_id)
      : undefined;
    return message;
  },
};

function createBaseSchedulingClassGroupTeacherPeriodAllocation(): SchedulingClassGroupTeacherPeriodAllocation {
  return {
    id: undefined,
    organization: undefined,
    scheduling_workspace: undefined,
    class_group_id: undefined,
    abstract_course_id: undefined,
    teacher_id: undefined,
    periods_per_week: undefined,
  };
}

export const SchedulingClassGroupTeacherPeriodAllocation: MessageFns<SchedulingClassGroupTeacherPeriodAllocation> = {
  encode(
    message: SchedulingClassGroupTeacherPeriodAllocation,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    if (message.class_group_id !== undefined) {
      Uuid.encode(message.class_group_id, writer.uint32(34).fork()).join();
    }
    if (message.abstract_course_id !== undefined) {
      ObjectId.encode(message.abstract_course_id, writer.uint32(42).fork()).join();
    }
    if (message.teacher_id !== undefined) {
      ObjectId.encode(message.teacher_id, writer.uint32(50).fork()).join();
    }
    if (message.periods_per_week !== undefined) {
      writer.uint32(56).uint32(message.periods_per_week);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingClassGroupTeacherPeriodAllocation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingClassGroupTeacherPeriodAllocation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.class_group_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.abstract_course_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.teacher_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.periods_per_week = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingClassGroupTeacherPeriodAllocation {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      class_group_id: isSet(object.classGroupId) ? Uuid.fromJSON(object.classGroupId) : undefined,
      abstract_course_id: isSet(object.abstractCourseId) ? ObjectId.fromJSON(object.abstractCourseId) : undefined,
      teacher_id: isSet(object.teacherId) ? ObjectId.fromJSON(object.teacherId) : undefined,
      periods_per_week: isSet(object.periodsPerWeek) ? globalThis.Number(object.periodsPerWeek) : undefined,
    };
  },

  toJSON(message: SchedulingClassGroupTeacherPeriodAllocation): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.class_group_id !== undefined) {
      obj.classGroupId = Uuid.toJSON(message.class_group_id);
    }
    if (message.abstract_course_id !== undefined) {
      obj.abstractCourseId = ObjectId.toJSON(message.abstract_course_id);
    }
    if (message.teacher_id !== undefined) {
      obj.teacherId = ObjectId.toJSON(message.teacher_id);
    }
    if (message.periods_per_week !== undefined) {
      obj.periodsPerWeek = Math.round(message.periods_per_week);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingClassGroupTeacherPeriodAllocation>, I>>(
    base?: I,
  ): SchedulingClassGroupTeacherPeriodAllocation {
    return SchedulingClassGroupTeacherPeriodAllocation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingClassGroupTeacherPeriodAllocation>, I>>(
    object: I,
  ): SchedulingClassGroupTeacherPeriodAllocation {
    const message = createBaseSchedulingClassGroupTeacherPeriodAllocation();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.class_group_id = (object.class_group_id !== undefined && object.class_group_id !== null)
      ? Uuid.fromPartial(object.class_group_id)
      : undefined;
    message.abstract_course_id = (object.abstract_course_id !== undefined && object.abstract_course_id !== null)
      ? ObjectId.fromPartial(object.abstract_course_id)
      : undefined;
    message.teacher_id = (object.teacher_id !== undefined && object.teacher_id !== null)
      ? ObjectId.fromPartial(object.teacher_id)
      : undefined;
    message.periods_per_week = object.periods_per_week ?? undefined;
    return message;
  },
};

function createBaseSchedulingSharedLessonMember(): SchedulingSharedLessonMember {
  return { class_group_id: undefined, abstract_course_id: undefined };
}

export const SchedulingSharedLessonMember: MessageFns<SchedulingSharedLessonMember> = {
  encode(message: SchedulingSharedLessonMember, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.class_group_id !== undefined) {
      Uuid.encode(message.class_group_id, writer.uint32(10).fork()).join();
    }
    if (message.abstract_course_id !== undefined) {
      ObjectId.encode(message.abstract_course_id, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingSharedLessonMember {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingSharedLessonMember();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.class_group_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.abstract_course_id = ObjectId.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingSharedLessonMember {
    return {
      class_group_id: isSet(object.classGroupId) ? Uuid.fromJSON(object.classGroupId) : undefined,
      abstract_course_id: isSet(object.abstractCourseId) ? ObjectId.fromJSON(object.abstractCourseId) : undefined,
    };
  },

  toJSON(message: SchedulingSharedLessonMember): unknown {
    const obj: any = {};
    if (message.class_group_id !== undefined) {
      obj.classGroupId = Uuid.toJSON(message.class_group_id);
    }
    if (message.abstract_course_id !== undefined) {
      obj.abstractCourseId = ObjectId.toJSON(message.abstract_course_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingSharedLessonMember>, I>>(base?: I): SchedulingSharedLessonMember {
    return SchedulingSharedLessonMember.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingSharedLessonMember>, I>>(object: I): SchedulingSharedLessonMember {
    const message = createBaseSchedulingSharedLessonMember();
    message.class_group_id = (object.class_group_id !== undefined && object.class_group_id !== null)
      ? Uuid.fromPartial(object.class_group_id)
      : undefined;
    message.abstract_course_id = (object.abstract_course_id !== undefined && object.abstract_course_id !== null)
      ? ObjectId.fromPartial(object.abstract_course_id)
      : undefined;
    return message;
  },
};

function createBaseSchedulingSharedLesson(): SchedulingSharedLesson {
  return {
    id: undefined,
    organization: undefined,
    scheduling_workspace: undefined,
    members: [],
    teacher_id: undefined,
    teacher_period_allocations: [],
  };
}

export const SchedulingSharedLesson: MessageFns<SchedulingSharedLesson> = {
  encode(message: SchedulingSharedLesson, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    for (const v of message.members) {
      SchedulingSharedLessonMember.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.teacher_id !== undefined) {
      ObjectId.encode(message.teacher_id, writer.uint32(42).fork()).join();
    }
    for (const v of message.teacher_period_allocations) {
      SchedulingTeacherPeriodAllocation.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingSharedLesson {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingSharedLesson();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.members.push(SchedulingSharedLessonMember.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.teacher_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.teacher_period_allocations.push(SchedulingTeacherPeriodAllocation.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingSharedLesson {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      members: globalThis.Array.isArray(object?.members)
        ? object.members.map((e: any) => SchedulingSharedLessonMember.fromJSON(e))
        : [],
      teacher_id: isSet(object.teacherId) ? ObjectId.fromJSON(object.teacherId) : undefined,
      teacher_period_allocations: globalThis.Array.isArray(object?.teacherPeriodAllocations)
        ? object.teacherPeriodAllocations.map((e: any) => SchedulingTeacherPeriodAllocation.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingSharedLesson): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.members?.length) {
      obj.members = message.members.map((e) => SchedulingSharedLessonMember.toJSON(e));
    }
    if (message.teacher_id !== undefined) {
      obj.teacherId = ObjectId.toJSON(message.teacher_id);
    }
    if (message.teacher_period_allocations?.length) {
      obj.teacherPeriodAllocations = message.teacher_period_allocations.map((e) =>
        SchedulingTeacherPeriodAllocation.toJSON(e)
      );
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingSharedLesson>, I>>(base?: I): SchedulingSharedLesson {
    return SchedulingSharedLesson.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingSharedLesson>, I>>(object: I): SchedulingSharedLesson {
    const message = createBaseSchedulingSharedLesson();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.members = object.members?.map((e) => SchedulingSharedLessonMember.fromPartial(e)) || [];
    message.teacher_id = (object.teacher_id !== undefined && object.teacher_id !== null)
      ? ObjectId.fromPartial(object.teacher_id)
      : undefined;
    message.teacher_period_allocations =
      object.teacher_period_allocations?.map((e) => SchedulingTeacherPeriodAllocation.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingClassGroupSetup(): SchedulingClassGroupSetup {
  return {
    groups: [],
    subjects: [],
    assignments: [],
    teachers: [],
    teacher_profiles: [],
    semesters: [],
    high_school_courses: [],
    high_school_course_setups: [],
    high_school_course_assignments: [],
    teacher_period_allocations: [],
    instructional_requirements: [],
    shared_lessons: [],
  };
}

export const SchedulingClassGroupSetup: MessageFns<SchedulingClassGroupSetup> = {
  encode(message: SchedulingClassGroupSetup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.groups) {
      SchedulingClassGroup.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.subjects) {
      AbstractCourse.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.assignments) {
      SchedulingClassGroupTeacherAssignment.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.teachers) {
      TeacherBasic.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.teacher_profiles) {
      SchedulingTeacherProfile.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.semesters) {
      Semester.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.high_school_courses) {
      AbstractCourse.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.high_school_course_setups) {
      SchedulingHighSchoolCourseSetup.encode(v!, writer.uint32(66).fork()).join();
    }
    for (const v of message.high_school_course_assignments) {
      SchedulingHighSchoolCourseStudentAssignment.encode(v!, writer.uint32(74).fork()).join();
    }
    for (const v of message.teacher_period_allocations) {
      SchedulingClassGroupTeacherPeriodAllocation.encode(v!, writer.uint32(82).fork()).join();
    }
    for (const v of message.instructional_requirements) {
      SchedulingInstructionalRequirement.encode(v!, writer.uint32(90).fork()).join();
    }
    for (const v of message.shared_lessons) {
      SchedulingSharedLesson.encode(v!, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingClassGroupSetup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingClassGroupSetup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.groups.push(SchedulingClassGroup.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.subjects.push(AbstractCourse.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.assignments.push(SchedulingClassGroupTeacherAssignment.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.teachers.push(TeacherBasic.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.teacher_profiles.push(SchedulingTeacherProfile.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.semesters.push(Semester.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.high_school_courses.push(AbstractCourse.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.high_school_course_setups.push(SchedulingHighSchoolCourseSetup.decode(reader, reader.uint32()));
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.high_school_course_assignments.push(
            SchedulingHighSchoolCourseStudentAssignment.decode(reader, reader.uint32()),
          );
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.teacher_period_allocations.push(
            SchedulingClassGroupTeacherPeriodAllocation.decode(reader, reader.uint32()),
          );
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.instructional_requirements.push(SchedulingInstructionalRequirement.decode(reader, reader.uint32()));
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.shared_lessons.push(SchedulingSharedLesson.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingClassGroupSetup {
    return {
      groups: globalThis.Array.isArray(object?.groups)
        ? object.groups.map((e: any) => SchedulingClassGroup.fromJSON(e))
        : [],
      subjects: globalThis.Array.isArray(object?.subjects)
        ? object.subjects.map((e: any) => AbstractCourse.fromJSON(e))
        : [],
      assignments: globalThis.Array.isArray(object?.assignments)
        ? object.assignments.map((e: any) => SchedulingClassGroupTeacherAssignment.fromJSON(e))
        : [],
      teachers: globalThis.Array.isArray(object?.teachers)
        ? object.teachers.map((e: any) => TeacherBasic.fromJSON(e))
        : [],
      teacher_profiles: globalThis.Array.isArray(object?.teacherProfiles)
        ? object.teacherProfiles.map((e: any) => SchedulingTeacherProfile.fromJSON(e))
        : [],
      semesters: globalThis.Array.isArray(object?.semesters)
        ? object.semesters.map((e: any) => Semester.fromJSON(e))
        : [],
      high_school_courses: globalThis.Array.isArray(object?.highSchoolCourses)
        ? object.highSchoolCourses.map((e: any) => AbstractCourse.fromJSON(e))
        : [],
      high_school_course_setups: globalThis.Array.isArray(object?.highSchoolCourseSetups)
        ? object.highSchoolCourseSetups.map((e: any) => SchedulingHighSchoolCourseSetup.fromJSON(e))
        : [],
      high_school_course_assignments: globalThis.Array.isArray(object?.highSchoolCourseAssignments)
        ? object.highSchoolCourseAssignments.map((e: any) => SchedulingHighSchoolCourseStudentAssignment.fromJSON(e))
        : [],
      teacher_period_allocations: globalThis.Array.isArray(object?.teacherPeriodAllocations)
        ? object.teacherPeriodAllocations.map((e: any) => SchedulingClassGroupTeacherPeriodAllocation.fromJSON(e))
        : [],
      instructional_requirements: globalThis.Array.isArray(object?.instructionalRequirements)
        ? object.instructionalRequirements.map((e: any) => SchedulingInstructionalRequirement.fromJSON(e))
        : [],
      shared_lessons: globalThis.Array.isArray(object?.sharedLessons)
        ? object.sharedLessons.map((e: any) => SchedulingSharedLesson.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingClassGroupSetup): unknown {
    const obj: any = {};
    if (message.groups?.length) {
      obj.groups = message.groups.map((e) => SchedulingClassGroup.toJSON(e));
    }
    if (message.subjects?.length) {
      obj.subjects = message.subjects.map((e) => AbstractCourse.toJSON(e));
    }
    if (message.assignments?.length) {
      obj.assignments = message.assignments.map((e) => SchedulingClassGroupTeacherAssignment.toJSON(e));
    }
    if (message.teachers?.length) {
      obj.teachers = message.teachers.map((e) => TeacherBasic.toJSON(e));
    }
    if (message.teacher_profiles?.length) {
      obj.teacherProfiles = message.teacher_profiles.map((e) => SchedulingTeacherProfile.toJSON(e));
    }
    if (message.semesters?.length) {
      obj.semesters = message.semesters.map((e) => Semester.toJSON(e));
    }
    if (message.high_school_courses?.length) {
      obj.highSchoolCourses = message.high_school_courses.map((e) => AbstractCourse.toJSON(e));
    }
    if (message.high_school_course_setups?.length) {
      obj.highSchoolCourseSetups = message.high_school_course_setups.map((e) =>
        SchedulingHighSchoolCourseSetup.toJSON(e)
      );
    }
    if (message.high_school_course_assignments?.length) {
      obj.highSchoolCourseAssignments = message.high_school_course_assignments.map((e) =>
        SchedulingHighSchoolCourseStudentAssignment.toJSON(e)
      );
    }
    if (message.teacher_period_allocations?.length) {
      obj.teacherPeriodAllocations = message.teacher_period_allocations.map((e) =>
        SchedulingClassGroupTeacherPeriodAllocation.toJSON(e)
      );
    }
    if (message.instructional_requirements?.length) {
      obj.instructionalRequirements = message.instructional_requirements.map((e) =>
        SchedulingInstructionalRequirement.toJSON(e)
      );
    }
    if (message.shared_lessons?.length) {
      obj.sharedLessons = message.shared_lessons.map((e) => SchedulingSharedLesson.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingClassGroupSetup>, I>>(base?: I): SchedulingClassGroupSetup {
    return SchedulingClassGroupSetup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingClassGroupSetup>, I>>(object: I): SchedulingClassGroupSetup {
    const message = createBaseSchedulingClassGroupSetup();
    message.groups = object.groups?.map((e) => SchedulingClassGroup.fromPartial(e)) || [];
    message.subjects = object.subjects?.map((e) => AbstractCourse.fromPartial(e)) || [];
    message.assignments = object.assignments?.map((e) => SchedulingClassGroupTeacherAssignment.fromPartial(e)) || [];
    message.teachers = object.teachers?.map((e) => TeacherBasic.fromPartial(e)) || [];
    message.teacher_profiles = object.teacher_profiles?.map((e) => SchedulingTeacherProfile.fromPartial(e)) || [];
    message.semesters = object.semesters?.map((e) => Semester.fromPartial(e)) || [];
    message.high_school_courses = object.high_school_courses?.map((e) => AbstractCourse.fromPartial(e)) || [];
    message.high_school_course_setups =
      object.high_school_course_setups?.map((e) => SchedulingHighSchoolCourseSetup.fromPartial(e)) || [];
    message.high_school_course_assignments =
      object.high_school_course_assignments?.map((e) => SchedulingHighSchoolCourseStudentAssignment.fromPartial(e)) ||
      [];
    message.teacher_period_allocations =
      object.teacher_period_allocations?.map((e) => SchedulingClassGroupTeacherPeriodAllocation.fromPartial(e)) || [];
    message.instructional_requirements =
      object.instructional_requirements?.map((e) => SchedulingInstructionalRequirement.fromPartial(e)) || [];
    message.shared_lessons = object.shared_lessons?.map((e) => SchedulingSharedLesson.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingGenerationBlocker(): SchedulingGenerationBlocker {
  return { message: undefined, code: undefined };
}

export const SchedulingGenerationBlocker: MessageFns<SchedulingGenerationBlocker> = {
  encode(message: SchedulingGenerationBlocker, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== undefined) {
      writer.uint32(10).string(message.message);
    }
    if (message.code !== undefined) {
      writer.uint32(18).string(message.code);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingGenerationBlocker {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingGenerationBlocker();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.code = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingGenerationBlocker {
    return {
      message: isSet(object.message) ? globalThis.String(object.message) : undefined,
      code: isSet(object.code) ? globalThis.String(object.code) : undefined,
    };
  },

  toJSON(message: SchedulingGenerationBlocker): unknown {
    const obj: any = {};
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    if (message.code !== undefined) {
      obj.code = message.code;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingGenerationBlocker>, I>>(base?: I): SchedulingGenerationBlocker {
    return SchedulingGenerationBlocker.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingGenerationBlocker>, I>>(object: I): SchedulingGenerationBlocker {
    const message = createBaseSchedulingGenerationBlocker();
    message.message = object.message ?? undefined;
    message.code = object.code ?? undefined;
    return message;
  },
};

function createBaseSchedulingGenerationRun(): SchedulingGenerationRun {
  return {
    id: undefined,
    organization: undefined,
    scheduling_workspace: undefined,
    school_year: undefined,
    solver_input_snapshot_id: undefined,
    schema_version: undefined,
    status: undefined,
    started_at: undefined,
    finished_at: undefined,
    error_message: undefined,
    blockers: [],
    generated_schedule_id: undefined,
    created_by: undefined,
    created_at: undefined,
    solver_reasons: [],
    purpose: undefined,
    source_working_revision_id: undefined,
    pin_count: undefined,
  };
}

export const SchedulingGenerationRun: MessageFns<SchedulingGenerationRun> = {
  encode(message: SchedulingGenerationRun, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    if (message.school_year !== undefined) {
      ObjectId.encode(message.school_year, writer.uint32(34).fork()).join();
    }
    if (message.solver_input_snapshot_id !== undefined) {
      Uuid.encode(message.solver_input_snapshot_id, writer.uint32(42).fork()).join();
    }
    if (message.schema_version !== undefined) {
      writer.uint32(48).uint32(message.schema_version);
    }
    if (message.status !== undefined) {
      writer.uint32(56).int32(schedulingGenerationRunStatusToNumber(message.status));
    }
    if (message.started_at !== undefined) {
      writer.uint32(66).string(message.started_at);
    }
    if (message.finished_at !== undefined) {
      writer.uint32(74).string(message.finished_at);
    }
    if (message.error_message !== undefined) {
      writer.uint32(82).string(message.error_message);
    }
    for (const v of message.blockers) {
      SchedulingGenerationBlocker.encode(v!, writer.uint32(90).fork()).join();
    }
    if (message.generated_schedule_id !== undefined) {
      ObjectId.encode(message.generated_schedule_id, writer.uint32(98).fork()).join();
    }
    if (message.created_by !== undefined) {
      ObjectId.encode(message.created_by, writer.uint32(106).fork()).join();
    }
    if (message.created_at !== undefined) {
      writer.uint32(114).string(message.created_at);
    }
    for (const v of message.solver_reasons) {
      writer.uint32(122).string(v!);
    }
    if (message.purpose !== undefined) {
      writer.uint32(128).int32(schedulingGenerationPurposeToNumber(message.purpose));
    }
    if (message.source_working_revision_id !== undefined) {
      Uuid.encode(message.source_working_revision_id, writer.uint32(138).fork()).join();
    }
    if (message.pin_count !== undefined) {
      writer.uint32(144).uint32(message.pin_count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingGenerationRun {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingGenerationRun();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.school_year = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.solver_input_snapshot_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.schema_version = reader.uint32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.status = schedulingGenerationRunStatusFromJSON(reader.int32());
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.started_at = reader.string();
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.finished_at = reader.string();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.error_message = reader.string();
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.blockers.push(SchedulingGenerationBlocker.decode(reader, reader.uint32()));
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.generated_schedule_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.created_by = ObjectId.decode(reader, reader.uint32());
          continue;
        case 14:
          if (tag !== 114) {
            break;
          }

          message.created_at = reader.string();
          continue;
        case 15:
          if (tag !== 122) {
            break;
          }

          message.solver_reasons.push(reader.string());
          continue;
        case 16:
          if (tag !== 128) {
            break;
          }

          message.purpose = schedulingGenerationPurposeFromJSON(reader.int32());
          continue;
        case 17:
          if (tag !== 138) {
            break;
          }

          message.source_working_revision_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 18:
          if (tag !== 144) {
            break;
          }

          message.pin_count = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingGenerationRun {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      school_year: isSet(object.schoolYear) ? ObjectId.fromJSON(object.schoolYear) : undefined,
      solver_input_snapshot_id: isSet(object.solverInputSnapshotId)
        ? Uuid.fromJSON(object.solverInputSnapshotId)
        : undefined,
      schema_version: isSet(object.schemaVersion) ? globalThis.Number(object.schemaVersion) : undefined,
      status: isSet(object.status) ? schedulingGenerationRunStatusFromJSON(object.status) : undefined,
      started_at: isSet(object.startedAt) ? globalThis.String(object.startedAt) : undefined,
      finished_at: isSet(object.finishedAt) ? globalThis.String(object.finishedAt) : undefined,
      error_message: isSet(object.errorMessage) ? globalThis.String(object.errorMessage) : undefined,
      blockers: globalThis.Array.isArray(object?.blockers)
        ? object.blockers.map((e: any) => SchedulingGenerationBlocker.fromJSON(e))
        : [],
      generated_schedule_id: isSet(object.generatedScheduleId)
        ? ObjectId.fromJSON(object.generatedScheduleId)
        : undefined,
      created_by: isSet(object.createdBy) ? ObjectId.fromJSON(object.createdBy) : undefined,
      created_at: isSet(object.createdAt) ? globalThis.String(object.createdAt) : undefined,
      solver_reasons: globalThis.Array.isArray(object?.solverReasons)
        ? object.solverReasons.map((e: any) => globalThis.String(e))
        : [],
      purpose: isSet(object.purpose) ? schedulingGenerationPurposeFromJSON(object.purpose) : undefined,
      source_working_revision_id: isSet(object.sourceWorkingRevisionId)
        ? Uuid.fromJSON(object.sourceWorkingRevisionId)
        : undefined,
      pin_count: isSet(object.pinCount) ? globalThis.Number(object.pinCount) : undefined,
    };
  },

  toJSON(message: SchedulingGenerationRun): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.school_year !== undefined) {
      obj.schoolYear = ObjectId.toJSON(message.school_year);
    }
    if (message.solver_input_snapshot_id !== undefined) {
      obj.solverInputSnapshotId = Uuid.toJSON(message.solver_input_snapshot_id);
    }
    if (message.schema_version !== undefined) {
      obj.schemaVersion = Math.round(message.schema_version);
    }
    if (message.status !== undefined) {
      obj.status = schedulingGenerationRunStatusToJSON(message.status);
    }
    if (message.started_at !== undefined) {
      obj.startedAt = message.started_at;
    }
    if (message.finished_at !== undefined) {
      obj.finishedAt = message.finished_at;
    }
    if (message.error_message !== undefined) {
      obj.errorMessage = message.error_message;
    }
    if (message.blockers?.length) {
      obj.blockers = message.blockers.map((e) => SchedulingGenerationBlocker.toJSON(e));
    }
    if (message.generated_schedule_id !== undefined) {
      obj.generatedScheduleId = ObjectId.toJSON(message.generated_schedule_id);
    }
    if (message.created_by !== undefined) {
      obj.createdBy = ObjectId.toJSON(message.created_by);
    }
    if (message.created_at !== undefined) {
      obj.createdAt = message.created_at;
    }
    if (message.solver_reasons?.length) {
      obj.solverReasons = message.solver_reasons;
    }
    if (message.purpose !== undefined) {
      obj.purpose = schedulingGenerationPurposeToJSON(message.purpose);
    }
    if (message.source_working_revision_id !== undefined) {
      obj.sourceWorkingRevisionId = Uuid.toJSON(message.source_working_revision_id);
    }
    if (message.pin_count !== undefined) {
      obj.pinCount = Math.round(message.pin_count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingGenerationRun>, I>>(base?: I): SchedulingGenerationRun {
    return SchedulingGenerationRun.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingGenerationRun>, I>>(object: I): SchedulingGenerationRun {
    const message = createBaseSchedulingGenerationRun();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.school_year = (object.school_year !== undefined && object.school_year !== null)
      ? ObjectId.fromPartial(object.school_year)
      : undefined;
    message.solver_input_snapshot_id =
      (object.solver_input_snapshot_id !== undefined && object.solver_input_snapshot_id !== null)
        ? Uuid.fromPartial(object.solver_input_snapshot_id)
        : undefined;
    message.schema_version = object.schema_version ?? undefined;
    message.status = object.status ?? undefined;
    message.started_at = object.started_at ?? undefined;
    message.finished_at = object.finished_at ?? undefined;
    message.error_message = object.error_message ?? undefined;
    message.blockers = object.blockers?.map((e) => SchedulingGenerationBlocker.fromPartial(e)) || [];
    message.generated_schedule_id =
      (object.generated_schedule_id !== undefined && object.generated_schedule_id !== null)
        ? ObjectId.fromPartial(object.generated_schedule_id)
        : undefined;
    message.created_by = (object.created_by !== undefined && object.created_by !== null)
      ? ObjectId.fromPartial(object.created_by)
      : undefined;
    message.created_at = object.created_at ?? undefined;
    message.solver_reasons = object.solver_reasons?.map((e) => e) || [];
    message.purpose = object.purpose ?? undefined;
    message.source_working_revision_id =
      (object.source_working_revision_id !== undefined && object.source_working_revision_id !== null)
        ? Uuid.fromPartial(object.source_working_revision_id)
        : undefined;
    message.pin_count = object.pin_count ?? undefined;
    return message;
  },
};

function createBaseSchedulingGenerationRunList(): SchedulingGenerationRunList {
  return { runs: [] };
}

export const SchedulingGenerationRunList: MessageFns<SchedulingGenerationRunList> = {
  encode(message: SchedulingGenerationRunList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.runs) {
      SchedulingGenerationRun.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingGenerationRunList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingGenerationRunList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.runs.push(SchedulingGenerationRun.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingGenerationRunList {
    return {
      runs: globalThis.Array.isArray(object?.runs)
        ? object.runs.map((e: any) => SchedulingGenerationRun.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingGenerationRunList): unknown {
    const obj: any = {};
    if (message.runs?.length) {
      obj.runs = message.runs.map((e) => SchedulingGenerationRun.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingGenerationRunList>, I>>(base?: I): SchedulingGenerationRunList {
    return SchedulingGenerationRunList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingGenerationRunList>, I>>(object: I): SchedulingGenerationRunList {
    const message = createBaseSchedulingGenerationRunList();
    message.runs = object.runs?.map((e) => SchedulingGenerationRun.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingGeneratedScheduleEntry(): SchedulingGeneratedScheduleEntry {
  return {
    class_id: undefined,
    class_type: undefined,
    abstract_course_id: undefined,
    grade_group_id: undefined,
    offering_id: undefined,
    teacher_id: undefined,
    semester_id: undefined,
    day: undefined,
    period_sequence: undefined,
    slot_id: undefined,
    room_id: undefined,
    id: undefined,
  };
}

export const SchedulingGeneratedScheduleEntry: MessageFns<SchedulingGeneratedScheduleEntry> = {
  encode(message: SchedulingGeneratedScheduleEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.class_id !== undefined) {
      writer.uint32(10).string(message.class_id);
    }
    if (message.class_type !== undefined) {
      writer.uint32(18).string(message.class_type);
    }
    if (message.abstract_course_id !== undefined) {
      ObjectId.encode(message.abstract_course_id, writer.uint32(26).fork()).join();
    }
    if (message.grade_group_id !== undefined) {
      Uuid.encode(message.grade_group_id, writer.uint32(34).fork()).join();
    }
    if (message.offering_id !== undefined) {
      ObjectId.encode(message.offering_id, writer.uint32(42).fork()).join();
    }
    if (message.teacher_id !== undefined) {
      ObjectId.encode(message.teacher_id, writer.uint32(50).fork()).join();
    }
    if (message.semester_id !== undefined) {
      ObjectId.encode(message.semester_id, writer.uint32(58).fork()).join();
    }
    if (message.day !== undefined) {
      writer.uint32(64).int32(dayOfWeekToNumber(message.day));
    }
    if (message.period_sequence !== undefined) {
      writer.uint32(72).int32(message.period_sequence);
    }
    if (message.slot_id !== undefined) {
      writer.uint32(82).string(message.slot_id);
    }
    if (message.room_id !== undefined) {
      ObjectId.encode(message.room_id, writer.uint32(90).fork()).join();
    }
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingGeneratedScheduleEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingGeneratedScheduleEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.class_id = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.class_type = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.abstract_course_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.grade_group_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.offering_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.teacher_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.semester_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.day = dayOfWeekFromJSON(reader.int32());
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.period_sequence = reader.int32();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.slot_id = reader.string();
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.room_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingGeneratedScheduleEntry {
    return {
      class_id: isSet(object.classId) ? globalThis.String(object.classId) : undefined,
      class_type: isSet(object.classType) ? globalThis.String(object.classType) : undefined,
      abstract_course_id: isSet(object.abstractCourseId) ? ObjectId.fromJSON(object.abstractCourseId) : undefined,
      grade_group_id: isSet(object.gradeGroupId) ? Uuid.fromJSON(object.gradeGroupId) : undefined,
      offering_id: isSet(object.offeringId) ? ObjectId.fromJSON(object.offeringId) : undefined,
      teacher_id: isSet(object.teacherId) ? ObjectId.fromJSON(object.teacherId) : undefined,
      semester_id: isSet(object.semesterId) ? ObjectId.fromJSON(object.semesterId) : undefined,
      day: isSet(object.day) ? dayOfWeekFromJSON(object.day) : undefined,
      period_sequence: isSet(object.periodSequence) ? globalThis.Number(object.periodSequence) : undefined,
      slot_id: isSet(object.slotId) ? globalThis.String(object.slotId) : undefined,
      room_id: isSet(object.roomId) ? ObjectId.fromJSON(object.roomId) : undefined,
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
    };
  },

  toJSON(message: SchedulingGeneratedScheduleEntry): unknown {
    const obj: any = {};
    if (message.class_id !== undefined) {
      obj.classId = message.class_id;
    }
    if (message.class_type !== undefined) {
      obj.classType = message.class_type;
    }
    if (message.abstract_course_id !== undefined) {
      obj.abstractCourseId = ObjectId.toJSON(message.abstract_course_id);
    }
    if (message.grade_group_id !== undefined) {
      obj.gradeGroupId = Uuid.toJSON(message.grade_group_id);
    }
    if (message.offering_id !== undefined) {
      obj.offeringId = ObjectId.toJSON(message.offering_id);
    }
    if (message.teacher_id !== undefined) {
      obj.teacherId = ObjectId.toJSON(message.teacher_id);
    }
    if (message.semester_id !== undefined) {
      obj.semesterId = ObjectId.toJSON(message.semester_id);
    }
    if (message.day !== undefined) {
      obj.day = dayOfWeekToJSON(message.day);
    }
    if (message.period_sequence !== undefined) {
      obj.periodSequence = Math.round(message.period_sequence);
    }
    if (message.slot_id !== undefined) {
      obj.slotId = message.slot_id;
    }
    if (message.room_id !== undefined) {
      obj.roomId = ObjectId.toJSON(message.room_id);
    }
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingGeneratedScheduleEntry>, I>>(
    base?: I,
  ): SchedulingGeneratedScheduleEntry {
    return SchedulingGeneratedScheduleEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingGeneratedScheduleEntry>, I>>(
    object: I,
  ): SchedulingGeneratedScheduleEntry {
    const message = createBaseSchedulingGeneratedScheduleEntry();
    message.class_id = object.class_id ?? undefined;
    message.class_type = object.class_type ?? undefined;
    message.abstract_course_id = (object.abstract_course_id !== undefined && object.abstract_course_id !== null)
      ? ObjectId.fromPartial(object.abstract_course_id)
      : undefined;
    message.grade_group_id = (object.grade_group_id !== undefined && object.grade_group_id !== null)
      ? Uuid.fromPartial(object.grade_group_id)
      : undefined;
    message.offering_id = (object.offering_id !== undefined && object.offering_id !== null)
      ? ObjectId.fromPartial(object.offering_id)
      : undefined;
    message.teacher_id = (object.teacher_id !== undefined && object.teacher_id !== null)
      ? ObjectId.fromPartial(object.teacher_id)
      : undefined;
    message.semester_id = (object.semester_id !== undefined && object.semester_id !== null)
      ? ObjectId.fromPartial(object.semester_id)
      : undefined;
    message.day = object.day ?? undefined;
    message.period_sequence = object.period_sequence ?? undefined;
    message.slot_id = object.slot_id ?? undefined;
    message.room_id = (object.room_id !== undefined && object.room_id !== null)
      ? ObjectId.fromPartial(object.room_id)
      : undefined;
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    return message;
  },
};

function createBaseSchedulingGeneratedSchedule(): SchedulingGeneratedSchedule {
  return {
    id: undefined,
    organization: undefined,
    scheduling_workspace: undefined,
    school_year: undefined,
    generation_run_id: undefined,
    solver_input_snapshot_id: undefined,
    created_at: undefined,
    name: undefined,
    note: undefined,
    content_hash: undefined,
    updated_by: undefined,
    updated_at: undefined,
  };
}

export const SchedulingGeneratedSchedule: MessageFns<SchedulingGeneratedSchedule> = {
  encode(message: SchedulingGeneratedSchedule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      ObjectId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    if (message.school_year !== undefined) {
      ObjectId.encode(message.school_year, writer.uint32(34).fork()).join();
    }
    if (message.generation_run_id !== undefined) {
      Uuid.encode(message.generation_run_id, writer.uint32(42).fork()).join();
    }
    if (message.solver_input_snapshot_id !== undefined) {
      Uuid.encode(message.solver_input_snapshot_id, writer.uint32(50).fork()).join();
    }
    if (message.created_at !== undefined) {
      writer.uint32(58).string(message.created_at);
    }
    if (message.name !== undefined) {
      writer.uint32(66).string(message.name);
    }
    if (message.note !== undefined) {
      writer.uint32(74).string(message.note);
    }
    if (message.content_hash !== undefined) {
      writer.uint32(82).string(message.content_hash);
    }
    if (message.updated_by !== undefined) {
      ObjectId.encode(message.updated_by, writer.uint32(90).fork()).join();
    }
    if (message.updated_at !== undefined) {
      writer.uint32(98).string(message.updated_at);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingGeneratedSchedule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingGeneratedSchedule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.school_year = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.generation_run_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.solver_input_snapshot_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.created_at = reader.string();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.name = reader.string();
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.note = reader.string();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.content_hash = reader.string();
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.updated_by = ObjectId.decode(reader, reader.uint32());
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.updated_at = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingGeneratedSchedule {
    return {
      id: isSet(object.id) ? ObjectId.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      school_year: isSet(object.schoolYear) ? ObjectId.fromJSON(object.schoolYear) : undefined,
      generation_run_id: isSet(object.generationRunId) ? Uuid.fromJSON(object.generationRunId) : undefined,
      solver_input_snapshot_id: isSet(object.solverInputSnapshotId)
        ? Uuid.fromJSON(object.solverInputSnapshotId)
        : undefined,
      created_at: isSet(object.createdAt) ? globalThis.String(object.createdAt) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      note: isSet(object.note) ? globalThis.String(object.note) : undefined,
      content_hash: isSet(object.contentHash) ? globalThis.String(object.contentHash) : undefined,
      updated_by: isSet(object.updatedBy) ? ObjectId.fromJSON(object.updatedBy) : undefined,
      updated_at: isSet(object.updatedAt) ? globalThis.String(object.updatedAt) : undefined,
    };
  },

  toJSON(message: SchedulingGeneratedSchedule): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = ObjectId.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.school_year !== undefined) {
      obj.schoolYear = ObjectId.toJSON(message.school_year);
    }
    if (message.generation_run_id !== undefined) {
      obj.generationRunId = Uuid.toJSON(message.generation_run_id);
    }
    if (message.solver_input_snapshot_id !== undefined) {
      obj.solverInputSnapshotId = Uuid.toJSON(message.solver_input_snapshot_id);
    }
    if (message.created_at !== undefined) {
      obj.createdAt = message.created_at;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.note !== undefined) {
      obj.note = message.note;
    }
    if (message.content_hash !== undefined) {
      obj.contentHash = message.content_hash;
    }
    if (message.updated_by !== undefined) {
      obj.updatedBy = ObjectId.toJSON(message.updated_by);
    }
    if (message.updated_at !== undefined) {
      obj.updatedAt = message.updated_at;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingGeneratedSchedule>, I>>(base?: I): SchedulingGeneratedSchedule {
    return SchedulingGeneratedSchedule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingGeneratedSchedule>, I>>(object: I): SchedulingGeneratedSchedule {
    const message = createBaseSchedulingGeneratedSchedule();
    message.id = (object.id !== undefined && object.id !== null) ? ObjectId.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.school_year = (object.school_year !== undefined && object.school_year !== null)
      ? ObjectId.fromPartial(object.school_year)
      : undefined;
    message.generation_run_id = (object.generation_run_id !== undefined && object.generation_run_id !== null)
      ? Uuid.fromPartial(object.generation_run_id)
      : undefined;
    message.solver_input_snapshot_id =
      (object.solver_input_snapshot_id !== undefined && object.solver_input_snapshot_id !== null)
        ? Uuid.fromPartial(object.solver_input_snapshot_id)
        : undefined;
    message.created_at = object.created_at ?? undefined;
    message.name = object.name ?? undefined;
    message.note = object.note ?? undefined;
    message.content_hash = object.content_hash ?? undefined;
    message.updated_by = (object.updated_by !== undefined && object.updated_by !== null)
      ? ObjectId.fromPartial(object.updated_by)
      : undefined;
    message.updated_at = object.updated_at ?? undefined;
    return message;
  },
};

function createBaseSchedulingWorkingSchedule(): SchedulingWorkingSchedule {
  return {
    id: undefined,
    organization: undefined,
    scheduling_workspace: undefined,
    school_year: undefined,
    base_generated_schedule_id: undefined,
    validation_snapshot_id: undefined,
    current_revision_id: undefined,
    version: undefined,
    created_by: undefined,
    created_at: undefined,
    updated_by: undefined,
    updated_at: undefined,
  };
}

export const SchedulingWorkingSchedule: MessageFns<SchedulingWorkingSchedule> = {
  encode(message: SchedulingWorkingSchedule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization !== undefined) {
      ObjectId.encode(message.organization, writer.uint32(18).fork()).join();
    }
    if (message.scheduling_workspace !== undefined) {
      Uuid.encode(message.scheduling_workspace, writer.uint32(26).fork()).join();
    }
    if (message.school_year !== undefined) {
      ObjectId.encode(message.school_year, writer.uint32(34).fork()).join();
    }
    if (message.base_generated_schedule_id !== undefined) {
      ObjectId.encode(message.base_generated_schedule_id, writer.uint32(42).fork()).join();
    }
    if (message.validation_snapshot_id !== undefined) {
      Uuid.encode(message.validation_snapshot_id, writer.uint32(50).fork()).join();
    }
    if (message.current_revision_id !== undefined) {
      Uuid.encode(message.current_revision_id, writer.uint32(58).fork()).join();
    }
    if (message.version !== undefined) {
      writer.uint32(64).uint32(message.version);
    }
    if (message.created_by !== undefined) {
      ObjectId.encode(message.created_by, writer.uint32(74).fork()).join();
    }
    if (message.created_at !== undefined) {
      writer.uint32(82).string(message.created_at);
    }
    if (message.updated_by !== undefined) {
      ObjectId.encode(message.updated_by, writer.uint32(90).fork()).join();
    }
    if (message.updated_at !== undefined) {
      writer.uint32(98).string(message.updated_at);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingWorkingSchedule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingWorkingSchedule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.scheduling_workspace = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.school_year = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.base_generated_schedule_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.validation_snapshot_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.current_revision_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.version = reader.uint32();
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.created_by = ObjectId.decode(reader, reader.uint32());
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.created_at = reader.string();
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.updated_by = ObjectId.decode(reader, reader.uint32());
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.updated_at = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingWorkingSchedule {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      organization: isSet(object.organization) ? ObjectId.fromJSON(object.organization) : undefined,
      scheduling_workspace: isSet(object.schedulingWorkspace) ? Uuid.fromJSON(object.schedulingWorkspace) : undefined,
      school_year: isSet(object.schoolYear) ? ObjectId.fromJSON(object.schoolYear) : undefined,
      base_generated_schedule_id: isSet(object.baseGeneratedScheduleId)
        ? ObjectId.fromJSON(object.baseGeneratedScheduleId)
        : undefined,
      validation_snapshot_id: isSet(object.validationSnapshotId)
        ? Uuid.fromJSON(object.validationSnapshotId)
        : undefined,
      current_revision_id: isSet(object.currentRevisionId) ? Uuid.fromJSON(object.currentRevisionId) : undefined,
      version: isSet(object.version) ? globalThis.Number(object.version) : undefined,
      created_by: isSet(object.createdBy) ? ObjectId.fromJSON(object.createdBy) : undefined,
      created_at: isSet(object.createdAt) ? globalThis.String(object.createdAt) : undefined,
      updated_by: isSet(object.updatedBy) ? ObjectId.fromJSON(object.updatedBy) : undefined,
      updated_at: isSet(object.updatedAt) ? globalThis.String(object.updatedAt) : undefined,
    };
  },

  toJSON(message: SchedulingWorkingSchedule): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.organization !== undefined) {
      obj.organization = ObjectId.toJSON(message.organization);
    }
    if (message.scheduling_workspace !== undefined) {
      obj.schedulingWorkspace = Uuid.toJSON(message.scheduling_workspace);
    }
    if (message.school_year !== undefined) {
      obj.schoolYear = ObjectId.toJSON(message.school_year);
    }
    if (message.base_generated_schedule_id !== undefined) {
      obj.baseGeneratedScheduleId = ObjectId.toJSON(message.base_generated_schedule_id);
    }
    if (message.validation_snapshot_id !== undefined) {
      obj.validationSnapshotId = Uuid.toJSON(message.validation_snapshot_id);
    }
    if (message.current_revision_id !== undefined) {
      obj.currentRevisionId = Uuid.toJSON(message.current_revision_id);
    }
    if (message.version !== undefined) {
      obj.version = Math.round(message.version);
    }
    if (message.created_by !== undefined) {
      obj.createdBy = ObjectId.toJSON(message.created_by);
    }
    if (message.created_at !== undefined) {
      obj.createdAt = message.created_at;
    }
    if (message.updated_by !== undefined) {
      obj.updatedBy = ObjectId.toJSON(message.updated_by);
    }
    if (message.updated_at !== undefined) {
      obj.updatedAt = message.updated_at;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingWorkingSchedule>, I>>(base?: I): SchedulingWorkingSchedule {
    return SchedulingWorkingSchedule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingWorkingSchedule>, I>>(object: I): SchedulingWorkingSchedule {
    const message = createBaseSchedulingWorkingSchedule();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.organization = (object.organization !== undefined && object.organization !== null)
      ? ObjectId.fromPartial(object.organization)
      : undefined;
    message.scheduling_workspace = (object.scheduling_workspace !== undefined && object.scheduling_workspace !== null)
      ? Uuid.fromPartial(object.scheduling_workspace)
      : undefined;
    message.school_year = (object.school_year !== undefined && object.school_year !== null)
      ? ObjectId.fromPartial(object.school_year)
      : undefined;
    message.base_generated_schedule_id =
      (object.base_generated_schedule_id !== undefined && object.base_generated_schedule_id !== null)
        ? ObjectId.fromPartial(object.base_generated_schedule_id)
        : undefined;
    message.validation_snapshot_id =
      (object.validation_snapshot_id !== undefined && object.validation_snapshot_id !== null)
        ? Uuid.fromPartial(object.validation_snapshot_id)
        : undefined;
    message.current_revision_id = (object.current_revision_id !== undefined && object.current_revision_id !== null)
      ? Uuid.fromPartial(object.current_revision_id)
      : undefined;
    message.version = object.version ?? undefined;
    message.created_by = (object.created_by !== undefined && object.created_by !== null)
      ? ObjectId.fromPartial(object.created_by)
      : undefined;
    message.created_at = object.created_at ?? undefined;
    message.updated_by = (object.updated_by !== undefined && object.updated_by !== null)
      ? ObjectId.fromPartial(object.updated_by)
      : undefined;
    message.updated_at = object.updated_at ?? undefined;
    return message;
  },
};

function createBaseSchedulingScheduleRevision(): SchedulingScheduleRevision {
  return {
    id: undefined,
    working_schedule_id: undefined,
    base_generated_schedule_id: undefined,
    validation_snapshot_id: undefined,
    kind: undefined,
    sequence: undefined,
    summary: undefined,
    label: undefined,
    created_by: undefined,
    created_at: undefined,
    copied_from_revision_id: undefined,
    undone_revision_id: undefined,
  };
}

export const SchedulingScheduleRevision: MessageFns<SchedulingScheduleRevision> = {
  encode(message: SchedulingScheduleRevision, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.working_schedule_id !== undefined) {
      Uuid.encode(message.working_schedule_id, writer.uint32(18).fork()).join();
    }
    if (message.base_generated_schedule_id !== undefined) {
      ObjectId.encode(message.base_generated_schedule_id, writer.uint32(34).fork()).join();
    }
    if (message.validation_snapshot_id !== undefined) {
      Uuid.encode(message.validation_snapshot_id, writer.uint32(42).fork()).join();
    }
    if (message.kind !== undefined) {
      writer.uint32(48).int32(schedulingScheduleRevisionKindToNumber(message.kind));
    }
    if (message.sequence !== undefined) {
      writer.uint32(56).uint32(message.sequence);
    }
    if (message.summary !== undefined) {
      writer.uint32(66).string(message.summary);
    }
    if (message.label !== undefined) {
      writer.uint32(74).string(message.label);
    }
    if (message.created_by !== undefined) {
      ObjectId.encode(message.created_by, writer.uint32(82).fork()).join();
    }
    if (message.created_at !== undefined) {
      writer.uint32(90).string(message.created_at);
    }
    if (message.copied_from_revision_id !== undefined) {
      Uuid.encode(message.copied_from_revision_id, writer.uint32(98).fork()).join();
    }
    if (message.undone_revision_id !== undefined) {
      Uuid.encode(message.undone_revision_id, writer.uint32(106).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleRevision {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleRevision();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.working_schedule_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.base_generated_schedule_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.validation_snapshot_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.kind = schedulingScheduleRevisionKindFromJSON(reader.int32());
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.sequence = reader.uint32();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.summary = reader.string();
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.label = reader.string();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.created_by = ObjectId.decode(reader, reader.uint32());
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.created_at = reader.string();
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.copied_from_revision_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.undone_revision_id = Uuid.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleRevision {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      working_schedule_id: isSet(object.workingScheduleId) ? Uuid.fromJSON(object.workingScheduleId) : undefined,
      base_generated_schedule_id: isSet(object.baseGeneratedScheduleId)
        ? ObjectId.fromJSON(object.baseGeneratedScheduleId)
        : undefined,
      validation_snapshot_id: isSet(object.validationSnapshotId)
        ? Uuid.fromJSON(object.validationSnapshotId)
        : undefined,
      kind: isSet(object.kind) ? schedulingScheduleRevisionKindFromJSON(object.kind) : undefined,
      sequence: isSet(object.sequence) ? globalThis.Number(object.sequence) : undefined,
      summary: isSet(object.summary) ? globalThis.String(object.summary) : undefined,
      label: isSet(object.label) ? globalThis.String(object.label) : undefined,
      created_by: isSet(object.createdBy) ? ObjectId.fromJSON(object.createdBy) : undefined,
      created_at: isSet(object.createdAt) ? globalThis.String(object.createdAt) : undefined,
      copied_from_revision_id: isSet(object.copiedFromRevisionId)
        ? Uuid.fromJSON(object.copiedFromRevisionId)
        : undefined,
      undone_revision_id: isSet(object.undoneRevisionId) ? Uuid.fromJSON(object.undoneRevisionId) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleRevision): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.working_schedule_id !== undefined) {
      obj.workingScheduleId = Uuid.toJSON(message.working_schedule_id);
    }
    if (message.base_generated_schedule_id !== undefined) {
      obj.baseGeneratedScheduleId = ObjectId.toJSON(message.base_generated_schedule_id);
    }
    if (message.validation_snapshot_id !== undefined) {
      obj.validationSnapshotId = Uuid.toJSON(message.validation_snapshot_id);
    }
    if (message.kind !== undefined) {
      obj.kind = schedulingScheduleRevisionKindToJSON(message.kind);
    }
    if (message.sequence !== undefined) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.summary !== undefined) {
      obj.summary = message.summary;
    }
    if (message.label !== undefined) {
      obj.label = message.label;
    }
    if (message.created_by !== undefined) {
      obj.createdBy = ObjectId.toJSON(message.created_by);
    }
    if (message.created_at !== undefined) {
      obj.createdAt = message.created_at;
    }
    if (message.copied_from_revision_id !== undefined) {
      obj.copiedFromRevisionId = Uuid.toJSON(message.copied_from_revision_id);
    }
    if (message.undone_revision_id !== undefined) {
      obj.undoneRevisionId = Uuid.toJSON(message.undone_revision_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleRevision>, I>>(base?: I): SchedulingScheduleRevision {
    return SchedulingScheduleRevision.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleRevision>, I>>(object: I): SchedulingScheduleRevision {
    const message = createBaseSchedulingScheduleRevision();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.working_schedule_id = (object.working_schedule_id !== undefined && object.working_schedule_id !== null)
      ? Uuid.fromPartial(object.working_schedule_id)
      : undefined;
    message.base_generated_schedule_id =
      (object.base_generated_schedule_id !== undefined && object.base_generated_schedule_id !== null)
        ? ObjectId.fromPartial(object.base_generated_schedule_id)
        : undefined;
    message.validation_snapshot_id =
      (object.validation_snapshot_id !== undefined && object.validation_snapshot_id !== null)
        ? Uuid.fromPartial(object.validation_snapshot_id)
        : undefined;
    message.kind = object.kind ?? undefined;
    message.sequence = object.sequence ?? undefined;
    message.summary = object.summary ?? undefined;
    message.label = object.label ?? undefined;
    message.created_by = (object.created_by !== undefined && object.created_by !== null)
      ? ObjectId.fromPartial(object.created_by)
      : undefined;
    message.created_at = object.created_at ?? undefined;
    message.copied_from_revision_id =
      (object.copied_from_revision_id !== undefined && object.copied_from_revision_id !== null)
        ? Uuid.fromPartial(object.copied_from_revision_id)
        : undefined;
    message.undone_revision_id = (object.undone_revision_id !== undefined && object.undone_revision_id !== null)
      ? Uuid.fromPartial(object.undone_revision_id)
      : undefined;
    return message;
  },
};

function createBaseSchedulingScheduleReference(): SchedulingScheduleReference {
  return { generated_schedule_id: undefined, working_revision_id: undefined };
}

export const SchedulingScheduleReference: MessageFns<SchedulingScheduleReference> = {
  encode(message: SchedulingScheduleReference, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.generated_schedule_id !== undefined) {
      ObjectId.encode(message.generated_schedule_id, writer.uint32(10).fork()).join();
    }
    if (message.working_revision_id !== undefined) {
      Uuid.encode(message.working_revision_id, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleReference {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleReference();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.generated_schedule_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.working_revision_id = Uuid.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleReference {
    return {
      generated_schedule_id: isSet(object.generatedScheduleId)
        ? ObjectId.fromJSON(object.generatedScheduleId)
        : undefined,
      working_revision_id: isSet(object.workingRevisionId) ? Uuid.fromJSON(object.workingRevisionId) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleReference): unknown {
    const obj: any = {};
    if (message.generated_schedule_id !== undefined) {
      obj.generatedScheduleId = ObjectId.toJSON(message.generated_schedule_id);
    }
    if (message.working_revision_id !== undefined) {
      obj.workingRevisionId = Uuid.toJSON(message.working_revision_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleReference>, I>>(base?: I): SchedulingScheduleReference {
    return SchedulingScheduleReference.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleReference>, I>>(object: I): SchedulingScheduleReference {
    const message = createBaseSchedulingScheduleReference();
    message.generated_schedule_id =
      (object.generated_schedule_id !== undefined && object.generated_schedule_id !== null)
        ? ObjectId.fromPartial(object.generated_schedule_id)
        : undefined;
    message.working_revision_id = (object.working_revision_id !== undefined && object.working_revision_id !== null)
      ? Uuid.fromPartial(object.working_revision_id)
      : undefined;
    return message;
  },
};

function createBaseSchedulingScheduleSource(): SchedulingScheduleSource {
  return {
    reference: undefined,
    generated_schedule: undefined,
    working_schedule: undefined,
    working_revision: undefined,
  };
}

export const SchedulingScheduleSource: MessageFns<SchedulingScheduleSource> = {
  encode(message: SchedulingScheduleSource, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reference !== undefined) {
      SchedulingScheduleReference.encode(message.reference, writer.uint32(10).fork()).join();
    }
    if (message.generated_schedule !== undefined) {
      SchedulingGeneratedSchedule.encode(message.generated_schedule, writer.uint32(18).fork()).join();
    }
    if (message.working_schedule !== undefined) {
      SchedulingWorkingSchedule.encode(message.working_schedule, writer.uint32(26).fork()).join();
    }
    if (message.working_revision !== undefined) {
      SchedulingScheduleRevision.encode(message.working_revision, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleSource {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleSource();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.reference = SchedulingScheduleReference.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.generated_schedule = SchedulingGeneratedSchedule.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.working_schedule = SchedulingWorkingSchedule.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.working_revision = SchedulingScheduleRevision.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleSource {
    return {
      reference: isSet(object.reference) ? SchedulingScheduleReference.fromJSON(object.reference) : undefined,
      generated_schedule: isSet(object.generatedSchedule)
        ? SchedulingGeneratedSchedule.fromJSON(object.generatedSchedule)
        : undefined,
      working_schedule: isSet(object.workingSchedule)
        ? SchedulingWorkingSchedule.fromJSON(object.workingSchedule)
        : undefined,
      working_revision: isSet(object.workingRevision)
        ? SchedulingScheduleRevision.fromJSON(object.workingRevision)
        : undefined,
    };
  },

  toJSON(message: SchedulingScheduleSource): unknown {
    const obj: any = {};
    if (message.reference !== undefined) {
      obj.reference = SchedulingScheduleReference.toJSON(message.reference);
    }
    if (message.generated_schedule !== undefined) {
      obj.generatedSchedule = SchedulingGeneratedSchedule.toJSON(message.generated_schedule);
    }
    if (message.working_schedule !== undefined) {
      obj.workingSchedule = SchedulingWorkingSchedule.toJSON(message.working_schedule);
    }
    if (message.working_revision !== undefined) {
      obj.workingRevision = SchedulingScheduleRevision.toJSON(message.working_revision);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleSource>, I>>(base?: I): SchedulingScheduleSource {
    return SchedulingScheduleSource.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleSource>, I>>(object: I): SchedulingScheduleSource {
    const message = createBaseSchedulingScheduleSource();
    message.reference = (object.reference !== undefined && object.reference !== null)
      ? SchedulingScheduleReference.fromPartial(object.reference)
      : undefined;
    message.generated_schedule = (object.generated_schedule !== undefined && object.generated_schedule !== null)
      ? SchedulingGeneratedSchedule.fromPartial(object.generated_schedule)
      : undefined;
    message.working_schedule = (object.working_schedule !== undefined && object.working_schedule !== null)
      ? SchedulingWorkingSchedule.fromPartial(object.working_schedule)
      : undefined;
    message.working_revision = (object.working_revision !== undefined && object.working_revision !== null)
      ? SchedulingScheduleRevision.fromPartial(object.working_revision)
      : undefined;
    return message;
  },
};

function createBaseSchedulingGeneratedScheduleSummary(): SchedulingGeneratedScheduleSummary {
  return {
    schedule: undefined,
    input_hash: undefined,
    setup_compatibility: undefined,
    is_working_base: undefined,
    placement_count: undefined,
    duplicate_of_schedule_id: undefined,
  };
}

export const SchedulingGeneratedScheduleSummary: MessageFns<SchedulingGeneratedScheduleSummary> = {
  encode(message: SchedulingGeneratedScheduleSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.schedule !== undefined) {
      SchedulingGeneratedSchedule.encode(message.schedule, writer.uint32(10).fork()).join();
    }
    if (message.input_hash !== undefined) {
      writer.uint32(18).string(message.input_hash);
    }
    if (message.setup_compatibility !== undefined) {
      writer.uint32(24).int32(schedulingScheduleSetupCompatibilityToNumber(message.setup_compatibility));
    }
    if (message.is_working_base !== undefined) {
      writer.uint32(32).bool(message.is_working_base);
    }
    if (message.placement_count !== undefined) {
      writer.uint32(40).uint32(message.placement_count);
    }
    if (message.duplicate_of_schedule_id !== undefined) {
      ObjectId.encode(message.duplicate_of_schedule_id, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingGeneratedScheduleSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingGeneratedScheduleSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.schedule = SchedulingGeneratedSchedule.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.input_hash = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.setup_compatibility = schedulingScheduleSetupCompatibilityFromJSON(reader.int32());
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.is_working_base = reader.bool();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.placement_count = reader.uint32();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.duplicate_of_schedule_id = ObjectId.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingGeneratedScheduleSummary {
    return {
      schedule: isSet(object.schedule) ? SchedulingGeneratedSchedule.fromJSON(object.schedule) : undefined,
      input_hash: isSet(object.inputHash) ? globalThis.String(object.inputHash) : undefined,
      setup_compatibility: isSet(object.setupCompatibility)
        ? schedulingScheduleSetupCompatibilityFromJSON(object.setupCompatibility)
        : undefined,
      is_working_base: isSet(object.isWorkingBase) ? globalThis.Boolean(object.isWorkingBase) : undefined,
      placement_count: isSet(object.placementCount) ? globalThis.Number(object.placementCount) : undefined,
      duplicate_of_schedule_id: isSet(object.duplicateOfScheduleId)
        ? ObjectId.fromJSON(object.duplicateOfScheduleId)
        : undefined,
    };
  },

  toJSON(message: SchedulingGeneratedScheduleSummary): unknown {
    const obj: any = {};
    if (message.schedule !== undefined) {
      obj.schedule = SchedulingGeneratedSchedule.toJSON(message.schedule);
    }
    if (message.input_hash !== undefined) {
      obj.inputHash = message.input_hash;
    }
    if (message.setup_compatibility !== undefined) {
      obj.setupCompatibility = schedulingScheduleSetupCompatibilityToJSON(message.setup_compatibility);
    }
    if (message.is_working_base !== undefined) {
      obj.isWorkingBase = message.is_working_base;
    }
    if (message.placement_count !== undefined) {
      obj.placementCount = Math.round(message.placement_count);
    }
    if (message.duplicate_of_schedule_id !== undefined) {
      obj.duplicateOfScheduleId = ObjectId.toJSON(message.duplicate_of_schedule_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingGeneratedScheduleSummary>, I>>(
    base?: I,
  ): SchedulingGeneratedScheduleSummary {
    return SchedulingGeneratedScheduleSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingGeneratedScheduleSummary>, I>>(
    object: I,
  ): SchedulingGeneratedScheduleSummary {
    const message = createBaseSchedulingGeneratedScheduleSummary();
    message.schedule = (object.schedule !== undefined && object.schedule !== null)
      ? SchedulingGeneratedSchedule.fromPartial(object.schedule)
      : undefined;
    message.input_hash = object.input_hash ?? undefined;
    message.setup_compatibility = object.setup_compatibility ?? undefined;
    message.is_working_base = object.is_working_base ?? undefined;
    message.placement_count = object.placement_count ?? undefined;
    message.duplicate_of_schedule_id =
      (object.duplicate_of_schedule_id !== undefined && object.duplicate_of_schedule_id !== null)
        ? ObjectId.fromPartial(object.duplicate_of_schedule_id)
        : undefined;
    return message;
  },
};

function createBaseSchedulingGeneratedSchedulePage(): SchedulingGeneratedSchedulePage {
  return { schedules: [], next_cursor: undefined };
}

export const SchedulingGeneratedSchedulePage: MessageFns<SchedulingGeneratedSchedulePage> = {
  encode(message: SchedulingGeneratedSchedulePage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.schedules) {
      SchedulingGeneratedScheduleSummary.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.next_cursor !== undefined) {
      writer.uint32(18).string(message.next_cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingGeneratedSchedulePage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingGeneratedSchedulePage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.schedules.push(SchedulingGeneratedScheduleSummary.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.next_cursor = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingGeneratedSchedulePage {
    return {
      schedules: globalThis.Array.isArray(object?.schedules)
        ? object.schedules.map((e: any) => SchedulingGeneratedScheduleSummary.fromJSON(e))
        : [],
      next_cursor: isSet(object.nextCursor) ? globalThis.String(object.nextCursor) : undefined,
    };
  },

  toJSON(message: SchedulingGeneratedSchedulePage): unknown {
    const obj: any = {};
    if (message.schedules?.length) {
      obj.schedules = message.schedules.map((e) => SchedulingGeneratedScheduleSummary.toJSON(e));
    }
    if (message.next_cursor !== undefined) {
      obj.nextCursor = message.next_cursor;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingGeneratedSchedulePage>, I>>(base?: I): SchedulingGeneratedSchedulePage {
    return SchedulingGeneratedSchedulePage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingGeneratedSchedulePage>, I>>(
    object: I,
  ): SchedulingGeneratedSchedulePage {
    const message = createBaseSchedulingGeneratedSchedulePage();
    message.schedules = object.schedules?.map((e) => SchedulingGeneratedScheduleSummary.fromPartial(e)) || [];
    message.next_cursor = object.next_cursor ?? undefined;
    return message;
  },
};

function createBaseSchedulingScheduleRevisionPage(): SchedulingScheduleRevisionPage {
  return { revisions: [], next_cursor: undefined };
}

export const SchedulingScheduleRevisionPage: MessageFns<SchedulingScheduleRevisionPage> = {
  encode(message: SchedulingScheduleRevisionPage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.revisions) {
      SchedulingScheduleRevision.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.next_cursor !== undefined) {
      writer.uint32(18).string(message.next_cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleRevisionPage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleRevisionPage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.revisions.push(SchedulingScheduleRevision.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.next_cursor = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleRevisionPage {
    return {
      revisions: globalThis.Array.isArray(object?.revisions)
        ? object.revisions.map((e: any) => SchedulingScheduleRevision.fromJSON(e))
        : [],
      next_cursor: isSet(object.nextCursor) ? globalThis.String(object.nextCursor) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleRevisionPage): unknown {
    const obj: any = {};
    if (message.revisions?.length) {
      obj.revisions = message.revisions.map((e) => SchedulingScheduleRevision.toJSON(e));
    }
    if (message.next_cursor !== undefined) {
      obj.nextCursor = message.next_cursor;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleRevisionPage>, I>>(base?: I): SchedulingScheduleRevisionPage {
    return SchedulingScheduleRevisionPage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleRevisionPage>, I>>(
    object: I,
  ): SchedulingScheduleRevisionPage {
    const message = createBaseSchedulingScheduleRevisionPage();
    message.revisions = object.revisions?.map((e) => SchedulingScheduleRevision.fromPartial(e)) || [];
    message.next_cursor = object.next_cursor ?? undefined;
    return message;
  },
};

function createBaseSchedulingScheduleCompatibilityIssue(): SchedulingScheduleCompatibilityIssue {
  return { code: undefined, message: undefined };
}

export const SchedulingScheduleCompatibilityIssue: MessageFns<SchedulingScheduleCompatibilityIssue> = {
  encode(message: SchedulingScheduleCompatibilityIssue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== undefined) {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== undefined) {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleCompatibilityIssue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleCompatibilityIssue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleCompatibilityIssue {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : undefined,
      message: isSet(object.message) ? globalThis.String(object.message) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleCompatibilityIssue): unknown {
    const obj: any = {};
    if (message.code !== undefined) {
      obj.code = message.code;
    }
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleCompatibilityIssue>, I>>(
    base?: I,
  ): SchedulingScheduleCompatibilityIssue {
    return SchedulingScheduleCompatibilityIssue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleCompatibilityIssue>, I>>(
    object: I,
  ): SchedulingScheduleCompatibilityIssue {
    const message = createBaseSchedulingScheduleCompatibilityIssue();
    message.code = object.code ?? undefined;
    message.message = object.message ?? undefined;
    return message;
  },
};

function createBaseSchedulingScheduleSubjectMemberInfo(): SchedulingScheduleSubjectMemberInfo {
  return {
    class_group_id: undefined,
    abstract_course_id: undefined,
    group_label: undefined,
    subject_name: undefined,
    color: undefined,
    grade: undefined,
  };
}

export const SchedulingScheduleSubjectMemberInfo: MessageFns<SchedulingScheduleSubjectMemberInfo> = {
  encode(message: SchedulingScheduleSubjectMemberInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.class_group_id !== undefined) {
      Uuid.encode(message.class_group_id, writer.uint32(10).fork()).join();
    }
    if (message.abstract_course_id !== undefined) {
      ObjectId.encode(message.abstract_course_id, writer.uint32(18).fork()).join();
    }
    if (message.group_label !== undefined) {
      writer.uint32(26).string(message.group_label);
    }
    if (message.subject_name !== undefined) {
      writer.uint32(34).string(message.subject_name);
    }
    if (message.color !== undefined) {
      writer.uint32(42).string(message.color);
    }
    if (message.grade !== undefined) {
      writer.uint32(48).int32(studentGradeToNumber(message.grade));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleSubjectMemberInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleSubjectMemberInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.class_group_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.abstract_course_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.group_label = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.subject_name = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.color = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.grade = studentGradeFromJSON(reader.int32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleSubjectMemberInfo {
    return {
      class_group_id: isSet(object.classGroupId) ? Uuid.fromJSON(object.classGroupId) : undefined,
      abstract_course_id: isSet(object.abstractCourseId) ? ObjectId.fromJSON(object.abstractCourseId) : undefined,
      group_label: isSet(object.groupLabel) ? globalThis.String(object.groupLabel) : undefined,
      subject_name: isSet(object.subjectName) ? globalThis.String(object.subjectName) : undefined,
      color: isSet(object.color) ? globalThis.String(object.color) : undefined,
      grade: isSet(object.grade) ? studentGradeFromJSON(object.grade) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleSubjectMemberInfo): unknown {
    const obj: any = {};
    if (message.class_group_id !== undefined) {
      obj.classGroupId = Uuid.toJSON(message.class_group_id);
    }
    if (message.abstract_course_id !== undefined) {
      obj.abstractCourseId = ObjectId.toJSON(message.abstract_course_id);
    }
    if (message.group_label !== undefined) {
      obj.groupLabel = message.group_label;
    }
    if (message.subject_name !== undefined) {
      obj.subjectName = message.subject_name;
    }
    if (message.color !== undefined) {
      obj.color = message.color;
    }
    if (message.grade !== undefined) {
      obj.grade = studentGradeToJSON(message.grade);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleSubjectMemberInfo>, I>>(
    base?: I,
  ): SchedulingScheduleSubjectMemberInfo {
    return SchedulingScheduleSubjectMemberInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleSubjectMemberInfo>, I>>(
    object: I,
  ): SchedulingScheduleSubjectMemberInfo {
    const message = createBaseSchedulingScheduleSubjectMemberInfo();
    message.class_group_id = (object.class_group_id !== undefined && object.class_group_id !== null)
      ? Uuid.fromPartial(object.class_group_id)
      : undefined;
    message.abstract_course_id = (object.abstract_course_id !== undefined && object.abstract_course_id !== null)
      ? ObjectId.fromPartial(object.abstract_course_id)
      : undefined;
    message.group_label = object.group_label ?? undefined;
    message.subject_name = object.subject_name ?? undefined;
    message.color = object.color ?? undefined;
    message.grade = object.grade ?? undefined;
    return message;
  },
};

function createBaseSchedulingScheduleClassInfo(): SchedulingScheduleClassInfo {
  return {
    class_id: undefined,
    name: undefined,
    class_type: undefined,
    group_label: undefined,
    periods_per_week: undefined,
    student_ids: [],
    color: undefined,
    subject_members: [],
    shared_lesson_id: undefined,
  };
}

export const SchedulingScheduleClassInfo: MessageFns<SchedulingScheduleClassInfo> = {
  encode(message: SchedulingScheduleClassInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.class_id !== undefined) {
      writer.uint32(10).string(message.class_id);
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    if (message.class_type !== undefined) {
      writer.uint32(26).string(message.class_type);
    }
    if (message.group_label !== undefined) {
      writer.uint32(34).string(message.group_label);
    }
    if (message.periods_per_week !== undefined) {
      writer.uint32(40).uint32(message.periods_per_week);
    }
    for (const v of message.student_ids) {
      ObjectId.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.color !== undefined) {
      writer.uint32(58).string(message.color);
    }
    for (const v of message.subject_members) {
      SchedulingScheduleSubjectMemberInfo.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.shared_lesson_id !== undefined) {
      Uuid.encode(message.shared_lesson_id, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleClassInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleClassInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.class_id = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.class_type = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.group_label = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.periods_per_week = reader.uint32();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.student_ids.push(ObjectId.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.color = reader.string();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.subject_members.push(SchedulingScheduleSubjectMemberInfo.decode(reader, reader.uint32()));
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.shared_lesson_id = Uuid.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleClassInfo {
    return {
      class_id: isSet(object.classId) ? globalThis.String(object.classId) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      class_type: isSet(object.classType) ? globalThis.String(object.classType) : undefined,
      group_label: isSet(object.groupLabel) ? globalThis.String(object.groupLabel) : undefined,
      periods_per_week: isSet(object.periodsPerWeek) ? globalThis.Number(object.periodsPerWeek) : undefined,
      student_ids: globalThis.Array.isArray(object?.studentIds)
        ? object.studentIds.map((e: any) => ObjectId.fromJSON(e))
        : [],
      color: isSet(object.color) ? globalThis.String(object.color) : undefined,
      subject_members: globalThis.Array.isArray(object?.subjectMembers)
        ? object.subjectMembers.map((e: any) => SchedulingScheduleSubjectMemberInfo.fromJSON(e))
        : [],
      shared_lesson_id: isSet(object.sharedLessonId) ? Uuid.fromJSON(object.sharedLessonId) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleClassInfo): unknown {
    const obj: any = {};
    if (message.class_id !== undefined) {
      obj.classId = message.class_id;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.class_type !== undefined) {
      obj.classType = message.class_type;
    }
    if (message.group_label !== undefined) {
      obj.groupLabel = message.group_label;
    }
    if (message.periods_per_week !== undefined) {
      obj.periodsPerWeek = Math.round(message.periods_per_week);
    }
    if (message.student_ids?.length) {
      obj.studentIds = message.student_ids.map((e) => ObjectId.toJSON(e));
    }
    if (message.color !== undefined) {
      obj.color = message.color;
    }
    if (message.subject_members?.length) {
      obj.subjectMembers = message.subject_members.map((e) => SchedulingScheduleSubjectMemberInfo.toJSON(e));
    }
    if (message.shared_lesson_id !== undefined) {
      obj.sharedLessonId = Uuid.toJSON(message.shared_lesson_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleClassInfo>, I>>(base?: I): SchedulingScheduleClassInfo {
    return SchedulingScheduleClassInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleClassInfo>, I>>(object: I): SchedulingScheduleClassInfo {
    const message = createBaseSchedulingScheduleClassInfo();
    message.class_id = object.class_id ?? undefined;
    message.name = object.name ?? undefined;
    message.class_type = object.class_type ?? undefined;
    message.group_label = object.group_label ?? undefined;
    message.periods_per_week = object.periods_per_week ?? undefined;
    message.student_ids = object.student_ids?.map((e) => ObjectId.fromPartial(e)) || [];
    message.color = object.color ?? undefined;
    message.subject_members = object.subject_members?.map((e) => SchedulingScheduleSubjectMemberInfo.fromPartial(e)) ||
      [];
    message.shared_lesson_id = (object.shared_lesson_id !== undefined && object.shared_lesson_id !== null)
      ? Uuid.fromPartial(object.shared_lesson_id)
      : undefined;
    return message;
  },
};

function createBaseSchedulingScheduleTeacherInfo(): SchedulingScheduleTeacherInfo {
  return { id: undefined, display_name: undefined };
}

export const SchedulingScheduleTeacherInfo: MessageFns<SchedulingScheduleTeacherInfo> = {
  encode(message: SchedulingScheduleTeacherInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      ObjectId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.display_name !== undefined) {
      writer.uint32(18).string(message.display_name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleTeacherInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleTeacherInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.display_name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleTeacherInfo {
    return {
      id: isSet(object.id) ? ObjectId.fromJSON(object.id) : undefined,
      display_name: isSet(object.displayName) ? globalThis.String(object.displayName) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleTeacherInfo): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = ObjectId.toJSON(message.id);
    }
    if (message.display_name !== undefined) {
      obj.displayName = message.display_name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleTeacherInfo>, I>>(base?: I): SchedulingScheduleTeacherInfo {
    return SchedulingScheduleTeacherInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleTeacherInfo>, I>>(
    object: I,
  ): SchedulingScheduleTeacherInfo {
    const message = createBaseSchedulingScheduleTeacherInfo();
    message.id = (object.id !== undefined && object.id !== null) ? ObjectId.fromPartial(object.id) : undefined;
    message.display_name = object.display_name ?? undefined;
    return message;
  },
};

function createBaseSchedulingScheduleSlotInfo(): SchedulingScheduleSlotInfo {
  return {
    slot_id: undefined,
    semester_id: undefined,
    day: undefined,
    period_sequence: undefined,
    period_label: undefined,
    start_time: undefined,
    end_time: undefined,
    is_rest_period: undefined,
  };
}

export const SchedulingScheduleSlotInfo: MessageFns<SchedulingScheduleSlotInfo> = {
  encode(message: SchedulingScheduleSlotInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slot_id !== undefined) {
      writer.uint32(10).string(message.slot_id);
    }
    if (message.semester_id !== undefined) {
      ObjectId.encode(message.semester_id, writer.uint32(18).fork()).join();
    }
    if (message.day !== undefined) {
      writer.uint32(24).int32(dayOfWeekToNumber(message.day));
    }
    if (message.period_sequence !== undefined) {
      writer.uint32(32).int32(message.period_sequence);
    }
    if (message.period_label !== undefined) {
      writer.uint32(42).string(message.period_label);
    }
    if (message.start_time !== undefined) {
      writer.uint32(50).string(message.start_time);
    }
    if (message.end_time !== undefined) {
      writer.uint32(58).string(message.end_time);
    }
    if (message.is_rest_period !== undefined) {
      writer.uint32(64).bool(message.is_rest_period);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleSlotInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleSlotInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.slot_id = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.semester_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.day = dayOfWeekFromJSON(reader.int32());
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.period_sequence = reader.int32();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.period_label = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.start_time = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.end_time = reader.string();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.is_rest_period = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleSlotInfo {
    return {
      slot_id: isSet(object.slotId) ? globalThis.String(object.slotId) : undefined,
      semester_id: isSet(object.semesterId) ? ObjectId.fromJSON(object.semesterId) : undefined,
      day: isSet(object.day) ? dayOfWeekFromJSON(object.day) : undefined,
      period_sequence: isSet(object.periodSequence) ? globalThis.Number(object.periodSequence) : undefined,
      period_label: isSet(object.periodLabel) ? globalThis.String(object.periodLabel) : undefined,
      start_time: isSet(object.startTime) ? globalThis.String(object.startTime) : undefined,
      end_time: isSet(object.endTime) ? globalThis.String(object.endTime) : undefined,
      is_rest_period: isSet(object.isRestPeriod) ? globalThis.Boolean(object.isRestPeriod) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleSlotInfo): unknown {
    const obj: any = {};
    if (message.slot_id !== undefined) {
      obj.slotId = message.slot_id;
    }
    if (message.semester_id !== undefined) {
      obj.semesterId = ObjectId.toJSON(message.semester_id);
    }
    if (message.day !== undefined) {
      obj.day = dayOfWeekToJSON(message.day);
    }
    if (message.period_sequence !== undefined) {
      obj.periodSequence = Math.round(message.period_sequence);
    }
    if (message.period_label !== undefined) {
      obj.periodLabel = message.period_label;
    }
    if (message.start_time !== undefined) {
      obj.startTime = message.start_time;
    }
    if (message.end_time !== undefined) {
      obj.endTime = message.end_time;
    }
    if (message.is_rest_period !== undefined) {
      obj.isRestPeriod = message.is_rest_period;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleSlotInfo>, I>>(base?: I): SchedulingScheduleSlotInfo {
    return SchedulingScheduleSlotInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleSlotInfo>, I>>(object: I): SchedulingScheduleSlotInfo {
    const message = createBaseSchedulingScheduleSlotInfo();
    message.slot_id = object.slot_id ?? undefined;
    message.semester_id = (object.semester_id !== undefined && object.semester_id !== null)
      ? ObjectId.fromPartial(object.semester_id)
      : undefined;
    message.day = object.day ?? undefined;
    message.period_sequence = object.period_sequence ?? undefined;
    message.period_label = object.period_label ?? undefined;
    message.start_time = object.start_time ?? undefined;
    message.end_time = object.end_time ?? undefined;
    message.is_rest_period = object.is_rest_period ?? undefined;
    return message;
  },
};

function createBaseSchedulingScheduleSemesterInfo(): SchedulingScheduleSemesterInfo {
  return { id: undefined, name: undefined, campus_id: undefined, campus_name: undefined };
}

export const SchedulingScheduleSemesterInfo: MessageFns<SchedulingScheduleSemesterInfo> = {
  encode(message: SchedulingScheduleSemesterInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      ObjectId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    if (message.campus_id !== undefined) {
      ObjectId.encode(message.campus_id, writer.uint32(26).fork()).join();
    }
    if (message.campus_name !== undefined) {
      writer.uint32(34).string(message.campus_name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleSemesterInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleSemesterInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.campus_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.campus_name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleSemesterInfo {
    return {
      id: isSet(object.id) ? ObjectId.fromJSON(object.id) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      campus_id: isSet(object.campusId) ? ObjectId.fromJSON(object.campusId) : undefined,
      campus_name: isSet(object.campusName) ? globalThis.String(object.campusName) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleSemesterInfo): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = ObjectId.toJSON(message.id);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.campus_id !== undefined) {
      obj.campusId = ObjectId.toJSON(message.campus_id);
    }
    if (message.campus_name !== undefined) {
      obj.campusName = message.campus_name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleSemesterInfo>, I>>(base?: I): SchedulingScheduleSemesterInfo {
    return SchedulingScheduleSemesterInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleSemesterInfo>, I>>(
    object: I,
  ): SchedulingScheduleSemesterInfo {
    const message = createBaseSchedulingScheduleSemesterInfo();
    message.id = (object.id !== undefined && object.id !== null) ? ObjectId.fromPartial(object.id) : undefined;
    message.name = object.name ?? undefined;
    message.campus_id = (object.campus_id !== undefined && object.campus_id !== null)
      ? ObjectId.fromPartial(object.campus_id)
      : undefined;
    message.campus_name = object.campus_name ?? undefined;
    return message;
  },
};

function createBaseSchedulingScheduleStudentInfo(): SchedulingScheduleStudentInfo {
  return { id: undefined, display_name: undefined, grade: undefined };
}

export const SchedulingScheduleStudentInfo: MessageFns<SchedulingScheduleStudentInfo> = {
  encode(message: SchedulingScheduleStudentInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      ObjectId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.display_name !== undefined) {
      writer.uint32(18).string(message.display_name);
    }
    if (message.grade !== undefined) {
      writer.uint32(24).int32(studentGradeToNumber(message.grade));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleStudentInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleStudentInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.display_name = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.grade = studentGradeFromJSON(reader.int32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleStudentInfo {
    return {
      id: isSet(object.id) ? ObjectId.fromJSON(object.id) : undefined,
      display_name: isSet(object.displayName) ? globalThis.String(object.displayName) : undefined,
      grade: isSet(object.grade) ? studentGradeFromJSON(object.grade) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleStudentInfo): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = ObjectId.toJSON(message.id);
    }
    if (message.display_name !== undefined) {
      obj.displayName = message.display_name;
    }
    if (message.grade !== undefined) {
      obj.grade = studentGradeToJSON(message.grade);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleStudentInfo>, I>>(base?: I): SchedulingScheduleStudentInfo {
    return SchedulingScheduleStudentInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleStudentInfo>, I>>(
    object: I,
  ): SchedulingScheduleStudentInfo {
    const message = createBaseSchedulingScheduleStudentInfo();
    message.id = (object.id !== undefined && object.id !== null) ? ObjectId.fromPartial(object.id) : undefined;
    message.display_name = object.display_name ?? undefined;
    message.grade = object.grade ?? undefined;
    return message;
  },
};

function createBaseSchedulingScheduleRoomInfo(): SchedulingScheduleRoomInfo {
  return { id: undefined, name: undefined, campus_id: undefined, category_name: undefined };
}

export const SchedulingScheduleRoomInfo: MessageFns<SchedulingScheduleRoomInfo> = {
  encode(message: SchedulingScheduleRoomInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      ObjectId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    if (message.campus_id !== undefined) {
      ObjectId.encode(message.campus_id, writer.uint32(26).fork()).join();
    }
    if (message.category_name !== undefined) {
      writer.uint32(34).string(message.category_name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleRoomInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleRoomInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.campus_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.category_name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleRoomInfo {
    return {
      id: isSet(object.id) ? ObjectId.fromJSON(object.id) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      campus_id: isSet(object.campusId) ? ObjectId.fromJSON(object.campusId) : undefined,
      category_name: isSet(object.categoryName) ? globalThis.String(object.categoryName) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleRoomInfo): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = ObjectId.toJSON(message.id);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.campus_id !== undefined) {
      obj.campusId = ObjectId.toJSON(message.campus_id);
    }
    if (message.category_name !== undefined) {
      obj.categoryName = message.category_name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleRoomInfo>, I>>(base?: I): SchedulingScheduleRoomInfo {
    return SchedulingScheduleRoomInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleRoomInfo>, I>>(object: I): SchedulingScheduleRoomInfo {
    const message = createBaseSchedulingScheduleRoomInfo();
    message.id = (object.id !== undefined && object.id !== null) ? ObjectId.fromPartial(object.id) : undefined;
    message.name = object.name ?? undefined;
    message.campus_id = (object.campus_id !== undefined && object.campus_id !== null)
      ? ObjectId.fromPartial(object.campus_id)
      : undefined;
    message.category_name = object.category_name ?? undefined;
    return message;
  },
};

function createBaseSchedulingScheduleSectionInfo(): SchedulingScheduleSectionInfo {
  return {
    id: undefined,
    campus_id: undefined,
    grade: undefined,
    semester_id: undefined,
    label: undefined,
    campus_name: undefined,
  };
}

export const SchedulingScheduleSectionInfo: MessageFns<SchedulingScheduleSectionInfo> = {
  encode(message: SchedulingScheduleSectionInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.campus_id !== undefined) {
      ObjectId.encode(message.campus_id, writer.uint32(18).fork()).join();
    }
    if (message.grade !== undefined) {
      writer.uint32(24).int32(studentGradeToNumber(message.grade));
    }
    if (message.semester_id !== undefined) {
      ObjectId.encode(message.semester_id, writer.uint32(34).fork()).join();
    }
    if (message.label !== undefined) {
      writer.uint32(42).string(message.label);
    }
    if (message.campus_name !== undefined) {
      writer.uint32(50).string(message.campus_name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleSectionInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleSectionInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.campus_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.grade = studentGradeFromJSON(reader.int32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.semester_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.label = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.campus_name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleSectionInfo {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      campus_id: isSet(object.campusId) ? ObjectId.fromJSON(object.campusId) : undefined,
      grade: isSet(object.grade) ? studentGradeFromJSON(object.grade) : undefined,
      semester_id: isSet(object.semesterId) ? ObjectId.fromJSON(object.semesterId) : undefined,
      label: isSet(object.label) ? globalThis.String(object.label) : undefined,
      campus_name: isSet(object.campusName) ? globalThis.String(object.campusName) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleSectionInfo): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.campus_id !== undefined) {
      obj.campusId = ObjectId.toJSON(message.campus_id);
    }
    if (message.grade !== undefined) {
      obj.grade = studentGradeToJSON(message.grade);
    }
    if (message.semester_id !== undefined) {
      obj.semesterId = ObjectId.toJSON(message.semester_id);
    }
    if (message.label !== undefined) {
      obj.label = message.label;
    }
    if (message.campus_name !== undefined) {
      obj.campusName = message.campus_name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleSectionInfo>, I>>(base?: I): SchedulingScheduleSectionInfo {
    return SchedulingScheduleSectionInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleSectionInfo>, I>>(
    object: I,
  ): SchedulingScheduleSectionInfo {
    const message = createBaseSchedulingScheduleSectionInfo();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.campus_id = (object.campus_id !== undefined && object.campus_id !== null)
      ? ObjectId.fromPartial(object.campus_id)
      : undefined;
    message.grade = object.grade ?? undefined;
    message.semester_id = (object.semester_id !== undefined && object.semester_id !== null)
      ? ObjectId.fromPartial(object.semester_id)
      : undefined;
    message.label = object.label ?? undefined;
    message.campus_name = object.campus_name ?? undefined;
    return message;
  },
};

function createBaseSchedulingGeneratedScheduleView(): SchedulingGeneratedScheduleView {
  return {
    schedule: undefined,
    entries: [],
    classes: [],
    teachers: [],
    slots: [],
    semesters: [],
    sections: [],
    students: [],
    rooms: [],
    room_assignments_enforced: undefined,
  };
}

export const SchedulingGeneratedScheduleView: MessageFns<SchedulingGeneratedScheduleView> = {
  encode(message: SchedulingGeneratedScheduleView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.schedule !== undefined) {
      SchedulingGeneratedSchedule.encode(message.schedule, writer.uint32(10).fork()).join();
    }
    for (const v of message.entries) {
      SchedulingGeneratedScheduleEntry.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.classes) {
      SchedulingScheduleClassInfo.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.teachers) {
      SchedulingScheduleTeacherInfo.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.slots) {
      SchedulingScheduleSlotInfo.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.semesters) {
      SchedulingScheduleSemesterInfo.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.sections) {
      SchedulingScheduleSectionInfo.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.students) {
      SchedulingScheduleStudentInfo.encode(v!, writer.uint32(66).fork()).join();
    }
    for (const v of message.rooms) {
      SchedulingScheduleRoomInfo.encode(v!, writer.uint32(74).fork()).join();
    }
    if (message.room_assignments_enforced !== undefined) {
      writer.uint32(80).bool(message.room_assignments_enforced);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingGeneratedScheduleView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingGeneratedScheduleView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.schedule = SchedulingGeneratedSchedule.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.entries.push(SchedulingGeneratedScheduleEntry.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.classes.push(SchedulingScheduleClassInfo.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.teachers.push(SchedulingScheduleTeacherInfo.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.slots.push(SchedulingScheduleSlotInfo.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.semesters.push(SchedulingScheduleSemesterInfo.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.sections.push(SchedulingScheduleSectionInfo.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.students.push(SchedulingScheduleStudentInfo.decode(reader, reader.uint32()));
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.rooms.push(SchedulingScheduleRoomInfo.decode(reader, reader.uint32()));
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.room_assignments_enforced = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingGeneratedScheduleView {
    return {
      schedule: isSet(object.schedule) ? SchedulingGeneratedSchedule.fromJSON(object.schedule) : undefined,
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => SchedulingGeneratedScheduleEntry.fromJSON(e))
        : [],
      classes: globalThis.Array.isArray(object?.classes)
        ? object.classes.map((e: any) => SchedulingScheduleClassInfo.fromJSON(e))
        : [],
      teachers: globalThis.Array.isArray(object?.teachers)
        ? object.teachers.map((e: any) => SchedulingScheduleTeacherInfo.fromJSON(e))
        : [],
      slots: globalThis.Array.isArray(object?.slots)
        ? object.slots.map((e: any) => SchedulingScheduleSlotInfo.fromJSON(e))
        : [],
      semesters: globalThis.Array.isArray(object?.semesters)
        ? object.semesters.map((e: any) => SchedulingScheduleSemesterInfo.fromJSON(e))
        : [],
      sections: globalThis.Array.isArray(object?.sections)
        ? object.sections.map((e: any) => SchedulingScheduleSectionInfo.fromJSON(e))
        : [],
      students: globalThis.Array.isArray(object?.students)
        ? object.students.map((e: any) => SchedulingScheduleStudentInfo.fromJSON(e))
        : [],
      rooms: globalThis.Array.isArray(object?.rooms)
        ? object.rooms.map((e: any) => SchedulingScheduleRoomInfo.fromJSON(e))
        : [],
      room_assignments_enforced: isSet(object.roomAssignmentsEnforced)
        ? globalThis.Boolean(object.roomAssignmentsEnforced)
        : undefined,
    };
  },

  toJSON(message: SchedulingGeneratedScheduleView): unknown {
    const obj: any = {};
    if (message.schedule !== undefined) {
      obj.schedule = SchedulingGeneratedSchedule.toJSON(message.schedule);
    }
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => SchedulingGeneratedScheduleEntry.toJSON(e));
    }
    if (message.classes?.length) {
      obj.classes = message.classes.map((e) => SchedulingScheduleClassInfo.toJSON(e));
    }
    if (message.teachers?.length) {
      obj.teachers = message.teachers.map((e) => SchedulingScheduleTeacherInfo.toJSON(e));
    }
    if (message.slots?.length) {
      obj.slots = message.slots.map((e) => SchedulingScheduleSlotInfo.toJSON(e));
    }
    if (message.semesters?.length) {
      obj.semesters = message.semesters.map((e) => SchedulingScheduleSemesterInfo.toJSON(e));
    }
    if (message.sections?.length) {
      obj.sections = message.sections.map((e) => SchedulingScheduleSectionInfo.toJSON(e));
    }
    if (message.students?.length) {
      obj.students = message.students.map((e) => SchedulingScheduleStudentInfo.toJSON(e));
    }
    if (message.rooms?.length) {
      obj.rooms = message.rooms.map((e) => SchedulingScheduleRoomInfo.toJSON(e));
    }
    if (message.room_assignments_enforced !== undefined) {
      obj.roomAssignmentsEnforced = message.room_assignments_enforced;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingGeneratedScheduleView>, I>>(base?: I): SchedulingGeneratedScheduleView {
    return SchedulingGeneratedScheduleView.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingGeneratedScheduleView>, I>>(
    object: I,
  ): SchedulingGeneratedScheduleView {
    const message = createBaseSchedulingGeneratedScheduleView();
    message.schedule = (object.schedule !== undefined && object.schedule !== null)
      ? SchedulingGeneratedSchedule.fromPartial(object.schedule)
      : undefined;
    message.entries = object.entries?.map((e) => SchedulingGeneratedScheduleEntry.fromPartial(e)) || [];
    message.classes = object.classes?.map((e) => SchedulingScheduleClassInfo.fromPartial(e)) || [];
    message.teachers = object.teachers?.map((e) => SchedulingScheduleTeacherInfo.fromPartial(e)) || [];
    message.slots = object.slots?.map((e) => SchedulingScheduleSlotInfo.fromPartial(e)) || [];
    message.semesters = object.semesters?.map((e) => SchedulingScheduleSemesterInfo.fromPartial(e)) || [];
    message.sections = object.sections?.map((e) => SchedulingScheduleSectionInfo.fromPartial(e)) || [];
    message.students = object.students?.map((e) => SchedulingScheduleStudentInfo.fromPartial(e)) || [];
    message.rooms = object.rooms?.map((e) => SchedulingScheduleRoomInfo.fromPartial(e)) || [];
    message.room_assignments_enforced = object.room_assignments_enforced ?? undefined;
    return message;
  },
};

function createBaseSchedulingScheduleView(): SchedulingScheduleView {
  return {
    source: undefined,
    entries: [],
    classes: [],
    teachers: [],
    slots: [],
    semesters: [],
    sections: [],
    students: [],
    rooms: [],
    room_assignments_enforced: undefined,
    setup_compatibility: undefined,
  };
}

export const SchedulingScheduleView: MessageFns<SchedulingScheduleView> = {
  encode(message: SchedulingScheduleView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== undefined) {
      SchedulingScheduleSource.encode(message.source, writer.uint32(10).fork()).join();
    }
    for (const v of message.entries) {
      SchedulingGeneratedScheduleEntry.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.classes) {
      SchedulingScheduleClassInfo.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.teachers) {
      SchedulingScheduleTeacherInfo.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.slots) {
      SchedulingScheduleSlotInfo.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.semesters) {
      SchedulingScheduleSemesterInfo.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.sections) {
      SchedulingScheduleSectionInfo.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.students) {
      SchedulingScheduleStudentInfo.encode(v!, writer.uint32(66).fork()).join();
    }
    for (const v of message.rooms) {
      SchedulingScheduleRoomInfo.encode(v!, writer.uint32(74).fork()).join();
    }
    if (message.room_assignments_enforced !== undefined) {
      writer.uint32(80).bool(message.room_assignments_enforced);
    }
    if (message.setup_compatibility !== undefined) {
      writer.uint32(88).int32(schedulingScheduleSetupCompatibilityToNumber(message.setup_compatibility));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.source = SchedulingScheduleSource.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.entries.push(SchedulingGeneratedScheduleEntry.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.classes.push(SchedulingScheduleClassInfo.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.teachers.push(SchedulingScheduleTeacherInfo.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.slots.push(SchedulingScheduleSlotInfo.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.semesters.push(SchedulingScheduleSemesterInfo.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.sections.push(SchedulingScheduleSectionInfo.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.students.push(SchedulingScheduleStudentInfo.decode(reader, reader.uint32()));
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.rooms.push(SchedulingScheduleRoomInfo.decode(reader, reader.uint32()));
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.room_assignments_enforced = reader.bool();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.setup_compatibility = schedulingScheduleSetupCompatibilityFromJSON(reader.int32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleView {
    return {
      source: isSet(object.source) ? SchedulingScheduleSource.fromJSON(object.source) : undefined,
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => SchedulingGeneratedScheduleEntry.fromJSON(e))
        : [],
      classes: globalThis.Array.isArray(object?.classes)
        ? object.classes.map((e: any) => SchedulingScheduleClassInfo.fromJSON(e))
        : [],
      teachers: globalThis.Array.isArray(object?.teachers)
        ? object.teachers.map((e: any) => SchedulingScheduleTeacherInfo.fromJSON(e))
        : [],
      slots: globalThis.Array.isArray(object?.slots)
        ? object.slots.map((e: any) => SchedulingScheduleSlotInfo.fromJSON(e))
        : [],
      semesters: globalThis.Array.isArray(object?.semesters)
        ? object.semesters.map((e: any) => SchedulingScheduleSemesterInfo.fromJSON(e))
        : [],
      sections: globalThis.Array.isArray(object?.sections)
        ? object.sections.map((e: any) => SchedulingScheduleSectionInfo.fromJSON(e))
        : [],
      students: globalThis.Array.isArray(object?.students)
        ? object.students.map((e: any) => SchedulingScheduleStudentInfo.fromJSON(e))
        : [],
      rooms: globalThis.Array.isArray(object?.rooms)
        ? object.rooms.map((e: any) => SchedulingScheduleRoomInfo.fromJSON(e))
        : [],
      room_assignments_enforced: isSet(object.roomAssignmentsEnforced)
        ? globalThis.Boolean(object.roomAssignmentsEnforced)
        : undefined,
      setup_compatibility: isSet(object.setupCompatibility)
        ? schedulingScheduleSetupCompatibilityFromJSON(object.setupCompatibility)
        : undefined,
    };
  },

  toJSON(message: SchedulingScheduleView): unknown {
    const obj: any = {};
    if (message.source !== undefined) {
      obj.source = SchedulingScheduleSource.toJSON(message.source);
    }
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => SchedulingGeneratedScheduleEntry.toJSON(e));
    }
    if (message.classes?.length) {
      obj.classes = message.classes.map((e) => SchedulingScheduleClassInfo.toJSON(e));
    }
    if (message.teachers?.length) {
      obj.teachers = message.teachers.map((e) => SchedulingScheduleTeacherInfo.toJSON(e));
    }
    if (message.slots?.length) {
      obj.slots = message.slots.map((e) => SchedulingScheduleSlotInfo.toJSON(e));
    }
    if (message.semesters?.length) {
      obj.semesters = message.semesters.map((e) => SchedulingScheduleSemesterInfo.toJSON(e));
    }
    if (message.sections?.length) {
      obj.sections = message.sections.map((e) => SchedulingScheduleSectionInfo.toJSON(e));
    }
    if (message.students?.length) {
      obj.students = message.students.map((e) => SchedulingScheduleStudentInfo.toJSON(e));
    }
    if (message.rooms?.length) {
      obj.rooms = message.rooms.map((e) => SchedulingScheduleRoomInfo.toJSON(e));
    }
    if (message.room_assignments_enforced !== undefined) {
      obj.roomAssignmentsEnforced = message.room_assignments_enforced;
    }
    if (message.setup_compatibility !== undefined) {
      obj.setupCompatibility = schedulingScheduleSetupCompatibilityToJSON(message.setup_compatibility);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleView>, I>>(base?: I): SchedulingScheduleView {
    return SchedulingScheduleView.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleView>, I>>(object: I): SchedulingScheduleView {
    const message = createBaseSchedulingScheduleView();
    message.source = (object.source !== undefined && object.source !== null)
      ? SchedulingScheduleSource.fromPartial(object.source)
      : undefined;
    message.entries = object.entries?.map((e) => SchedulingGeneratedScheduleEntry.fromPartial(e)) || [];
    message.classes = object.classes?.map((e) => SchedulingScheduleClassInfo.fromPartial(e)) || [];
    message.teachers = object.teachers?.map((e) => SchedulingScheduleTeacherInfo.fromPartial(e)) || [];
    message.slots = object.slots?.map((e) => SchedulingScheduleSlotInfo.fromPartial(e)) || [];
    message.semesters = object.semesters?.map((e) => SchedulingScheduleSemesterInfo.fromPartial(e)) || [];
    message.sections = object.sections?.map((e) => SchedulingScheduleSectionInfo.fromPartial(e)) || [];
    message.students = object.students?.map((e) => SchedulingScheduleStudentInfo.fromPartial(e)) || [];
    message.rooms = object.rooms?.map((e) => SchedulingScheduleRoomInfo.fromPartial(e)) || [];
    message.room_assignments_enforced = object.room_assignments_enforced ?? undefined;
    message.setup_compatibility = object.setup_compatibility ?? undefined;
    return message;
  },
};

function createBaseSchedulingWorkingScheduleState(): SchedulingWorkingScheduleState {
  return {
    working_schedule: undefined,
    current_revision: undefined,
    view: undefined,
    can_undo: undefined,
    can_redo: undefined,
  };
}

export const SchedulingWorkingScheduleState: MessageFns<SchedulingWorkingScheduleState> = {
  encode(message: SchedulingWorkingScheduleState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.working_schedule !== undefined) {
      SchedulingWorkingSchedule.encode(message.working_schedule, writer.uint32(10).fork()).join();
    }
    if (message.current_revision !== undefined) {
      SchedulingScheduleRevision.encode(message.current_revision, writer.uint32(18).fork()).join();
    }
    if (message.view !== undefined) {
      SchedulingScheduleView.encode(message.view, writer.uint32(26).fork()).join();
    }
    if (message.can_undo !== undefined) {
      writer.uint32(32).bool(message.can_undo);
    }
    if (message.can_redo !== undefined) {
      writer.uint32(40).bool(message.can_redo);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingWorkingScheduleState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingWorkingScheduleState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.working_schedule = SchedulingWorkingSchedule.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.current_revision = SchedulingScheduleRevision.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.view = SchedulingScheduleView.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.can_undo = reader.bool();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.can_redo = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingWorkingScheduleState {
    return {
      working_schedule: isSet(object.workingSchedule)
        ? SchedulingWorkingSchedule.fromJSON(object.workingSchedule)
        : undefined,
      current_revision: isSet(object.currentRevision)
        ? SchedulingScheduleRevision.fromJSON(object.currentRevision)
        : undefined,
      view: isSet(object.view) ? SchedulingScheduleView.fromJSON(object.view) : undefined,
      can_undo: isSet(object.canUndo) ? globalThis.Boolean(object.canUndo) : undefined,
      can_redo: isSet(object.canRedo) ? globalThis.Boolean(object.canRedo) : undefined,
    };
  },

  toJSON(message: SchedulingWorkingScheduleState): unknown {
    const obj: any = {};
    if (message.working_schedule !== undefined) {
      obj.workingSchedule = SchedulingWorkingSchedule.toJSON(message.working_schedule);
    }
    if (message.current_revision !== undefined) {
      obj.currentRevision = SchedulingScheduleRevision.toJSON(message.current_revision);
    }
    if (message.view !== undefined) {
      obj.view = SchedulingScheduleView.toJSON(message.view);
    }
    if (message.can_undo !== undefined) {
      obj.canUndo = message.can_undo;
    }
    if (message.can_redo !== undefined) {
      obj.canRedo = message.can_redo;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingWorkingScheduleState>, I>>(base?: I): SchedulingWorkingScheduleState {
    return SchedulingWorkingScheduleState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingWorkingScheduleState>, I>>(
    object: I,
  ): SchedulingWorkingScheduleState {
    const message = createBaseSchedulingWorkingScheduleState();
    message.working_schedule = (object.working_schedule !== undefined && object.working_schedule !== null)
      ? SchedulingWorkingSchedule.fromPartial(object.working_schedule)
      : undefined;
    message.current_revision = (object.current_revision !== undefined && object.current_revision !== null)
      ? SchedulingScheduleRevision.fromPartial(object.current_revision)
      : undefined;
    message.view = (object.view !== undefined && object.view !== null)
      ? SchedulingScheduleView.fromPartial(object.view)
      : undefined;
    message.can_undo = object.can_undo ?? undefined;
    message.can_redo = object.can_redo ?? undefined;
    return message;
  },
};

function createBaseSchedulingWorkingScheduleMutationResult(): SchedulingWorkingScheduleMutationResult {
  return { applied: undefined, state: undefined, issues: [] };
}

export const SchedulingWorkingScheduleMutationResult: MessageFns<SchedulingWorkingScheduleMutationResult> = {
  encode(message: SchedulingWorkingScheduleMutationResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.applied !== undefined) {
      writer.uint32(8).bool(message.applied);
    }
    if (message.state !== undefined) {
      SchedulingWorkingScheduleState.encode(message.state, writer.uint32(18).fork()).join();
    }
    for (const v of message.issues) {
      SchedulingScheduleCompatibilityIssue.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingWorkingScheduleMutationResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingWorkingScheduleMutationResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.applied = reader.bool();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.state = SchedulingWorkingScheduleState.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.issues.push(SchedulingScheduleCompatibilityIssue.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingWorkingScheduleMutationResult {
    return {
      applied: isSet(object.applied) ? globalThis.Boolean(object.applied) : undefined,
      state: isSet(object.state) ? SchedulingWorkingScheduleState.fromJSON(object.state) : undefined,
      issues: globalThis.Array.isArray(object?.issues)
        ? object.issues.map((e: any) => SchedulingScheduleCompatibilityIssue.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingWorkingScheduleMutationResult): unknown {
    const obj: any = {};
    if (message.applied !== undefined) {
      obj.applied = message.applied;
    }
    if (message.state !== undefined) {
      obj.state = SchedulingWorkingScheduleState.toJSON(message.state);
    }
    if (message.issues?.length) {
      obj.issues = message.issues.map((e) => SchedulingScheduleCompatibilityIssue.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingWorkingScheduleMutationResult>, I>>(
    base?: I,
  ): SchedulingWorkingScheduleMutationResult {
    return SchedulingWorkingScheduleMutationResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingWorkingScheduleMutationResult>, I>>(
    object: I,
  ): SchedulingWorkingScheduleMutationResult {
    const message = createBaseSchedulingWorkingScheduleMutationResult();
    message.applied = object.applied ?? undefined;
    message.state = (object.state !== undefined && object.state !== null)
      ? SchedulingWorkingScheduleState.fromPartial(object.state)
      : undefined;
    message.issues = object.issues?.map((e) => SchedulingScheduleCompatibilityIssue.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingScheduleSwap(): SchedulingScheduleSwap {
  return { placement_id: undefined, target_placement_id: undefined };
}

export const SchedulingScheduleSwap: MessageFns<SchedulingScheduleSwap> = {
  encode(message: SchedulingScheduleSwap, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.placement_id !== undefined) {
      Uuid.encode(message.placement_id, writer.uint32(10).fork()).join();
    }
    if (message.target_placement_id !== undefined) {
      Uuid.encode(message.target_placement_id, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleSwap {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleSwap();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.placement_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.target_placement_id = Uuid.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleSwap {
    return {
      placement_id: isSet(object.placementId) ? Uuid.fromJSON(object.placementId) : undefined,
      target_placement_id: isSet(object.targetPlacementId) ? Uuid.fromJSON(object.targetPlacementId) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleSwap): unknown {
    const obj: any = {};
    if (message.placement_id !== undefined) {
      obj.placementId = Uuid.toJSON(message.placement_id);
    }
    if (message.target_placement_id !== undefined) {
      obj.targetPlacementId = Uuid.toJSON(message.target_placement_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleSwap>, I>>(base?: I): SchedulingScheduleSwap {
    return SchedulingScheduleSwap.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleSwap>, I>>(object: I): SchedulingScheduleSwap {
    const message = createBaseSchedulingScheduleSwap();
    message.placement_id = (object.placement_id !== undefined && object.placement_id !== null)
      ? Uuid.fromPartial(object.placement_id)
      : undefined;
    message.target_placement_id = (object.target_placement_id !== undefined && object.target_placement_id !== null)
      ? Uuid.fromPartial(object.target_placement_id)
      : undefined;
    return message;
  },
};

function createBaseSchedulingScheduleAdjustmentOperation(): SchedulingScheduleAdjustmentOperation {
  return { swap: undefined };
}

export const SchedulingScheduleAdjustmentOperation: MessageFns<SchedulingScheduleAdjustmentOperation> = {
  encode(message: SchedulingScheduleAdjustmentOperation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.swap !== undefined) {
      SchedulingScheduleSwap.encode(message.swap, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleAdjustmentOperation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleAdjustmentOperation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2:
          if (tag !== 18) {
            break;
          }

          message.swap = SchedulingScheduleSwap.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleAdjustmentOperation {
    return { swap: isSet(object.swap) ? SchedulingScheduleSwap.fromJSON(object.swap) : undefined };
  },

  toJSON(message: SchedulingScheduleAdjustmentOperation): unknown {
    const obj: any = {};
    if (message.swap !== undefined) {
      obj.swap = SchedulingScheduleSwap.toJSON(message.swap);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentOperation>, I>>(
    base?: I,
  ): SchedulingScheduleAdjustmentOperation {
    return SchedulingScheduleAdjustmentOperation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentOperation>, I>>(
    object: I,
  ): SchedulingScheduleAdjustmentOperation {
    const message = createBaseSchedulingScheduleAdjustmentOperation();
    message.swap = (object.swap !== undefined && object.swap !== null)
      ? SchedulingScheduleSwap.fromPartial(object.swap)
      : undefined;
    return message;
  },
};

function createBaseSchedulingScheduleAdjustmentIssue(): SchedulingScheduleAdjustmentIssue {
  return { code: undefined, message: undefined, severity: undefined, placement_ids: [], resource_ids: [] };
}

export const SchedulingScheduleAdjustmentIssue: MessageFns<SchedulingScheduleAdjustmentIssue> = {
  encode(message: SchedulingScheduleAdjustmentIssue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== undefined) {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== undefined) {
      writer.uint32(18).string(message.message);
    }
    if (message.severity !== undefined) {
      writer.uint32(24).int32(schedulingScheduleAdjustmentIssueSeverityToNumber(message.severity));
    }
    for (const v of message.placement_ids) {
      Uuid.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.resource_ids) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleAdjustmentIssue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleAdjustmentIssue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.severity = schedulingScheduleAdjustmentIssueSeverityFromJSON(reader.int32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.placement_ids.push(Uuid.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.resource_ids.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleAdjustmentIssue {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : undefined,
      message: isSet(object.message) ? globalThis.String(object.message) : undefined,
      severity: isSet(object.severity) ? schedulingScheduleAdjustmentIssueSeverityFromJSON(object.severity) : undefined,
      placement_ids: globalThis.Array.isArray(object?.placementIds)
        ? object.placementIds.map((e: any) => Uuid.fromJSON(e))
        : [],
      resource_ids: globalThis.Array.isArray(object?.resourceIds)
        ? object.resourceIds.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: SchedulingScheduleAdjustmentIssue): unknown {
    const obj: any = {};
    if (message.code !== undefined) {
      obj.code = message.code;
    }
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    if (message.severity !== undefined) {
      obj.severity = schedulingScheduleAdjustmentIssueSeverityToJSON(message.severity);
    }
    if (message.placement_ids?.length) {
      obj.placementIds = message.placement_ids.map((e) => Uuid.toJSON(e));
    }
    if (message.resource_ids?.length) {
      obj.resourceIds = message.resource_ids;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentIssue>, I>>(
    base?: I,
  ): SchedulingScheduleAdjustmentIssue {
    return SchedulingScheduleAdjustmentIssue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentIssue>, I>>(
    object: I,
  ): SchedulingScheduleAdjustmentIssue {
    const message = createBaseSchedulingScheduleAdjustmentIssue();
    message.code = object.code ?? undefined;
    message.message = object.message ?? undefined;
    message.severity = object.severity ?? undefined;
    message.placement_ids = object.placement_ids?.map((e) => Uuid.fromPartial(e)) || [];
    message.resource_ids = object.resource_ids?.map((e) => e) || [];
    return message;
  },
};

function createBaseSchedulingScheduleCellChange(): SchedulingScheduleCellChange {
  return { placement_id: undefined, class_id: undefined, from_slot_id: undefined, to_slot_id: undefined };
}

export const SchedulingScheduleCellChange: MessageFns<SchedulingScheduleCellChange> = {
  encode(message: SchedulingScheduleCellChange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.placement_id !== undefined) {
      Uuid.encode(message.placement_id, writer.uint32(10).fork()).join();
    }
    if (message.class_id !== undefined) {
      writer.uint32(18).string(message.class_id);
    }
    if (message.from_slot_id !== undefined) {
      writer.uint32(26).string(message.from_slot_id);
    }
    if (message.to_slot_id !== undefined) {
      writer.uint32(34).string(message.to_slot_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleCellChange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleCellChange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.placement_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.class_id = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.from_slot_id = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.to_slot_id = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleCellChange {
    return {
      placement_id: isSet(object.placementId) ? Uuid.fromJSON(object.placementId) : undefined,
      class_id: isSet(object.classId) ? globalThis.String(object.classId) : undefined,
      from_slot_id: isSet(object.fromSlotId) ? globalThis.String(object.fromSlotId) : undefined,
      to_slot_id: isSet(object.toSlotId) ? globalThis.String(object.toSlotId) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleCellChange): unknown {
    const obj: any = {};
    if (message.placement_id !== undefined) {
      obj.placementId = Uuid.toJSON(message.placement_id);
    }
    if (message.class_id !== undefined) {
      obj.classId = message.class_id;
    }
    if (message.from_slot_id !== undefined) {
      obj.fromSlotId = message.from_slot_id;
    }
    if (message.to_slot_id !== undefined) {
      obj.toSlotId = message.to_slot_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleCellChange>, I>>(base?: I): SchedulingScheduleCellChange {
    return SchedulingScheduleCellChange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleCellChange>, I>>(object: I): SchedulingScheduleCellChange {
    const message = createBaseSchedulingScheduleCellChange();
    message.placement_id = (object.placement_id !== undefined && object.placement_id !== null)
      ? Uuid.fromPartial(object.placement_id)
      : undefined;
    message.class_id = object.class_id ?? undefined;
    message.from_slot_id = object.from_slot_id ?? undefined;
    message.to_slot_id = object.to_slot_id ?? undefined;
    return message;
  },
};

function createBaseSchedulingScheduleAdjustmentImpact(): SchedulingScheduleAdjustmentImpact {
  return { cell_changes: [], class_ids: [], section_ids: [], teacher_ids: [], student_ids: [], room_ids: [] };
}

export const SchedulingScheduleAdjustmentImpact: MessageFns<SchedulingScheduleAdjustmentImpact> = {
  encode(message: SchedulingScheduleAdjustmentImpact, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.cell_changes) {
      SchedulingScheduleCellChange.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.class_ids) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.section_ids) {
      Uuid.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.teacher_ids) {
      ObjectId.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.student_ids) {
      ObjectId.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.room_ids) {
      ObjectId.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleAdjustmentImpact {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleAdjustmentImpact();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.cell_changes.push(SchedulingScheduleCellChange.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.class_ids.push(reader.string());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.section_ids.push(Uuid.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.teacher_ids.push(ObjectId.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.student_ids.push(ObjectId.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.room_ids.push(ObjectId.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleAdjustmentImpact {
    return {
      cell_changes: globalThis.Array.isArray(object?.cellChanges)
        ? object.cellChanges.map((e: any) => SchedulingScheduleCellChange.fromJSON(e))
        : [],
      class_ids: globalThis.Array.isArray(object?.classIds)
        ? object.classIds.map((e: any) => globalThis.String(e))
        : [],
      section_ids: globalThis.Array.isArray(object?.sectionIds)
        ? object.sectionIds.map((e: any) => Uuid.fromJSON(e))
        : [],
      teacher_ids: globalThis.Array.isArray(object?.teacherIds)
        ? object.teacherIds.map((e: any) => ObjectId.fromJSON(e))
        : [],
      student_ids: globalThis.Array.isArray(object?.studentIds)
        ? object.studentIds.map((e: any) => ObjectId.fromJSON(e))
        : [],
      room_ids: globalThis.Array.isArray(object?.roomIds) ? object.roomIds.map((e: any) => ObjectId.fromJSON(e)) : [],
    };
  },

  toJSON(message: SchedulingScheduleAdjustmentImpact): unknown {
    const obj: any = {};
    if (message.cell_changes?.length) {
      obj.cellChanges = message.cell_changes.map((e) => SchedulingScheduleCellChange.toJSON(e));
    }
    if (message.class_ids?.length) {
      obj.classIds = message.class_ids;
    }
    if (message.section_ids?.length) {
      obj.sectionIds = message.section_ids.map((e) => Uuid.toJSON(e));
    }
    if (message.teacher_ids?.length) {
      obj.teacherIds = message.teacher_ids.map((e) => ObjectId.toJSON(e));
    }
    if (message.student_ids?.length) {
      obj.studentIds = message.student_ids.map((e) => ObjectId.toJSON(e));
    }
    if (message.room_ids?.length) {
      obj.roomIds = message.room_ids.map((e) => ObjectId.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentImpact>, I>>(
    base?: I,
  ): SchedulingScheduleAdjustmentImpact {
    return SchedulingScheduleAdjustmentImpact.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentImpact>, I>>(
    object: I,
  ): SchedulingScheduleAdjustmentImpact {
    const message = createBaseSchedulingScheduleAdjustmentImpact();
    message.cell_changes = object.cell_changes?.map((e) => SchedulingScheduleCellChange.fromPartial(e)) || [];
    message.class_ids = object.class_ids?.map((e) => e) || [];
    message.section_ids = object.section_ids?.map((e) => Uuid.fromPartial(e)) || [];
    message.teacher_ids = object.teacher_ids?.map((e) => ObjectId.fromPartial(e)) || [];
    message.student_ids = object.student_ids?.map((e) => ObjectId.fromPartial(e)) || [];
    message.room_ids = object.room_ids?.map((e) => ObjectId.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingScheduleAdjustmentOption(): SchedulingScheduleAdjustmentOption {
  return { kind: undefined, target_placement_id: undefined, valid: undefined, primary_issue: undefined };
}

export const SchedulingScheduleAdjustmentOption: MessageFns<SchedulingScheduleAdjustmentOption> = {
  encode(message: SchedulingScheduleAdjustmentOption, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kind !== undefined) {
      writer.uint32(8).int32(schedulingScheduleAdjustmentKindToNumber(message.kind));
    }
    if (message.target_placement_id !== undefined) {
      Uuid.encode(message.target_placement_id, writer.uint32(26).fork()).join();
    }
    if (message.valid !== undefined) {
      writer.uint32(32).bool(message.valid);
    }
    if (message.primary_issue !== undefined) {
      SchedulingScheduleAdjustmentIssue.encode(message.primary_issue, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleAdjustmentOption {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleAdjustmentOption();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.kind = schedulingScheduleAdjustmentKindFromJSON(reader.int32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.target_placement_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.valid = reader.bool();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.primary_issue = SchedulingScheduleAdjustmentIssue.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleAdjustmentOption {
    return {
      kind: isSet(object.kind) ? schedulingScheduleAdjustmentKindFromJSON(object.kind) : undefined,
      target_placement_id: isSet(object.targetPlacementId) ? Uuid.fromJSON(object.targetPlacementId) : undefined,
      valid: isSet(object.valid) ? globalThis.Boolean(object.valid) : undefined,
      primary_issue: isSet(object.primaryIssue)
        ? SchedulingScheduleAdjustmentIssue.fromJSON(object.primaryIssue)
        : undefined,
    };
  },

  toJSON(message: SchedulingScheduleAdjustmentOption): unknown {
    const obj: any = {};
    if (message.kind !== undefined) {
      obj.kind = schedulingScheduleAdjustmentKindToJSON(message.kind);
    }
    if (message.target_placement_id !== undefined) {
      obj.targetPlacementId = Uuid.toJSON(message.target_placement_id);
    }
    if (message.valid !== undefined) {
      obj.valid = message.valid;
    }
    if (message.primary_issue !== undefined) {
      obj.primaryIssue = SchedulingScheduleAdjustmentIssue.toJSON(message.primary_issue);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentOption>, I>>(
    base?: I,
  ): SchedulingScheduleAdjustmentOption {
    return SchedulingScheduleAdjustmentOption.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentOption>, I>>(
    object: I,
  ): SchedulingScheduleAdjustmentOption {
    const message = createBaseSchedulingScheduleAdjustmentOption();
    message.kind = object.kind ?? undefined;
    message.target_placement_id = (object.target_placement_id !== undefined && object.target_placement_id !== null)
      ? Uuid.fromPartial(object.target_placement_id)
      : undefined;
    message.valid = object.valid ?? undefined;
    message.primary_issue = (object.primary_issue !== undefined && object.primary_issue !== null)
      ? SchedulingScheduleAdjustmentIssue.fromPartial(object.primary_issue)
      : undefined;
    return message;
  },
};

function createBaseSchedulingScheduleAdjustmentOptions(): SchedulingScheduleAdjustmentOptions {
  return { source_placement_ids: [], options: [] };
}

export const SchedulingScheduleAdjustmentOptions: MessageFns<SchedulingScheduleAdjustmentOptions> = {
  encode(message: SchedulingScheduleAdjustmentOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.source_placement_ids) {
      Uuid.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.options) {
      SchedulingScheduleAdjustmentOption.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleAdjustmentOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleAdjustmentOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.source_placement_ids.push(Uuid.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.options.push(SchedulingScheduleAdjustmentOption.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleAdjustmentOptions {
    return {
      source_placement_ids: globalThis.Array.isArray(object?.sourcePlacementIds)
        ? object.sourcePlacementIds.map((e: any) => Uuid.fromJSON(e))
        : [],
      options: globalThis.Array.isArray(object?.options)
        ? object.options.map((e: any) => SchedulingScheduleAdjustmentOption.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingScheduleAdjustmentOptions): unknown {
    const obj: any = {};
    if (message.source_placement_ids?.length) {
      obj.sourcePlacementIds = message.source_placement_ids.map((e) => Uuid.toJSON(e));
    }
    if (message.options?.length) {
      obj.options = message.options.map((e) => SchedulingScheduleAdjustmentOption.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentOptions>, I>>(
    base?: I,
  ): SchedulingScheduleAdjustmentOptions {
    return SchedulingScheduleAdjustmentOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentOptions>, I>>(
    object: I,
  ): SchedulingScheduleAdjustmentOptions {
    const message = createBaseSchedulingScheduleAdjustmentOptions();
    message.source_placement_ids = object.source_placement_ids?.map((e) => Uuid.fromPartial(e)) || [];
    message.options = object.options?.map((e) => SchedulingScheduleAdjustmentOption.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingScheduleAdjustmentPreview(): SchedulingScheduleAdjustmentPreview {
  return { valid: undefined, current_revision_id: undefined, proposed_entries: [], issues: [], impact: undefined };
}

export const SchedulingScheduleAdjustmentPreview: MessageFns<SchedulingScheduleAdjustmentPreview> = {
  encode(message: SchedulingScheduleAdjustmentPreview, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.valid !== undefined) {
      writer.uint32(8).bool(message.valid);
    }
    if (message.current_revision_id !== undefined) {
      Uuid.encode(message.current_revision_id, writer.uint32(18).fork()).join();
    }
    for (const v of message.proposed_entries) {
      SchedulingGeneratedScheduleEntry.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.issues) {
      SchedulingScheduleAdjustmentIssue.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.impact !== undefined) {
      SchedulingScheduleAdjustmentImpact.encode(message.impact, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleAdjustmentPreview {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleAdjustmentPreview();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.valid = reader.bool();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.current_revision_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.proposed_entries.push(SchedulingGeneratedScheduleEntry.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.issues.push(SchedulingScheduleAdjustmentIssue.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.impact = SchedulingScheduleAdjustmentImpact.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleAdjustmentPreview {
    return {
      valid: isSet(object.valid) ? globalThis.Boolean(object.valid) : undefined,
      current_revision_id: isSet(object.currentRevisionId) ? Uuid.fromJSON(object.currentRevisionId) : undefined,
      proposed_entries: globalThis.Array.isArray(object?.proposedEntries)
        ? object.proposedEntries.map((e: any) => SchedulingGeneratedScheduleEntry.fromJSON(e))
        : [],
      issues: globalThis.Array.isArray(object?.issues)
        ? object.issues.map((e: any) => SchedulingScheduleAdjustmentIssue.fromJSON(e))
        : [],
      impact: isSet(object.impact) ? SchedulingScheduleAdjustmentImpact.fromJSON(object.impact) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleAdjustmentPreview): unknown {
    const obj: any = {};
    if (message.valid !== undefined) {
      obj.valid = message.valid;
    }
    if (message.current_revision_id !== undefined) {
      obj.currentRevisionId = Uuid.toJSON(message.current_revision_id);
    }
    if (message.proposed_entries?.length) {
      obj.proposedEntries = message.proposed_entries.map((e) => SchedulingGeneratedScheduleEntry.toJSON(e));
    }
    if (message.issues?.length) {
      obj.issues = message.issues.map((e) => SchedulingScheduleAdjustmentIssue.toJSON(e));
    }
    if (message.impact !== undefined) {
      obj.impact = SchedulingScheduleAdjustmentImpact.toJSON(message.impact);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentPreview>, I>>(
    base?: I,
  ): SchedulingScheduleAdjustmentPreview {
    return SchedulingScheduleAdjustmentPreview.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentPreview>, I>>(
    object: I,
  ): SchedulingScheduleAdjustmentPreview {
    const message = createBaseSchedulingScheduleAdjustmentPreview();
    message.valid = object.valid ?? undefined;
    message.current_revision_id = (object.current_revision_id !== undefined && object.current_revision_id !== null)
      ? Uuid.fromPartial(object.current_revision_id)
      : undefined;
    message.proposed_entries = object.proposed_entries?.map((e) => SchedulingGeneratedScheduleEntry.fromPartial(e)) ||
      [];
    message.issues = object.issues?.map((e) => SchedulingScheduleAdjustmentIssue.fromPartial(e)) || [];
    message.impact = (object.impact !== undefined && object.impact !== null)
      ? SchedulingScheduleAdjustmentImpact.fromPartial(object.impact)
      : undefined;
    return message;
  },
};

function createBaseSchedulingScheduleAdjustmentMutationResult(): SchedulingScheduleAdjustmentMutationResult {
  return { applied: undefined, state: undefined, issues: [], impact: undefined };
}

export const SchedulingScheduleAdjustmentMutationResult: MessageFns<SchedulingScheduleAdjustmentMutationResult> = {
  encode(message: SchedulingScheduleAdjustmentMutationResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.applied !== undefined) {
      writer.uint32(8).bool(message.applied);
    }
    if (message.state !== undefined) {
      SchedulingWorkingScheduleState.encode(message.state, writer.uint32(18).fork()).join();
    }
    for (const v of message.issues) {
      SchedulingScheduleAdjustmentIssue.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.impact !== undefined) {
      SchedulingScheduleAdjustmentImpact.encode(message.impact, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingScheduleAdjustmentMutationResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingScheduleAdjustmentMutationResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.applied = reader.bool();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.state = SchedulingWorkingScheduleState.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.issues.push(SchedulingScheduleAdjustmentIssue.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.impact = SchedulingScheduleAdjustmentImpact.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingScheduleAdjustmentMutationResult {
    return {
      applied: isSet(object.applied) ? globalThis.Boolean(object.applied) : undefined,
      state: isSet(object.state) ? SchedulingWorkingScheduleState.fromJSON(object.state) : undefined,
      issues: globalThis.Array.isArray(object?.issues)
        ? object.issues.map((e: any) => SchedulingScheduleAdjustmentIssue.fromJSON(e))
        : [],
      impact: isSet(object.impact) ? SchedulingScheduleAdjustmentImpact.fromJSON(object.impact) : undefined,
    };
  },

  toJSON(message: SchedulingScheduleAdjustmentMutationResult): unknown {
    const obj: any = {};
    if (message.applied !== undefined) {
      obj.applied = message.applied;
    }
    if (message.state !== undefined) {
      obj.state = SchedulingWorkingScheduleState.toJSON(message.state);
    }
    if (message.issues?.length) {
      obj.issues = message.issues.map((e) => SchedulingScheduleAdjustmentIssue.toJSON(e));
    }
    if (message.impact !== undefined) {
      obj.impact = SchedulingScheduleAdjustmentImpact.toJSON(message.impact);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentMutationResult>, I>>(
    base?: I,
  ): SchedulingScheduleAdjustmentMutationResult {
    return SchedulingScheduleAdjustmentMutationResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingScheduleAdjustmentMutationResult>, I>>(
    object: I,
  ): SchedulingScheduleAdjustmentMutationResult {
    const message = createBaseSchedulingScheduleAdjustmentMutationResult();
    message.applied = object.applied ?? undefined;
    message.state = (object.state !== undefined && object.state !== null)
      ? SchedulingWorkingScheduleState.fromPartial(object.state)
      : undefined;
    message.issues = object.issues?.map((e) => SchedulingScheduleAdjustmentIssue.fromPartial(e)) || [];
    message.impact = (object.impact !== undefined && object.impact !== null)
      ? SchedulingScheduleAdjustmentImpact.fromPartial(object.impact)
      : undefined;
    return message;
  },
};

function createBaseSchedulingSchedulePin(): SchedulingSchedulePin {
  return {
    id: undefined,
    pin_group_id: undefined,
    placement_id: undefined,
    class_id: undefined,
    slot_id: undefined,
    teacher_id: undefined,
    room_id: undefined,
  };
}

export const SchedulingSchedulePin: MessageFns<SchedulingSchedulePin> = {
  encode(message: SchedulingSchedulePin, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.pin_group_id !== undefined) {
      Uuid.encode(message.pin_group_id, writer.uint32(18).fork()).join();
    }
    if (message.placement_id !== undefined) {
      Uuid.encode(message.placement_id, writer.uint32(26).fork()).join();
    }
    if (message.class_id !== undefined) {
      writer.uint32(34).string(message.class_id);
    }
    if (message.slot_id !== undefined) {
      writer.uint32(42).string(message.slot_id);
    }
    if (message.teacher_id !== undefined) {
      ObjectId.encode(message.teacher_id, writer.uint32(50).fork()).join();
    }
    if (message.room_id !== undefined) {
      ObjectId.encode(message.room_id, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingSchedulePin {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingSchedulePin();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.pin_group_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.placement_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.class_id = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.slot_id = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.teacher_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.room_id = ObjectId.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingSchedulePin {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      pin_group_id: isSet(object.pinGroupId) ? Uuid.fromJSON(object.pinGroupId) : undefined,
      placement_id: isSet(object.placementId) ? Uuid.fromJSON(object.placementId) : undefined,
      class_id: isSet(object.classId) ? globalThis.String(object.classId) : undefined,
      slot_id: isSet(object.slotId) ? globalThis.String(object.slotId) : undefined,
      teacher_id: isSet(object.teacherId) ? ObjectId.fromJSON(object.teacherId) : undefined,
      room_id: isSet(object.roomId) ? ObjectId.fromJSON(object.roomId) : undefined,
    };
  },

  toJSON(message: SchedulingSchedulePin): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.pin_group_id !== undefined) {
      obj.pinGroupId = Uuid.toJSON(message.pin_group_id);
    }
    if (message.placement_id !== undefined) {
      obj.placementId = Uuid.toJSON(message.placement_id);
    }
    if (message.class_id !== undefined) {
      obj.classId = message.class_id;
    }
    if (message.slot_id !== undefined) {
      obj.slotId = message.slot_id;
    }
    if (message.teacher_id !== undefined) {
      obj.teacherId = ObjectId.toJSON(message.teacher_id);
    }
    if (message.room_id !== undefined) {
      obj.roomId = ObjectId.toJSON(message.room_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingSchedulePin>, I>>(base?: I): SchedulingSchedulePin {
    return SchedulingSchedulePin.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingSchedulePin>, I>>(object: I): SchedulingSchedulePin {
    const message = createBaseSchedulingSchedulePin();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.pin_group_id = (object.pin_group_id !== undefined && object.pin_group_id !== null)
      ? Uuid.fromPartial(object.pin_group_id)
      : undefined;
    message.placement_id = (object.placement_id !== undefined && object.placement_id !== null)
      ? Uuid.fromPartial(object.placement_id)
      : undefined;
    message.class_id = object.class_id ?? undefined;
    message.slot_id = object.slot_id ?? undefined;
    message.teacher_id = (object.teacher_id !== undefined && object.teacher_id !== null)
      ? ObjectId.fromPartial(object.teacher_id)
      : undefined;
    message.room_id = (object.room_id !== undefined && object.room_id !== null)
      ? ObjectId.fromPartial(object.room_id)
      : undefined;
    return message;
  },
};

function createBaseSchedulingSchedulePinGroup(): SchedulingSchedulePinGroup {
  return {
    id: undefined,
    working_schedule_id: undefined,
    revision_id: undefined,
    scope: undefined,
    label: undefined,
    pins: [],
    created_by: undefined,
    created_at: undefined,
  };
}

export const SchedulingSchedulePinGroup: MessageFns<SchedulingSchedulePinGroup> = {
  encode(message: SchedulingSchedulePinGroup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      Uuid.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.working_schedule_id !== undefined) {
      Uuid.encode(message.working_schedule_id, writer.uint32(18).fork()).join();
    }
    if (message.revision_id !== undefined) {
      Uuid.encode(message.revision_id, writer.uint32(26).fork()).join();
    }
    if (message.scope !== undefined) {
      writer.uint32(32).int32(schedulingSchedulePinScopeToNumber(message.scope));
    }
    if (message.label !== undefined) {
      writer.uint32(42).string(message.label);
    }
    for (const v of message.pins) {
      SchedulingSchedulePin.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.created_by !== undefined) {
      ObjectId.encode(message.created_by, writer.uint32(58).fork()).join();
    }
    if (message.created_at !== undefined) {
      writer.uint32(66).string(message.created_at);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingSchedulePinGroup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingSchedulePinGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = Uuid.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.working_schedule_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.revision_id = Uuid.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.scope = schedulingSchedulePinScopeFromJSON(reader.int32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.label = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.pins.push(SchedulingSchedulePin.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.created_by = ObjectId.decode(reader, reader.uint32());
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.created_at = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingSchedulePinGroup {
    return {
      id: isSet(object.id) ? Uuid.fromJSON(object.id) : undefined,
      working_schedule_id: isSet(object.workingScheduleId) ? Uuid.fromJSON(object.workingScheduleId) : undefined,
      revision_id: isSet(object.revisionId) ? Uuid.fromJSON(object.revisionId) : undefined,
      scope: isSet(object.scope) ? schedulingSchedulePinScopeFromJSON(object.scope) : undefined,
      label: isSet(object.label) ? globalThis.String(object.label) : undefined,
      pins: globalThis.Array.isArray(object?.pins)
        ? object.pins.map((e: any) => SchedulingSchedulePin.fromJSON(e))
        : [],
      created_by: isSet(object.createdBy) ? ObjectId.fromJSON(object.createdBy) : undefined,
      created_at: isSet(object.createdAt) ? globalThis.String(object.createdAt) : undefined,
    };
  },

  toJSON(message: SchedulingSchedulePinGroup): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = Uuid.toJSON(message.id);
    }
    if (message.working_schedule_id !== undefined) {
      obj.workingScheduleId = Uuid.toJSON(message.working_schedule_id);
    }
    if (message.revision_id !== undefined) {
      obj.revisionId = Uuid.toJSON(message.revision_id);
    }
    if (message.scope !== undefined) {
      obj.scope = schedulingSchedulePinScopeToJSON(message.scope);
    }
    if (message.label !== undefined) {
      obj.label = message.label;
    }
    if (message.pins?.length) {
      obj.pins = message.pins.map((e) => SchedulingSchedulePin.toJSON(e));
    }
    if (message.created_by !== undefined) {
      obj.createdBy = ObjectId.toJSON(message.created_by);
    }
    if (message.created_at !== undefined) {
      obj.createdAt = message.created_at;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingSchedulePinGroup>, I>>(base?: I): SchedulingSchedulePinGroup {
    return SchedulingSchedulePinGroup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingSchedulePinGroup>, I>>(object: I): SchedulingSchedulePinGroup {
    const message = createBaseSchedulingSchedulePinGroup();
    message.id = (object.id !== undefined && object.id !== null) ? Uuid.fromPartial(object.id) : undefined;
    message.working_schedule_id = (object.working_schedule_id !== undefined && object.working_schedule_id !== null)
      ? Uuid.fromPartial(object.working_schedule_id)
      : undefined;
    message.revision_id = (object.revision_id !== undefined && object.revision_id !== null)
      ? Uuid.fromPartial(object.revision_id)
      : undefined;
    message.scope = object.scope ?? undefined;
    message.label = object.label ?? undefined;
    message.pins = object.pins?.map((e) => SchedulingSchedulePin.fromPartial(e)) || [];
    message.created_by = (object.created_by !== undefined && object.created_by !== null)
      ? ObjectId.fromPartial(object.created_by)
      : undefined;
    message.created_at = object.created_at ?? undefined;
    return message;
  },
};

function createBaseSchedulingSchedulePinGroupList(): SchedulingSchedulePinGroupList {
  return { groups: [], current_revision_id: undefined };
}

export const SchedulingSchedulePinGroupList: MessageFns<SchedulingSchedulePinGroupList> = {
  encode(message: SchedulingSchedulePinGroupList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.groups) {
      SchedulingSchedulePinGroup.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.current_revision_id !== undefined) {
      Uuid.encode(message.current_revision_id, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingSchedulePinGroupList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingSchedulePinGroupList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.groups.push(SchedulingSchedulePinGroup.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.current_revision_id = Uuid.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingSchedulePinGroupList {
    return {
      groups: globalThis.Array.isArray(object?.groups)
        ? object.groups.map((e: any) => SchedulingSchedulePinGroup.fromJSON(e))
        : [],
      current_revision_id: isSet(object.currentRevisionId) ? Uuid.fromJSON(object.currentRevisionId) : undefined,
    };
  },

  toJSON(message: SchedulingSchedulePinGroupList): unknown {
    const obj: any = {};
    if (message.groups?.length) {
      obj.groups = message.groups.map((e) => SchedulingSchedulePinGroup.toJSON(e));
    }
    if (message.current_revision_id !== undefined) {
      obj.currentRevisionId = Uuid.toJSON(message.current_revision_id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingSchedulePinGroupList>, I>>(base?: I): SchedulingSchedulePinGroupList {
    return SchedulingSchedulePinGroupList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingSchedulePinGroupList>, I>>(
    object: I,
  ): SchedulingSchedulePinGroupList {
    const message = createBaseSchedulingSchedulePinGroupList();
    message.groups = object.groups?.map((e) => SchedulingSchedulePinGroup.fromPartial(e)) || [];
    message.current_revision_id = (object.current_revision_id !== undefined && object.current_revision_id !== null)
      ? Uuid.fromPartial(object.current_revision_id)
      : undefined;
    return message;
  },
};

function createBaseSchedulingPinnedRegenerationProposal(): SchedulingPinnedRegenerationProposal {
  return { run: undefined, proposed_view: undefined, issues: [] };
}

export const SchedulingPinnedRegenerationProposal: MessageFns<SchedulingPinnedRegenerationProposal> = {
  encode(message: SchedulingPinnedRegenerationProposal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.run !== undefined) {
      SchedulingGenerationRun.encode(message.run, writer.uint32(10).fork()).join();
    }
    if (message.proposed_view !== undefined) {
      SchedulingScheduleView.encode(message.proposed_view, writer.uint32(18).fork()).join();
    }
    for (const v of message.issues) {
      SchedulingScheduleAdjustmentIssue.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingPinnedRegenerationProposal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingPinnedRegenerationProposal();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.run = SchedulingGenerationRun.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.proposed_view = SchedulingScheduleView.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.issues.push(SchedulingScheduleAdjustmentIssue.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingPinnedRegenerationProposal {
    return {
      run: isSet(object.run) ? SchedulingGenerationRun.fromJSON(object.run) : undefined,
      proposed_view: isSet(object.proposedView) ? SchedulingScheduleView.fromJSON(object.proposedView) : undefined,
      issues: globalThis.Array.isArray(object?.issues)
        ? object.issues.map((e: any) => SchedulingScheduleAdjustmentIssue.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingPinnedRegenerationProposal): unknown {
    const obj: any = {};
    if (message.run !== undefined) {
      obj.run = SchedulingGenerationRun.toJSON(message.run);
    }
    if (message.proposed_view !== undefined) {
      obj.proposedView = SchedulingScheduleView.toJSON(message.proposed_view);
    }
    if (message.issues?.length) {
      obj.issues = message.issues.map((e) => SchedulingScheduleAdjustmentIssue.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingPinnedRegenerationProposal>, I>>(
    base?: I,
  ): SchedulingPinnedRegenerationProposal {
    return SchedulingPinnedRegenerationProposal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingPinnedRegenerationProposal>, I>>(
    object: I,
  ): SchedulingPinnedRegenerationProposal {
    const message = createBaseSchedulingPinnedRegenerationProposal();
    message.run = (object.run !== undefined && object.run !== null)
      ? SchedulingGenerationRun.fromPartial(object.run)
      : undefined;
    message.proposed_view = (object.proposed_view !== undefined && object.proposed_view !== null)
      ? SchedulingScheduleView.fromPartial(object.proposed_view)
      : undefined;
    message.issues = object.issues?.map((e) => SchedulingScheduleAdjustmentIssue.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingReviewMetric(): SchedulingReviewMetric {
  return { label: undefined, value: undefined };
}

export const SchedulingReviewMetric: MessageFns<SchedulingReviewMetric> = {
  encode(message: SchedulingReviewMetric, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.label !== undefined) {
      writer.uint32(10).string(message.label);
    }
    if (message.value !== undefined) {
      writer.uint32(16).uint32(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingReviewMetric {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingReviewMetric();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.label = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.value = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingReviewMetric {
    return {
      label: isSet(object.label) ? globalThis.String(object.label) : undefined,
      value: isSet(object.value) ? globalThis.Number(object.value) : undefined,
    };
  },

  toJSON(message: SchedulingReviewMetric): unknown {
    const obj: any = {};
    if (message.label !== undefined) {
      obj.label = message.label;
    }
    if (message.value !== undefined) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingReviewMetric>, I>>(base?: I): SchedulingReviewMetric {
    return SchedulingReviewMetric.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingReviewMetric>, I>>(object: I): SchedulingReviewMetric {
    const message = createBaseSchedulingReviewMetric();
    message.label = object.label ?? undefined;
    message.value = object.value ?? undefined;
    return message;
  },
};

function createBaseSchedulingReviewIssue(): SchedulingReviewIssue {
  return {
    action_step: undefined,
    message: undefined,
    code: undefined,
    destination: undefined,
    warning_only: undefined,
  };
}

export const SchedulingReviewIssue: MessageFns<SchedulingReviewIssue> = {
  encode(message: SchedulingReviewIssue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action_step !== undefined) {
      writer.uint32(8).int32(schedulingPreparationStepToNumber(message.action_step));
    }
    if (message.message !== undefined) {
      writer.uint32(18).string(message.message);
    }
    if (message.code !== undefined) {
      writer.uint32(26).string(message.code);
    }
    if (message.destination !== undefined) {
      writer.uint32(32).int32(schedulingReviewActionDestinationToNumber(message.destination));
    }
    if (message.warning_only !== undefined) {
      writer.uint32(40).bool(message.warning_only);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingReviewIssue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingReviewIssue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.action_step = schedulingPreparationStepFromJSON(reader.int32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.code = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.destination = schedulingReviewActionDestinationFromJSON(reader.int32());
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.warning_only = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingReviewIssue {
    return {
      action_step: isSet(object.actionStep) ? schedulingPreparationStepFromJSON(object.actionStep) : undefined,
      message: isSet(object.message) ? globalThis.String(object.message) : undefined,
      code: isSet(object.code) ? globalThis.String(object.code) : undefined,
      destination: isSet(object.destination)
        ? schedulingReviewActionDestinationFromJSON(object.destination)
        : undefined,
      warning_only: isSet(object.warningOnly) ? globalThis.Boolean(object.warningOnly) : undefined,
    };
  },

  toJSON(message: SchedulingReviewIssue): unknown {
    const obj: any = {};
    if (message.action_step !== undefined) {
      obj.actionStep = schedulingPreparationStepToJSON(message.action_step);
    }
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    if (message.code !== undefined) {
      obj.code = message.code;
    }
    if (message.destination !== undefined) {
      obj.destination = schedulingReviewActionDestinationToJSON(message.destination);
    }
    if (message.warning_only !== undefined) {
      obj.warningOnly = message.warning_only;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingReviewIssue>, I>>(base?: I): SchedulingReviewIssue {
    return SchedulingReviewIssue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingReviewIssue>, I>>(object: I): SchedulingReviewIssue {
    const message = createBaseSchedulingReviewIssue();
    message.action_step = object.action_step ?? undefined;
    message.message = object.message ?? undefined;
    message.code = object.code ?? undefined;
    message.destination = object.destination ?? undefined;
    message.warning_only = object.warning_only ?? undefined;
    return message;
  },
};

function createBaseSchedulingReviewSection(): SchedulingReviewSection {
  return { section: undefined, metrics: [], issues: [] };
}

export const SchedulingReviewSection: MessageFns<SchedulingReviewSection> = {
  encode(message: SchedulingReviewSection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.section !== undefined) {
      writer.uint32(8).int32(schedulingReviewSectionTypeToNumber(message.section));
    }
    for (const v of message.metrics) {
      SchedulingReviewMetric.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.issues) {
      SchedulingReviewIssue.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingReviewSection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingReviewSection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.section = schedulingReviewSectionTypeFromJSON(reader.int32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.metrics.push(SchedulingReviewMetric.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.issues.push(SchedulingReviewIssue.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingReviewSection {
    return {
      section: isSet(object.section) ? schedulingReviewSectionTypeFromJSON(object.section) : undefined,
      metrics: globalThis.Array.isArray(object?.metrics)
        ? object.metrics.map((e: any) => SchedulingReviewMetric.fromJSON(e))
        : [],
      issues: globalThis.Array.isArray(object?.issues)
        ? object.issues.map((e: any) => SchedulingReviewIssue.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SchedulingReviewSection): unknown {
    const obj: any = {};
    if (message.section !== undefined) {
      obj.section = schedulingReviewSectionTypeToJSON(message.section);
    }
    if (message.metrics?.length) {
      obj.metrics = message.metrics.map((e) => SchedulingReviewMetric.toJSON(e));
    }
    if (message.issues?.length) {
      obj.issues = message.issues.map((e) => SchedulingReviewIssue.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingReviewSection>, I>>(base?: I): SchedulingReviewSection {
    return SchedulingReviewSection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingReviewSection>, I>>(object: I): SchedulingReviewSection {
    const message = createBaseSchedulingReviewSection();
    message.section = object.section ?? undefined;
    message.metrics = object.metrics?.map((e) => SchedulingReviewMetric.fromPartial(e)) || [];
    message.issues = object.issues?.map((e) => SchedulingReviewIssue.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSchedulingReview(): SchedulingReview {
  return { sections: [], cross_step_issues: [], ready: undefined };
}

export const SchedulingReview: MessageFns<SchedulingReview> = {
  encode(message: SchedulingReview, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.sections) {
      SchedulingReviewSection.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.cross_step_issues) {
      SchedulingReviewIssue.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.ready !== undefined) {
      writer.uint32(24).bool(message.ready);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchedulingReview {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchedulingReview();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.sections.push(SchedulingReviewSection.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.cross_step_issues.push(SchedulingReviewIssue.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.ready = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchedulingReview {
    return {
      sections: globalThis.Array.isArray(object?.sections)
        ? object.sections.map((e: any) => SchedulingReviewSection.fromJSON(e))
        : [],
      cross_step_issues: globalThis.Array.isArray(object?.crossStepIssues)
        ? object.crossStepIssues.map((e: any) => SchedulingReviewIssue.fromJSON(e))
        : [],
      ready: isSet(object.ready) ? globalThis.Boolean(object.ready) : undefined,
    };
  },

  toJSON(message: SchedulingReview): unknown {
    const obj: any = {};
    if (message.sections?.length) {
      obj.sections = message.sections.map((e) => SchedulingReviewSection.toJSON(e));
    }
    if (message.cross_step_issues?.length) {
      obj.crossStepIssues = message.cross_step_issues.map((e) => SchedulingReviewIssue.toJSON(e));
    }
    if (message.ready !== undefined) {
      obj.ready = message.ready;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchedulingReview>, I>>(base?: I): SchedulingReview {
    return SchedulingReview.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchedulingReview>, I>>(object: I): SchedulingReview {
    const message = createBaseSchedulingReview();
    message.sections = object.sections?.map((e) => SchedulingReviewSection.fromPartial(e)) || [];
    message.cross_step_issues = object.cross_step_issues?.map((e) => SchedulingReviewIssue.fromPartial(e)) || [];
    message.ready = object.ready ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
