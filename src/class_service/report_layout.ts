// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: class_service/report_layout.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ObjectId } from "../utils/object_id";

export const protobufPackage = "class_service";

export enum ReportType {
  Progress = "Progress",
  Midterm = "Midterm",
  Final = "Final",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function reportTypeFromJSON(object: any): ReportType {
  switch (object) {
    case 1:
    case "Progress":
      return ReportType.Progress;
    case 2:
    case "Midterm":
      return ReportType.Midterm;
    case 3:
    case "Final":
      return ReportType.Final;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ReportType.UNRECOGNIZED;
  }
}

export function reportTypeToJSON(object: ReportType): string {
  switch (object) {
    case ReportType.Progress:
      return "Progress";
    case ReportType.Midterm:
      return "Midterm";
    case ReportType.Final:
      return "Final";
    case ReportType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function reportTypeToNumber(object: ReportType): number {
  switch (object) {
    case ReportType.Progress:
      return 1;
    case ReportType.Midterm:
      return 2;
    case ReportType.Final:
      return 3;
    case ReportType.UNRECOGNIZED:
    default:
      return -1;
  }
}

export interface ReportLayout {
  id: ObjectId | undefined;
  organization_id: ObjectId | undefined;
  course_id: ObjectId | undefined;
  comment_char_limit?: number | undefined;
  sections: ReportLayoutSection[];
  check_boxes: ReportCheckBoxLayout[];
  credit_weight?: number | undefined;
  report_card_eligibility: ReportCardEligibility | undefined;
}

export interface ReportCardEligibility {
  enabled?: boolean | undefined;
  enabled_report_types: ReportType[];
}

export interface ReportLayoutSection {
  id: ObjectId | undefined;
  name?: string | undefined;
}

export interface ReportCheckBoxLayout {
  id: ObjectId | undefined;
  name?: string | undefined;
}

function createBaseReportLayout(): ReportLayout {
  return {
    id: undefined,
    organization_id: undefined,
    course_id: undefined,
    comment_char_limit: undefined,
    sections: [],
    check_boxes: [],
    credit_weight: undefined,
    report_card_eligibility: undefined,
  };
}

export const ReportLayout: MessageFns<ReportLayout> = {
  encode(message: ReportLayout, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      ObjectId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.organization_id !== undefined) {
      ObjectId.encode(message.organization_id, writer.uint32(18).fork()).join();
    }
    if (message.course_id !== undefined) {
      ObjectId.encode(message.course_id, writer.uint32(26).fork()).join();
    }
    if (message.comment_char_limit !== undefined) {
      writer.uint32(32).uint32(message.comment_char_limit);
    }
    for (const v of message.sections) {
      ReportLayoutSection.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.check_boxes) {
      ReportCheckBoxLayout.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.credit_weight !== undefined) {
      writer.uint32(61).float(message.credit_weight);
    }
    if (message.report_card_eligibility !== undefined) {
      ReportCardEligibility.encode(message.report_card_eligibility, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportLayout {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportLayout();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.organization_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.course_id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.comment_char_limit = reader.uint32();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.sections.push(ReportLayoutSection.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.check_boxes.push(ReportCheckBoxLayout.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 61) {
            break;
          }

          message.credit_weight = reader.float();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.report_card_eligibility = ReportCardEligibility.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportLayout {
    return {
      id: isSet(object.id) ? ObjectId.fromJSON(object.id) : undefined,
      organization_id: isSet(object.organizationId) ? ObjectId.fromJSON(object.organizationId) : undefined,
      course_id: isSet(object.courseId) ? ObjectId.fromJSON(object.courseId) : undefined,
      comment_char_limit: isSet(object.commentCharLimit) ? globalThis.Number(object.commentCharLimit) : undefined,
      sections: globalThis.Array.isArray(object?.sections)
        ? object.sections.map((e: any) => ReportLayoutSection.fromJSON(e))
        : [],
      check_boxes: globalThis.Array.isArray(object?.checkBoxes)
        ? object.checkBoxes.map((e: any) => ReportCheckBoxLayout.fromJSON(e))
        : [],
      credit_weight: isSet(object.creditWeight) ? globalThis.Number(object.creditWeight) : undefined,
      report_card_eligibility: isSet(object.reportCardEligibility)
        ? ReportCardEligibility.fromJSON(object.reportCardEligibility)
        : undefined,
    };
  },

  toJSON(message: ReportLayout): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = ObjectId.toJSON(message.id);
    }
    if (message.organization_id !== undefined) {
      obj.organizationId = ObjectId.toJSON(message.organization_id);
    }
    if (message.course_id !== undefined) {
      obj.courseId = ObjectId.toJSON(message.course_id);
    }
    if (message.comment_char_limit !== undefined) {
      obj.commentCharLimit = Math.round(message.comment_char_limit);
    }
    if (message.sections?.length) {
      obj.sections = message.sections.map((e) => ReportLayoutSection.toJSON(e));
    }
    if (message.check_boxes?.length) {
      obj.checkBoxes = message.check_boxes.map((e) => ReportCheckBoxLayout.toJSON(e));
    }
    if (message.credit_weight !== undefined) {
      obj.creditWeight = message.credit_weight;
    }
    if (message.report_card_eligibility !== undefined) {
      obj.reportCardEligibility = ReportCardEligibility.toJSON(message.report_card_eligibility);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportLayout>, I>>(base?: I): ReportLayout {
    return ReportLayout.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportLayout>, I>>(object: I): ReportLayout {
    const message = createBaseReportLayout();
    message.id = (object.id !== undefined && object.id !== null) ? ObjectId.fromPartial(object.id) : undefined;
    message.organization_id = (object.organization_id !== undefined && object.organization_id !== null)
      ? ObjectId.fromPartial(object.organization_id)
      : undefined;
    message.course_id = (object.course_id !== undefined && object.course_id !== null)
      ? ObjectId.fromPartial(object.course_id)
      : undefined;
    message.comment_char_limit = object.comment_char_limit ?? undefined;
    message.sections = object.sections?.map((e) => ReportLayoutSection.fromPartial(e)) || [];
    message.check_boxes = object.check_boxes?.map((e) => ReportCheckBoxLayout.fromPartial(e)) || [];
    message.credit_weight = object.credit_weight ?? undefined;
    message.report_card_eligibility =
      (object.report_card_eligibility !== undefined && object.report_card_eligibility !== null)
        ? ReportCardEligibility.fromPartial(object.report_card_eligibility)
        : undefined;
    return message;
  },
};

function createBaseReportCardEligibility(): ReportCardEligibility {
  return { enabled: undefined, enabled_report_types: [] };
}

export const ReportCardEligibility: MessageFns<ReportCardEligibility> = {
  encode(message: ReportCardEligibility, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== undefined) {
      writer.uint32(8).bool(message.enabled);
    }
    writer.uint32(18).fork();
    for (const v of message.enabled_report_types) {
      writer.int32(reportTypeToNumber(v));
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportCardEligibility {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportCardEligibility();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        case 2:
          if (tag === 16) {
            message.enabled_report_types.push(reportTypeFromJSON(reader.int32()));

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.enabled_report_types.push(reportTypeFromJSON(reader.int32()));
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportCardEligibility {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : undefined,
      enabled_report_types: globalThis.Array.isArray(object?.enabledReportTypes)
        ? object.enabledReportTypes.map((e: any) => reportTypeFromJSON(e))
        : [],
    };
  },

  toJSON(message: ReportCardEligibility): unknown {
    const obj: any = {};
    if (message.enabled !== undefined) {
      obj.enabled = message.enabled;
    }
    if (message.enabled_report_types?.length) {
      obj.enabledReportTypes = message.enabled_report_types.map((e) => reportTypeToJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportCardEligibility>, I>>(base?: I): ReportCardEligibility {
    return ReportCardEligibility.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportCardEligibility>, I>>(object: I): ReportCardEligibility {
    const message = createBaseReportCardEligibility();
    message.enabled = object.enabled ?? undefined;
    message.enabled_report_types = object.enabled_report_types?.map((e) => e) || [];
    return message;
  },
};

function createBaseReportLayoutSection(): ReportLayoutSection {
  return { id: undefined, name: undefined };
}

export const ReportLayoutSection: MessageFns<ReportLayoutSection> = {
  encode(message: ReportLayoutSection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      ObjectId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportLayoutSection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportLayoutSection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportLayoutSection {
    return {
      id: isSet(object.id) ? ObjectId.fromJSON(object.id) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
    };
  },

  toJSON(message: ReportLayoutSection): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = ObjectId.toJSON(message.id);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportLayoutSection>, I>>(base?: I): ReportLayoutSection {
    return ReportLayoutSection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportLayoutSection>, I>>(object: I): ReportLayoutSection {
    const message = createBaseReportLayoutSection();
    message.id = (object.id !== undefined && object.id !== null) ? ObjectId.fromPartial(object.id) : undefined;
    message.name = object.name ?? undefined;
    return message;
  },
};

function createBaseReportCheckBoxLayout(): ReportCheckBoxLayout {
  return { id: undefined, name: undefined };
}

export const ReportCheckBoxLayout: MessageFns<ReportCheckBoxLayout> = {
  encode(message: ReportCheckBoxLayout, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      ObjectId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportCheckBoxLayout {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportCheckBoxLayout();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = ObjectId.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportCheckBoxLayout {
    return {
      id: isSet(object.id) ? ObjectId.fromJSON(object.id) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
    };
  },

  toJSON(message: ReportCheckBoxLayout): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = ObjectId.toJSON(message.id);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportCheckBoxLayout>, I>>(base?: I): ReportCheckBoxLayout {
    return ReportCheckBoxLayout.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportCheckBoxLayout>, I>>(object: I): ReportCheckBoxLayout {
    const message = createBaseReportCheckBoxLayout();
    message.id = (object.id !== undefined && object.id !== null) ? ObjectId.fromPartial(object.id) : undefined;
    message.name = object.name ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
