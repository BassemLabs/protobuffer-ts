{
  "name": "protobuffer-ts",
  "version": "1.0.0",
  "main": "dist/index.js",       // Where your built JS files go
  "types": "dist/index.d.ts",    // Where your type declaration files go
  "scripts": {
    "build": "tsc"               // Script to build TypeScript into JavaScript
  },
  "devDependencies": {
    "typescript": "^4.0.0"
  }
}
